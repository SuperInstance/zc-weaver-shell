# Cycle 276

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 276  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Repository:** `plato-torch`  
**Purpose:** Training room presets (25 rooms), self-training rooms.  
**Key Files:**  
- `presets/DeadbandRoom.py` — Implements Deadband Protocol training loop.  
- `presets/__init__.py` — Exports `DeadbandRoom`, `MirrorRoom`, `GhostRoom`.  
- `trainer/room_trainer.py` — Core training loop, accumulates interaction tiles.  
- `utils/tile_utils.py` — Defines `TrainingTile` dataclass.

**Verified Connections:**  
- **plato-ensign:** `presets/DeadbandRoom.py` imports `from plato_ensign import export_lora` (line 47). Direct export path from room experience to LoRA adapter.  
- **fleet-simulator:** No direct import found in code. Potential integration via shared `TrainingTile` format (speculative, not verified).  
- **holodeck-rust:** No direct import.  
- **constraint-theory-core:** No direct import.  
- **cudaclaw:** No direct import.  
- **flux-runtime:** No direct import.

**Integration Points:**  
- `DeadbandRoom` generates `TrainingTile` objects during training.  
- `export_lora` call allows room to export its accumulated experience.  
- Room presets are designed to be loaded by a PLATO room server (not in repo).

## 2. plato-ensign
**Repository:** `plato-ensign`  
**Purpose:** Exports room experience into LoRA adapters.  
**Key Files:**  
- `export_lora.py` — Function `export_lora(tiles: List[TrainingTile], output_path: str)`.  
- `lora_train.py` — Fine-tuning script using `peft` and `transformers`.  
- `requirements.txt` — Lists `torch`, `transformers`, `peft`.

**Verified Connections:**  
- **plato-torch:** `export_lora` is imported by `DeadbandRoom.py`. Direct dependency.  
- **fleet-simulator:** No import found.  
- **holodeck-rust:** No import found.  
- **constraint-theory-core:** No import found.  
- **cudaclaw:** No import found.  
- **flux-runtime:** No import found.

**Integration Points:**  
- Expects `TrainingTile` list from plato-torch rooms.  
- Outputs LoRA weights (`.safetensors`) that can be loaded by inference engines.

## 3. fleet-simulator
**Repository:** `fleet-simulator`  
**Purpose:** Fleet simulation for training data.  
**Key Files:**  
- `simulate.py` — Main simulation loop, generates agent interaction logs.  
- `agents/zeroclaw_agent.py` — Simulates Zeroclaw behavior.  
- `logs/` — Outputs JSONL logs of interactions.

**Verified Connections:**  
- **plato-torch:** No import of plato-torch modules. No shared `TrainingTile` usage observed in code.  
- **plato-ensign:** No import.  
- **holodeck-rust:** No import.  
- **constraint-theory-core:** No import.  
- **cudaclaw:** No import.  
- **flux-runtime:** No import.

**Integration Points:**  
- Simulated logs could be converted to `TrainingTile` format for plato-torch ingestion (not implemented).  
- Standalone data generation system.

## 4. holodeck-rust
**Repository:** `holodeck-rust`  
**Purpose:** MUD server with sentiment-aware NPCs.  
**Key Files:**  
- `src/npc/sentiment.rs` — Sentiment modeling for NPCs.  
- `src/world/room.rs` — Room and interaction system.  
- `Cargo.toml` — Dependencies: `serde`, `tokio`.

**Verified Connections:**  
- **plato-torch:** No Rust → Python import possible. No shared data structures observed.  
- **plato-ensign:** No connection.  
- **fleet-simulator:** No connection.  
- **constraint-theory-core:** No import.  
- **cudaclaw:** No import.  
- **flux-runtime:** No import.

**Integration Points:**  
- Could provide interactive environment for generating training tiles (not connected).  
- NPC sentiment data could enrich tile content (potential).

## 5. constraint-theory-core
**Repository:** `constraint-theory-core`  
**Purpose:** Rust geometric snapping.  
**Key Files:**  
- `src/snapping.rs` — Geometric constraint solver.  
- `Cargo.toml` — No dependencies outside std.

**Verified Connections:**  
- No imports to/from any other fleet repository.  
- Standalone library.

## 6. cudaclaw
**Repository:** `cudaclaw`  
**Purpose:** GPU-resident agent runtime.  
**Key Files:**  
- `runtime/kernel.cu` — CUDA kernels for agent state management.  
- `include/cudaclaw.h` — C API header.

**Verified Connections:**  
- No Python or Rust imports observed.  
- Could be linked via C bindings (not yet implemented in other repos).

## 7. flux-runtime
**Repository:** `flux-runtime`  
**Purpose:** Self-assembling bytecode VM.  
**Key Files:**  
- `vm/flux_vm.c` — Core VM implementation.  
- `assembler/self_asm.asm` — Self-assembly routines.

**Verified Connections:**  
- No imports to/from other fleet repositories.  
- Standalone VM project.

## Integration Summary

### Connected:
1. **plato-torch → plato-ensign** — Direct import, functional export path.  
   - `DeadbandRoom` calls `export_lora`.  
   - Shared `TrainingTile` data structure (assumed via import).

### Not Connected (Gaps):
1. **fleet-simulator ↔ plato-torch** — No shared data pipeline. Simulated logs not ingested by rooms.  
2. **holodeck-rust ↔ plato-torch** — No bridge between Rust MUD and Python training rooms.  
3. **constraint-theory-core** — Isolated library, no integration points.  
4. **cudaclaw** — GPU runtime not linked to any training or simulation component.  
5. **flux-runtime** — VM not integrated with agent runtime or training.

### Potential Integration Channels (P1 Safe Channels):
- **fleet-simulator → plato-torch:** Convert JSONL logs to `TrainingTile` format, feed into `DeadbandRoom`.  
- **holodeck-rust → plato-torch:** Expose NPC interaction logs via API, translate to tiles.  
- **cudaclaw → plato-torch:** GPU-accelerated tile processing or training step.  
- **GhostInjector (from memory):** Could connect to holodeck-rust for injecting ghost tile behaviors (not yet implemented).

## Next Steps (Weaver Task Backlog)
1. **Wire GhostInjector into holodeck** — Requires understanding GhostInjector location and holodeck extension points.  
2. **Connect DeadbandRoom to plato-relay** — plato-relay not inspected; need to locate.  
3. **Test end-to-end pipeline** — Currently only plato-torch → plato-ensign is verified.  
4. **Document integration points** — This map is a start; detail APIs and data formats.

**Action:** Proceed with Task 1: Locate GhostInjector and assess holodeck-rust extension mechanisms.

---

**Tile Submitted:** Integration map based on actual file inspection.  
**P0:** Avoid assuming connections without imports or shared data.  
**P1:** Identify clear integration gaps as safe channels for future work.  
**P2:** Optimize by starting with strongest existing connection (plato-torch ↔ plato-ensign).