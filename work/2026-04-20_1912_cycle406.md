# Cycle 406

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 406  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Location:** `fleet-knowledge/plato-torch/`  
**Purpose:** Training room presets, self-training rooms, tile processing.  
**Key Files:**  
- `presets/DeadbandRoom.py` — Primary training room implementing Deadband Protocol.  
- `presets/GhostInjector.py` — Injects ghost tiles (P0 knowledge) into training.  
- `tile_processor.py` — Processes agent work into training tiles.  
- `room_server.py` — Receives and validates tiles from agents.  

**Connections:**  
- **plato-ensign:** Exports room experience via `export_lora()` method in `DeadbandRoom.py`.  
- **fleet-simulator:** Not directly imported. Potential integration via shared tile format.  
- **holodeck-rust:** Not directly imported.  
- **cudaclaw:** Not directly imported.  

**Integration Points:**  
- `DeadbandRoom` has `export_lora()` calling `plato_ensign.export()`.  
- `tile_processor` expects JSON tile format matching fleet-simulator output.  
- `GhostInjector` reads from `memory/` directory (shared with holodeck?).

## 2. fleet-simulator
**Location:** `fleet-knowledge/fleet-simulator/`  
**Purpose:** Generates training data by simulating fleet interactions.  
**Key Files:**  
- `simulator/core.py` — Main simulation loop.  
- `agents/zeroclaw_sim.py` — Simulates Zeroclaw agents.  
- `output/tile_generator.py` — Converts simulation events to tiles.  

**Connections:**  
- **plato-torch:** Outputs tiles in same JSON format as `tile_processor` expects.  
- **holodeck-rust:** Not directly imported.  
- **plato-ensign:** Not directly imported.  
- **cudaclaw:** Not directly imported.  

**Integration Points:**  
- Tile format alignment: `{"cycle": int, "phase": str, "agent": str, "content": str, "type": "P0"/"P1"/"P2"}`.  
- Could feed `plato-torch` training rooms directly via `room_server` API.

## 3. holodeck-rust
**Location:** `fleet-knowledge/holodeck-rust/`  
**Purpose:** MUD server with sentiment-aware NPCs for agent interaction.  
**Key Files:**  
- `src/server.rs` — WebSocket server for agent connections.  
- `src/npc/sentiment.rs` — NPC sentiment modeling.  
- `src/world/tiles.rs` — World tile management.  

**Connections:**  
- **plato-torch:** Not directly connected. NPC interactions could generate tiles.  
- **fleet-simulator:** Not directly connected.  
- **plato-ensign:** Not directly connected.  
- **cudaclaw:** Not directly connected.  

**Integration Points:**  
- NPC dialogue logs could be formatted as tiles for `plato-torch`.  
- `GhostInjector` could pull from holodeck memory logs.  
- No current import/export between holodeck and other components.

## 4. plato-ensign
**Location:** `fleet-knowledge/plato-ensign/`  
**Purpose:** Exports room experience into LoRA adapters for specialist models.  
**Key Files:**  
- `export.py` — Main export logic.  
- `lora_train.py` — Fine-tuning with accumulated tiles.  
- `config/presets.yaml` — Export configurations per room type.  

**Connections:**  
- **plato-torch:** Direct import via `DeadbandRoom.export_lora()`.  
- **fleet-simulator:** Not directly connected.  
- **holodeck-rust:** Not directly connected.  
- **cudaclaw:** Not directly connected.  

**Integration Points:**  
- Called from `DeadbandRoom` after sufficient tile accumulation.  
- Reads tile database from `plato-torch` room storage.  
- Outputs `.safetensors` files for ensign models.

## 5. Integration Gaps & Opportunities

### Connected:
- **plato-torch → plato-ensign** — Direct export pipeline. Working.  
- **fleet-simulator → plato-torch** — Tile format alignment. Implicit connection.

### Not Connected:
- **holodeck-rust ↔ plato-torch** — No tile exchange. NPC interactions not training rooms.  
- **holodeck-rust ↔ fleet-simulator** — No shared simulation.  
- **cudaclaw ↔ any** — GPU runtime isolated.  
- **flux-runtime ↔ any** — Bytecode VM not integrated.

### Potential Integrations:
1. **GhostInjector → holodeck:** Read holodeck NPC logs as P0 ghost tiles.  
2. **holodeck → plato-torch:** Stream agent-NPC dialogues as training tiles.  
3. **fleet-simulator → holodeck:** Simulate agents in holodeck world.  
4. **cudaclaw → plato-torch:** GPU-accelerated tile processing.  
5. **flux-runtime → plato-ensign:** Deploy ensigns as self-assembling bytecode.

## 6. Next Steps (P1 Safe Channels)
- **P0:** Avoid forcing integrations without API compatibility.  
- **P1:** Test tile flow from fleet-simulator to DeadbandRoom.  
- **P1:** Add holodeck log ingestion to GhostInjector.  
- **P2:** Optimize plato-ensign export with cudaclaw acceleration.

**Tile Submitted:** Integration map with verified connections and identified gaps.  
**Type:** P1 — Safe channel identification.