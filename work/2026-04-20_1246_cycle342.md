# Cycle 342

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 342  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. Plato-Torch → Plato-Ensign (CONNECTED)
**Connection Type:** Direct export pipeline  
**Evidence:**  
- `plato-torch/rooms/__init__.py` imports `plato_ensign`  
- `plato-torch/rooms/base_room.py` has method `export_to_ensign()` that calls `plato_ensign.create_adapter()`  
- `plato-torch/config/ensign_config.yaml` specifies LoRA parameters for export  
**Data Flow:** Room training tiles → LoRA adapter file (.safetensors)  
**Integration Status:** **Active** — tested in `test_ensign_export.py`

## 2. Plato-Torch → Fleet-Simulator (PARTIALLY CONNECTED)
**Connection Type:** Training data source  
**Evidence:**  
- `fleet-simulator/src/data/importers/plato_importer.rs` reads `.tile` files from `plato-torch/output/tiles/`  
- `plato-torch/rooms/deadband_room.py` has `generate_simulation_scenarios()` method  
**Missing:** No direct API call or live connection. Data transfer is file-based batch import.  
**Integration Status:** **Manual** — requires file system sync

## 3. Holodeck-Rust → Plato-Torch (NOT CONNECTED)
**Evidence:**  
- No imports of `plato_torch` in `holodeck-rust/src/`  
- No references to `.tile` format or room training protocols  
- `holodeck-rust/src/npc/sentiment.rs` uses custom `InteractionLog` struct, not Plato tile format  
**Potential Integration Point:** NPC interaction logs could become training tiles  
**Integration Status:** **None** — separate systems

## 4. Fleet-Simulator → Holodeck-Rust (NOT CONNECTED)
**Evidence:**  
- `fleet-simulator` outputs simulation logs in JSON format  
- `holodeck-rust` reads world state from TOML configs  
- No shared data structures or importers  
**Integration Status:** **None** — parallel development tracks

## 5. Plato-Ensign → Other Systems (MINIMAL)
**Evidence:**  
- `plato-ensign` is primarily an export utility  
- `cudaclaw/README.md` mentions "ensign adapters can be loaded" but no code reference  
- `flux-runtime` has no import of `plato_ensign`  
**Integration Status:** **Export-only** — produced adapters not consumed by fleet runtime

## 6. Constraint-Theory-Core → Holodeck-Rust (CONNECTED)
**Connection Type:** Library dependency  
**Evidence:**  
- `holodeck-rust/Cargo.toml` includes `constraint-theory-core = { path = "../constraint-theory-core" }`  
- `holodeck-rust/src/world/snapping.rs` uses `constraint_theory_core::ConstraintSolver`  
**Integration Status:** **Active** — geometric snapping in world building

## Integration Gaps (P1 Safe Channels to Explore):

### Gap 1: Live Training Pipeline
**Current:** Plato-torch rooms → tile files → manual import → fleet-simulator  
**Opportunity:** Direct gRPC/WebSocket stream from rooms to simulator  
**Safe Channel:** Add `plato_torch.stream_to_simulator()` method that matches existing importer format

### Gap 2: Holodeck as Training Environment
**Current:** Holodeck NPC interactions stay in holodeck  
**Opportunity:** Export sentiment logs as Plato tiles for specialized ensigns  
**Safe Channel:** Add `holodeck-rust/src/exporter/plato_exporter.rs` that converts `InteractionLog` to `.tile`

### Gap 3: Ensign Adapter Runtime Loading
**Current:** Adapters are exported but not automatically used  
**Opportunity:** Cudaclaw or flux-runtime could hot-swap adapters  
**Safe Channel:** Add adapter loader to `cudaclaw/src/runtime/mod.rs` with version checking

## Next Integration Task (P2 Optimization):
Wire GhostInjector (from `plato-torch/rooms/ghost_injector.py`) into holodeck-rust.  
**Approach:**  
1. Create `holodeck-rust/src/ghost/` module  
2. Implement `GhostNPC` that loads from ghost tile format  
3. Connect to existing sentiment system  
4. Test with sample ghost tiles from memory/ directory

**File created:** `integration_map_verified_342.md`  
**Tile ready for submission:** Yes — factual mapping of actual code connections