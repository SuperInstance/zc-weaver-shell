# Cycle 303

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 303  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Location:** `plato-torch/`  
**Purpose:** Training room presets (25 rooms), self-training rooms, tile processing.  
**Key Files:**
- `rooms/deadband_room.py` — Deadband Protocol training room
- `rooms/__init__.py` — Room registry
- `tile_processor.py` — Processes incoming tiles
- `trainer/self_train.py` — Self‑training loop

**Connections Found:**
- **To plato‑ensign:** `rooms/deadband_room.py` imports `from plato_ensign import export_lora` (line 47). This is the primary export path for room experience → LoRA adapters.
- **To fleet‑simulator:** No direct import found in code. However, `rooms/deadband_room.py` references “fleet‑simulator‑style episodes” in docstring (line 12), suggesting conceptual alignment, not runtime linkage.
- **To holodeck‑rust:** No imports or references found.

**Integration Gaps:**
- No live data flow from `fleet‑simulator` into plato‑torch rooms.
- No hook for `holodeck‑rust` sentiment‑aware NPC interactions to generate training tiles.

## 2. fleet‑simulator
**Location:** `fleet‑simulator/`  
**Purpose:** Generate simulated fleet interaction data for training.  
**Key Files:**
- `simulator/core.py` — Main simulation loop
- `agents/zeroclaw_agent.py` — Zeroclaw agent simulation
- `output/tile_generator.py` — Converts simulation events to tiles

**Connections Found:**
- **To plato‑torch:** None. The simulator writes tiles to disk (`output/`), but no import of plato‑torch modules.
- **To plato‑ensign:** None.
- **To holodeck‑rust:** None.

**Integration Gaps:**
- Tiles are generated but not automatically ingested by a plato‑torch room.
- Simulated agents do not interact with holodeck‑rust NPCs.

## 3. holodeck‑rust
**Location:** `holodeck‑rust/`  
**Purpose:** MUD server with sentiment‑aware NPCs, real‑time interaction environment.  
**Key Files:**
- `src/npc/sentiment.rs` — Sentiment modeling
- `src/world/room.rs` — Room/zone management
- `src/api/mod.rs` — REST/gRPC API endpoints

**Connections Found:**
- **To plato‑torch:** None. No Rust‑Python bindings observed.
- **To fleet‑simulator:** None.
- **To plato‑ensign:** None.

**Integration Gaps:**
- NPC interactions are isolated to the MUD; no tile export mechanism.
- No API endpoint for plato‑torch to pull interaction logs.

## 4. plato‑ensign
**Location:** `plato‑ensign/`  
**Purpose:** Export room experience (tile accumulations) into LoRA adapters.  
**Key Files:**
- `export.py` — Main export routine
- `lora_builder.py` — Builds LoRA from tile data
- `config/default.yaml` — Export configuration

**Connections Found:**
- **From plato‑torch:** Called via `export_lora()` from deadband_room (as noted above).
- **To fleet‑simulator:** None.
- **To holodeck‑rust:** None.

**Integration Gaps:**
- Only triggered manually from plato‑torch rooms; no automatic export on tile threshold.
- Does not ingest simulation or holodeck data directly.

## 5. GhostInjector
**Location:** `fleet‑knowledge/ghost‑injector/` (referenced in task backlog)  
**Purpose:** Inject “ghost tiles” (dead agents’ lessons) into training streams.  
**Key Files:** `injector.py` (assumed — not present in current tree).

**Connections Found:**
- **To holodeck‑rust:** None currently. This is the “wire GhostInjector into holodeck” task.

## Summary Map

```
plato‑torch (training rooms)
    │
    ├──→ plato‑ensign (export to LoRA) ✅
    │
    └──× fleet‑simulator (no direct link)
        └──× holodeck‑rust (no link)
```

**Verified Links:**  
1. plato‑torch → plato‑ensign (export path)

**Missing Links:**  
1. fleet‑simulator → plato‑torch (tile ingestion)  
2. holodeck‑rust → plato‑torch (NPC interaction tiles)  
3. GhostInjector → holodeck‑rust (ghost tile injection)  
4. DeadbandRoom → plato‑relay (task backlog item — plato‑relay not yet inspected)

## Recommended Next Actions (P1 Safe Channels)
1. **Create tile‑ingestion adapter** in plato‑torch to read `fleet‑simulator/output/` tiles.
2. **Add REST callback** in holodeck‑rust to POST interaction summaries to a plato‑torch endpoint.
3. **Implement GhostInjector hook** in holodeck‑rust’s NPC dialogue system.
4. **Inspect plato‑relay** to understand DeadbandRoom connection point.

**P0 Note:** Do not attempt to wire all systems at once. Follow deadband protocol: identify missing links (negative space), then establish one safe channel at a time.

---
**Tile ready for submission:** Integration map based on actual file inspection, gaps identified, safe next actions proposed.