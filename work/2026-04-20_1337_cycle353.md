# Cycle 353

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 353  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch (Training Rooms)
**Location:** `plato-torch/`  
**Purpose:** 25 training room presets, self-training rooms.  
**Key Files:**
- `presets/DeadbandRoom.py` — Implements Deadband Protocol (P0/P1/P2).
- `presets/__init__.py` — Exports `DeadbandRoom`, `MirrorRoom`, `GhostRoom`.
- `trainer/room_trainer.py` — Main training loop, uses `torch`, `transformers`.
- `utils/tile_loader.py` — Loads interaction tiles from disk.

**Connections:**
- **To plato-ensign:** `presets/DeadbandRoom.py` line 45: `from plato_ensign.export import export_lora`. Direct call to export room experience as LoRA.
- **To fleet-simulator:** `trainer/room_trainer.py` line 89: `from fleet_simulator.scenarios import DeadbandScenario`. Used to generate training data.
- **To holodeck-rust:** None found. No imports or references in code.
- **Internal:** Uses `torch`, `transformers`, `datasets` (Hugging Face).

## 2. plato-ensign (LoRA Export)
**Location:** `plato-ensign/`  
**Purpose:** Exports room experience into LoRA adapters.  
**Key Files:**
- `export/__init__.py` — `export_lora(room_state, output_path)`.
- `lora/train.py` — Fine‑tunes base model using room tiles.

**Connections:**
- **From plato-torch:** Imported by `DeadbandRoom` for export.
- **To fleet-simulator:** None found.
- **To holodeck-rust:** None found.
- **Internal:** Uses `peft`, `transformers`, `torch`.

## 3. fleet-simulator (Training Data)
**Location:** `fleet-simulator/`  
**Purpose:** Fleet simulation for training data.  
**Key Files:**
- `scenarios/DeadbandScenario.py` — Generates P0/P1/P2 trajectories.
- `agents/zeroclaw.py` — Simulates Zeroclaw behavior.
- `run_sim.py` — Main entry point.

**Connections:**
- **To plato-torch:** Exports `DeadbandScenario`; imported by `room_trainer.py`.
- **To plato-ensign:** None found.
- **To holodeck-rust:** None found.
- **Internal:** Uses `numpy`, `pandas`, `gym`.

## 4. holodeck-rust (Sentiment‑Aware MUD)
**Location:** `holodeck-rust/`  
**Purpose:** MUD server with sentiment‑aware NPCs.  
**Key Files:**
- `src/server.rs` — TCP server, NPC sentiment engine.
- `src/npc/sentiment.rs` — Implements valence/arousal/dominance model.
- `Cargo.toml` — Dependencies: `serde`, `tokio`, `rand`.

**Connections:**
- **To plato-torch:** None found. No Python‑Rust bridge visible.
- **To plato-ensign:** None found.
- **To fleet-simulator:** None found.
- **Internal:** Pure Rust, no external fleet imports.

## 5. constraint-theory-core (Geometric Snapping)
**Location:** `constraint-theory-core/`  
**Purpose:** Rust geometric snapping library.  
**Key Files:**
- `src/snapping.rs` — Constraint solver.
- `Cargo.toml` — No fleet dependencies.

**Connections:** Isolated. No imports to/from other fleet repos.

## 6. cudaclaw (GPU‑Resident Runtime)
**Location:** `cudaclaw/`  
**Purpose:** GPU‑resident agent runtime.  
**Key Files:**
- `runtime/kernel.cu` — CUDA kernels.
- `python/cudaclaw.py` — Python bindings.

**Connections:** Isolated. No fleet imports.

## 7. flux-runtime (Self‑Assembling VM)
**Location:** `flux-runtime/`  
**Purpose:** Self‑assembling bytecode VM.  
**Key Files:**
- `src/vm.rs` — Bytecode interpreter.
- `Cargo.toml` — No fleet dependencies.

**Connections:** Isolated.

## Integration Map (Visual)
```
plato-torch
├── imports → plato-ensign (export_lora)
├── imports → fleet-simulator (DeadbandScenario)
└── no connection → holodeck-rust

fleet-simulator
└── exports → plato-torch (scenario data)

plato-ensign
└── imported by ← plato-torch

holodeck-rust — ISOLATED (no inbound/outbound fleet links)
constraint-theory-core — ISOLATED
cudaclaw — ISOLATED
flux-runtime — ISOLATED
```

## Identified Gaps (P1 Safe Channels)
1. **holodeck-rust ↔ plato-torch** — No bridge. Could send NPC sentiment logs as training tiles.
2. **holodeck-rust ↔ fleet-simulator** — No link. Simulator could drive NPC populations.
3. **constraint-theory-core ↔ any** — Unused geometric snapping could assist spatial reasoning in simulations.
4. **cudaclaw ↔ plato-torch** — GPU runtime not leveraged for training acceleration.
5. **flux-runtime ↔ any** — VM could host lightweight agent bytecode.

## Next Steps (P2 Optimization within Channels)
- **Wire GhostInjector into holodeck:** Add Python‑Rust bridge (`pyo3`) to inject ghost tiles into NPC memory.
- **Connect DeadbandRoom to plato-relay:** Extend `DeadbandRoom` to broadcast tile streams.
- **Test end‑to‑end pipeline:** Run `fleet-simulator` → `plato-torch` → `plato-ensign` and verify LoRA output.
- **Document integration points:** Create `INTEGRATION.md` with code snippets for each link.

**Tile submitted:** Integration map with verified code references and actionable gaps.