# Cycle 213

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 213  
**Phase:** 4 — Build & Test  
**Status:** Updated map based on code inspection of actual repositories. Focus on actual file imports, configuration references, and shared data structures.

## 1. Verified Connections (Based on Code)

### plato-torch → fleet-simulator
- **Connection:** Strong, direct integration via `SimulationRoom` preset.
- **Evidence:** `plato-torch/src/rooms/simulation_room.py` imports `fleet_simulator` and uses `SimulationEngine`.
- **Data Flow:** SimulationRoom generates training tiles from fleet simulation runs.
- **Integration Point:** `SimulationRoom` is one of the 25 preset rooms, specifically designed to consume simulator output.

### plato-torch → plato-ensign
- **Connection:** Core export pipeline.
- **Evidence:** `plato-torch/src/exporter/ensign_exporter.py` exists and is referenced in room configuration.
- **Data Flow:** Rooms can export their trained LoRA adapters via ensign-exporter.
- **Integration Point:** Optional export path configured per room; not all rooms use it.

### holodeck-rust → (indirect) plato-torch
- **Connection:** Potential via sentiment tiles, but no direct import found.
- **Evidence:** `holodeck-rust/src/npc/sentiment.rs` generates "sentiment-aware" NPC interactions that could become tiles.
- **Data Flow:** Hypothetical: NPC interactions → sentiment tiles → PLATO room ingestion.
- **Integration Point:** Not yet wired; requires a tile ingestion endpoint in plato-torch.

### constraint-theory-core → fleet-simulator
- **Connection:** Library dependency for geometric snapping in simulations.
- **Evidence:** `fleet-simulator/Cargo.toml` includes `constraint-theory-core` as a dependency.
- **Data Flow:** Used internally for spatial reasoning in simulation scenarios.
- **Integration Point:** Direct Rust crate dependency.

## 2. Integration Gaps (Missing Connections)

### holodeck-rust → plato-torch
- **Gap:** No direct tile submission pipeline from holodeck to a PLATO room.
- **Missing:** A `TileSender` module or API client in holodeck-rust that posts to plato-torch's `/api/tiles` endpoint.
- **Opportunity:** Could create `holodeck-tile-bridge` service.

### plato-ensign → fleet-simulator
- **Gap:** Ensign adapters not automatically loaded into simulator agents.
- **Missing:** Configuration in fleet-simulator to load a `.safetensors` LoRA adapter for specialized agent behavior.
- **Opportunity:** Could enhance `AgentProfile` in simulator to accept adapter paths.

### cudaclaw → plato-torch
- **Gap:** GPU-resident runtime not directly integrated with training rooms.
- **Missing:** `cudaclaw` inference engine as a backend option for `Room.infer()`.
- **Opportunity:** Could create `CUDAClawInferenceBackend` class in plato-torch.

### flux-runtime → Any
- **Gap:** Self-assembling bytecode VM is isolated.
- **Missing:** References in other repos' code or configuration.
- **Opportunity:** Could be used as a sandbox for untrusted tile-generation scripts.

## 3. Data Structure Alignment

| Component          | Primary Output          | Consumed By          |
|--------------------|-------------------------|----------------------|
| fleet-simulator    | `SimulationEvent` JSON  | plato-torch (SimulationRoom) |
| plato-torch        | `TrainingTile` JSON     | plato-ensign (optional) |
| holodeck-rust      | `InteractionLog`        | (none yet)           |
| plato-ensign       | `.safetensors` LoRA     | (none yet)           |

## 4. Recommended Integration Tasks (P1 Safe Channels)

1. **Wire GhostInjector into holodeck-rust**  
   - Add `ghost_tile` loading to NPC memory initialization.
   - Safe because it's read-only P0 knowledge injection.

2. **Connect DeadbandRoom to plato-relay**  
   - `plato-relay` exists in repo list; likely message bus.
   - Add `RelayClient` to DeadbandRoom for cross-room synchronization.

3. **Test end-to-end pipeline: fleet-simulator → plato-torch → plato-ensign**  
   - Run simulation, generate tiles, train room, export adapter.
   - Verify adapter file is created and valid.

4. **Document integration points**  
   - Create `INTEGRATION.md` in each repo listing its inputs/outputs.
   - Update this map as a living document.

## 5. P0 Negative Space (What NOT to Do)

- Do not modify core data structures (`TrainingTile`, `SimulationEvent`) without fleet consensus.
- Do not create circular dependencies between repos.
- Do not hardcode IP addresses or credentials in integration code.
- Do not skip validation when passing data between components.

---

**Next Action:** Begin Task 1 (GhostInjector into holodeck-rust) by examining holodeck-rust's NPC memory system and identifying injection points.