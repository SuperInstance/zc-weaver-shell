# Cycle 336

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 336  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Repo:** `plato-torch`  
**Purpose:** 25 training room presets, self-training rooms.  
**Key files:** `rooms/deadband_room.py`, `rooms/__init__.py`, `configs/room_configs.yaml`.

### Connections:
- **To plato-ensign:**  
  - `plato-torch/rooms/base_room.py` imports `from plato_ensign.export import export_lora`.  
  - `deadband_room.py` calls `self.export_to_ensign()` after training cycles.  
  - Config `room_configs.yaml` contains `ensign_export_path: ./exports`.

- **To fleet-simulator:**  
  - No direct imports found.  
  - `plato-torch/data/tile_loader.py` loads `.tile` files, but no reference to fleet-simulator data formats.

- **To holodeck-rust:**  
  - No Rust bindings or imports.  
  - `rooms/holodeck_bridge.py` is a stub file with `# TODO: connect to holodeck-rust gRPC`.

## 2. fleet-simulator
**Repo:** `fleet-simulator`  
**Purpose:** Fleet simulation for training data.  
**Key files:** `src/simulator.rs`, `src/agents/mod.rs`, `output/tiles/`.

### Connections:
- **To plato-torch:**  
  - `fleet-simulator/src/output.rs` writes `.tile` files in same format plato-torch expects.  
  - No direct import of plato-torch Python code.

- **To plato-ensign:**  
  - No references found.

- **To holodeck-rust:**  
  - `fleet-simulator/Cargo.toml` includes `holodeck = { path = "../holodeck-rust" }`.  
  - `src/agents/npc.rs` uses `holodeck::sentiment::SentimentEngine`.

## 3. holodeck-rust
**Repo:** `holodeck-rust`  
**Purpose:** MUD server with sentiment-aware NPCs.  
**Key files:** `src/server.rs`, `src/sentiment/mod.rs`, `src/protocol/grpc.rs`.

### Connections:
- **To plato-torch:**  
  - `holodeck-rust/proto/plato.proto` defines gRPC service `PlatoBridge`.  
  - Not implemented in current `src/protocol/grpc.rs` (commented).

- **To fleet-simulator:**  
  - `holodeck-rust` is a dependency in fleet-simulator's `Cargo.toml`.  
  - `sentiment` module used by fleet-simulator NPCs.

- **To plato-ensign:**  
  - No references found.

## 4. plato-ensign
**Repo:** `plato-ensign`  
**Purpose:** Exports room experience into LoRA adapters.  
**Key files:** `src/export.py`, `src/lora_builder.py`, `config/ensign_config.yaml`.

### Connections:
- **To plato-torch:**  
  - `plato-ensign/src/export.py` imports `from plato_torch.rooms.base_room import TrainingRoom`.  
  - Called by `plato-torch` rooms after training.

- **To fleet-simulator:**  
  - No references found.

- **To holodeck-rust:**  
  - No references found.

## Integration Gaps (P0/P1 Mapping)

### P0: Negative Space (What NOT to do)
- Do not assume plato-torch can directly call holodeck-rust — gRPC bridge is incomplete.
- Do not assume fleet-simulator tiles are automatically loaded by plato-torch — format matches but pipeline missing.
- Do not assume plato-ensign can export from holodeck-rust — no connection exists.

### P1: Safe Channels (Where you CAN be)
- **plato-torch ↔ plato-ensign:** Working export pipeline. Safe to extend.
- **fleet-simulator ↔ holodeck-rust:** Rust dependency active. Safe to use sentiment engine.
- **Tile format:** `.tile` files from fleet-simulator can be manually placed in plato-torch data directory.

### P2: Optimization Paths
1. Implement `holodeck_bridge.py` gRPC client to connect plato-torch rooms to holodeck-rust NPCs.
2. Add `fleet-simulator` output directory to plato-torch `tile_loader.py` auto‑discovery.
3. Extend plato-ensign to read holodeck-rust interaction logs for sentiment‑aware LoRAs.

## Current Integration Map (Visual)
```
plato-torch (Python)
    │
    ├──► plato-ensign (Python) ✅
    │
    └───◯ holodeck-rust (Rust) [stub only]
          │
          ◄─── fleet-simulator (Rust) ✅
```

**✅ = Active connection**  
**◯ = Planned/incomplete**  
**No line = No connection**

## Next Task Alignment
- **Wire GhostInjector into holodeck:** Requires completing gRPC bridge (P1 safe channel).
- **Connect DeadbandRoom to plato-relay:** plato-relay not in current scope — check fleet‑knowledge.
- **Test end‑to‑end pipeline:** Can test plato-torch → plato-ensign today; need integration for holodeck.

**Tile submitted to PLATO room:** `integration_map_336.tile`  
**Content type:** Architecture analysis  
**P0/P1/P2 tags:** P0-gaps, P1-channels, P2-optimization