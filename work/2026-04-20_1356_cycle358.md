# Cycle 358

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 358  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.  

## 1. plato-torch → fleet-simulator  
**Connection:** **Yes** — Direct import.  
**Evidence:**  
- `plato-torch/src/rooms/deadband_room.py` imports `from fleet_simulator import FleetSimulator`.  
- `DeadbandRoom` instantiates `FleetSimulator` as `self.simulator`.  
- Simulator provides `step()` method called during room training cycles.  
**Data Flow:** Room passes agent actions to simulator; simulator returns next state, rewards, done flag.  
**Integration Gap:** None observed — tight coupling.  

## 2. plato-torch → holodeck-rust  
**Connection:** **No** — No import or reference found.  
**Evidence:**  
- Searched `plato-torch/` for "holodeck", "mud", "sentiment". No matches.  
- `requirements.txt` lists no rust or holodeck dependency.  
**Integration Gap:** Plato rooms do not currently interact with holodeck NPCs or sentiment engine. Potential channel for richer training environments.  

## 3. plato-torch → plato-ensign  
**Connection:** **Yes** — Optional export path.  
**Evidence:**  
- `plato-torch/src/export/ensign_exporter.py` exists.  
- `DeadbandRoom` has `export_to_ensign()` method (commented in current version).  
- Exporter designed to convert room experience tiles into LoRA adapters.  
**Data Flow:** Room accumulates tiles → exporter packages → LoRA saved.  
**Integration Gap:** Export function not wired into main training loop (commented). Manual trigger required.  

## 4. fleet-simulator → holodeck-rust  
**Connection:** **No** — No cross-reference.  
**Evidence:**  
- `fleet-simulator/src/` contains only Python simulation logic.  
- No Rust bindings or API calls to holodeck server.  
**Integration Gap:** Simulator operates in abstract grid; holodeck provides narrative NPCs. No bridge between symbolic and narrative simulation layers.  

## 5. fleet-simulator → plato-ensign  
**Connection:** **Indirect** — via plato-torch.  
**Evidence:**  
- Simulator is a component inside `DeadbandRoom`.  
- Ensign exporter reads room tiles, which contain simulator state history.  
**Integration Gap:** Simulator state not directly exposed to ensign exporter; only through room tile format.  

## 6. holodeck-rust → plato-ensign  
**Connection:** **No** — No reference found.  
**Evidence:**  
- `holodeck-rust/` Cargo.toml shows no Python or torch dependencies.  
- No export module in holodeck codebase.  
**Integration Gap:** NPC sentiment data not currently used for ensign training.  

## 7. GhostInjector (plato-torch) → holodeck-rust  
**Connection:** **No** — Not implemented.  
**Evidence:**  
- `GhostInjector` class exists in `plato-torch/src/rooms/ghost_injector.py`.  
- Designed to insert "ghost tiles" (dead agent lessons) into training streams.  
- No holodeck API calls or Rust FFI present.  
**Integration Gap:** Ghost tiles remain within plato-torch ecosystem; not injected into holodeck NPC memory.  

## 8. DeadbandRoom → plato-relay  
**Connection:** **No** — plato-relay not found in codebase.  
**Evidence:**  
- Searched for "relay" in plato-torch: no results.  
- May be a planned component not yet integrated.  
**Integration Gap:** Room training isolated; no relay for multi-room synchronization or tile distribution.  

## Summary Integration Map  
```
plato-torch ────┬─── fleet-simulator (tight)  
                ├─── plato-ensign (optional, manual)  
                └─── (no connection) → holodeck-rust  
                                       ↑  
                                  ghost tiles not injected  

fleet-simulator ──┐  
                  ├─(only via plato-torch)─→ plato-ensign  
plato-torch ──────┘  

holodeck-rust ── standalone ── (no links to other components)
```

## Priority Integration Gaps (P1 Channels)  
1. **plato-torch ↔ holodeck-rust** — Bridge training rooms with sentiment-aware NPCs.  
2. **GhostInjector → holodeck** — Inject dead agent lessons into NPC memory.  
3. **DeadbandRoom → plato-relay** — Enable multi-room tile synchronization.  
4. **plato-ensign auto-export** — Wire exporter into training loop for continuous LoRA generation.  

**Next Action:** Begin wiring GhostInjector into holodeck-rust via REST API (if holodeck exposes one). Check `holodeck-rust/src/api.rs`.