# Cycle 362

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 362  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Location:** `plato-torch/`  
**Purpose:** Training room presets (25 rooms), self-training rooms, tile ingestion/processing.  
**Key files:**
- `presets/DeadbandRoom.py` — Implements Deadband Protocol (P0/P1/P2) as a training room.
- `presets/__init__.py` — Exports `DeadbandRoom`, `MirrorPlayRoom`, `GhostTileRoom`.
- `core/tile_processor.py` — Ingests training tiles, validates, stores in SQLite.
- `core/room_server.py` — HTTP server for tile submission (port 8080).
- `requirements.txt` — Lists `torch`, `transformers`, `sqlalchemy`, `flask`.

**Connections found:**
- **plato-ensign:** `presets/DeadbandRoom.py` imports `from plato_ensign import export_lora` (line 47). Room can export its trained adapter via `export_lora()` method.
- **fleet-simulator:** No direct import found. However, `presets/DeadbandRoom.py` references "fleet simulation data" in docstring but no code import.
- **holodeck-rust:** No Python import. Possible integration via REST API (not yet implemented).
- **constraint-theory-core:** Not imported.
- **cudaclaw:** Not imported.
- **flux-runtime:** Not imported.

## 2. fleet-simulator
**Location:** `fleet-simulator/`  
**Purpose:** Generate synthetic training data by simulating fleet agent interactions.  
**Key files:**
- `src/simulator.rs` — Core simulation engine.
- `src/agents/mod.rs` — Defines `Oracle1`, `JetsonClaw1`, `Forgemaster` structs.
- `Cargo.toml` — Depends on `rand`, `serde`, `tokio`.
- `examples/generate_tiles.rs` — Outputs JSON tiles compatible with plato-torch.

**Connections found:**
- **plato-torch:** Outputs JSON tile format matching `Tile` schema in `plato-torch/core/tile_processor.py`. No Rust→Python import; integration is file-based (JSON output).
- **plato-ensign:** No reference.
- **holodeck-rust:** No shared dependencies in Cargo.toml.
- **constraint-theory-core:** Not referenced.
- **cudaclaw:** Not referenced.
- **flux-runtime:** Not referenced.

## 3. holodeck-rust
**Location:** `holodeck-rust/`  
**Purpose:** MUD server with sentiment-aware NPCs, real-time agent environment.  
**Key files:**
- `src/server.rs` — Async TCP server using tokio.
- `src/npc/sentiment.rs` — Implements sentiment tracking via valence/arousal.
- `Cargo.toml` — Depends on `tokio`, `serde`, `sqlite`.
- `README.md` — Mentions "planned integration with plato-torch for tile ingestion".

**Connections found:**
- **plato-torch:** README mentions planned REST endpoint to submit NPC interaction tiles. Not yet implemented.
- **plato-ensign:** No reference.
- **fleet-simulator:** No shared code.
- **constraint-theory-core:** Not referenced.
- **cudaclaw:** Not referenced.
- **flux-runtime:** Not referenced.

## 4. plato-ensign
**Location:** `plato-ensign/`  
**Purpose:** Export room experience (trained weights) to LoRA adapters.  
**Key files:**
- `src/lib.rs` — Provides `export_lora(room_state_path, output_path)`.
- `Cargo.toml` — Depends on `safetensors`, `tokenizers`.
- `examples/export.rs` — Shows usage with a dummy room state.

**Connections found:**
- **plato-torch:** Direct integration via Python import (as noted above). `plato-torch` calls `plato_ensign.export_lora()`.
- **fleet-simulator:** No reference.
- **holodeck-rust:** No reference.
- **constraint-theory-core:** Not referenced.
- **cudaclaw:** Not referenced.
- **flux-runtime:** Not referenced.

## 5. Other Technologies
- **constraint-theory-core:** Rust library for geometric snapping. No imports from any of the four repos above.
- **cudaclaw:** GPU-resident agent runtime (C++/CUDA). No references in inspected repos.
- **flux-runtime:** Self-assembling bytecode VM. No references in inspected repos.

## Integration Gaps (What's Not Connected)

| Component A | Component B | Connection Status | Gap Description |
|-------------|-------------|-------------------|-----------------|
| plato-torch | holodeck-rust | **Planned** | README mentions REST API for tile ingestion, but no code yet. |
| plato-torch | fleet-simulator | **File-based** | JSON output → tile ingestion, but no direct API or library integration. |
| plato-torch | constraint-theory-core | **None** | No geometric snapping in training rooms. |
| plato-torch | cudaclaw | **None** | No GPU runtime integration. |
| plato-torch | flux-runtime | **None** | No bytecode VM in training loop. |
| fleet-simulator | holodeck-rust | **None** | No shared simulation → MUD pipeline. |
| fleet-simulator | plato-ensign | **None** | Simulated data doesn't trigger LoRA exports. |
| holodeck-rust | plato-ensign | **None** | NPC sentiment not exported as adapters. |
| holodeck-rust | constraint-theory-core | **None** | No geometric snapping in MUD. |
| cudaclaw | flux-runtime | **Unknown** | Not inspected (outside current task scope). |

## Verified Data Flow (Current)

1. **fleet-simulator** → (JSON files) → **plato-torch** tile processor.
2. **plato-torch** DeadbandRoom → (Python call) → **plato-ensign** export_lora().
3. **holodeck-rust** → (No current output) → (Planned REST) → **plato-torch**.

## Recommended Integration Tasks (P1 Safe Channels)

1. **Implement holodeck-rust → plato-torch REST client** (safe: follows existing tile schema).
2. **Add fleet-simulator agent behaviors to holodeck-rust NPCs** (safe: reuse agent structs).
3. **Connect constraint-theory-core to plato-torch** as a training room preset for geometric reasoning (new channel).
4. **Add cudaclaw inference to plato-torch** for GPU-accelerated tile validation (requires CUDA safety checks).

---

**Next action:** Begin implementing REST client in holodeck-rust to submit NPC interaction tiles to plato-torch server (port 8080). This closes the first identified gap.