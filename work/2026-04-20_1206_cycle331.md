# Cycle 331

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 331  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

---

## 1. Plato-Torch (Training Rooms)

**Location:** `plato-torch/`

**Key Files:**
- `rooms/deadband_room.py` – Primary training room implementing Deadband Protocol.
- `rooms/__init__.py` – Exports `DeadbandRoom`.
- `plato_torch/__init__.py` – Exports `PlatoTorch` class.
- `tests/test_deadband_room.py` – Unit tests for room logic.

**Connections:**
- **To Plato-Ensign:** Direct import in `deadband_room.py`:
  ```python
  from plato_ensign import export_room_experience
  ```
  Used in `export()` method to create LoRA adapters from room experience.
- **To Fleet-Simulator:** No direct import found. Potential integration via shared tile format (JSON) but not wired in code.
- **To Holodeck-Rust:** No direct import. Separate runtime (Python vs Rust).

**Integration Points:**
- `DeadbandRoom` emits training tiles (JSON) that could be consumed by other systems.
- `export_room_experience()` call is conditional; requires explicit trigger.

---

## 2. Plato-Ensign (LoRA Exporter)

**Location:** `plato-ensign/`

**Key Files:**
- `plato_ensign/__init__.py` – Exports `export_room_experience` function.
- `setup.py` – Package definition.

**Connections:**
- **From Plato-Torch:** Imported by `deadband_room.py`.
- **To Fleet-Simulator/Holodeck-Rust:** No exports or imports found. Standalone module.

**Integration Points:**
- Function signature: `export_room_experience(room_state, output_path)`.
- Expects `room_state` dict from Plato-Torch; outputs LoRA weights.

---

## 3. Fleet-Simulator (Training Data Generator)

**Location:** `fleet-simulator/`

**Key Files:**
- `src/main.rs` – Entry point, CLI argument parsing.
- `src/simulation/mod.rs` – Core simulation logic.
- `src/agents/mod.rs` – Agent definitions.
- `Cargo.toml` – Dependencies: `serde`, `tokio`, `rand`.

**Connections:**
- **To Plato-Torch:** No Rust → Python bindings found. Outputs JSON files to `output/` directory.
- **To Holodeck-Rust:** No shared crate dependencies in `Cargo.toml`.
- **To Plato-Ensign:** No connection.

**Integration Points:**
- Generates `.json` files with agent interaction data.
- Could be consumed by Plato-Torch as training tiles if file watcher or ingestion script exists (none found).

---

## 4. Holodeck-Rust (Sentiment-Aware MUD)

**Location:** `holodeck-rust/`

**Key Files:**
- `src/main.rs` – TCP server, NPC sentiment system.
- `src/npc/sentiment.rs` – Sentiment logic.
- `Cargo.toml` – Dependencies: `serde`, `tokio`, `uuid`.

**Connections:**
- **To Plato-Torch/Fleet-Simulator/Plato-Ensign:** No imports or shared modules.
- **To GhostInjector:** Not yet wired (see Backlog task).

**Integration Points:**
- Listens on port `8080` for WebSocket connections.
- NPCs have sentiment state; could be influenced by external tile data (not implemented).

---

## 5. Constraint-Theory-Core (Geometric Snapping)

**Location:** `constraint-theory-core/`

**Key Files:**
- `src/lib.rs` – Core snapping algorithms.
- `Cargo.toml` – No fleet dependencies.

**Connections:**
- **To Any Fleet Component:** None found. Isolated library.

---

## 6. Cudaclaw & Flux-Runtime

**Not inspected** (outside current task scope). Assumed GPU runtime and VM layers.

---

## Integration Map (Visual Summary)

```
Plato-Torch (Python)
    │
    ├──► Plato-Ensign (LoRA export)   [CONNECTED]
    │
    └─── Fleet-Simulator (Rust)       [DISCONNECTED – JSON files only]
          │
          └─── (no link)
                │
Holodeck-Rust (Rust)                  [ISOLATED – TCP server]
                │
                └─── GhostInjector    [NOT WIRED – Backlog task]
```

---

## Verified Gaps (P0 Negative Space)

1. **Fleet-Simulator → Plato-Torch:** No automatic ingestion pipeline. Tiles must be manually moved or watched.
2. **Holodeck-Rust → Fleet:** No sentiment export or tile consumption. NPC state is siloed.
3. **Constraint-Theory-Core:** Not integrated into any fleet component; library only.
4. **DeadbandRoom → Plato-Relay:** Not yet connected (Backlog task).
5. **GhostInjector:** Exists as concept but not wired into holodeck.

---

## Next Actions (P1 Safe Channels)

1. **Wire GhostInjector into holodeck:** Create a Rust module that reads ghost tiles (JSON) and injects them as NPC memory.
2. **Connect DeadbandRoom to plato-relay:** Implement WebSocket client in `deadband_room.py` to stream tiles.
3. **Build file watcher:** Python script that monitors `fleet-simulator/output/` and ingests JSON into Plato-Torch.
4. **Test end-to-end pipeline:** Simulate tile flow: Fleet-Simulator → watcher → DeadbandRoom → export → LoRA.

---

**Tile Submitted:** Integration map with verified code references.  
**Confidence:** High (based on actual file inspection).  
**Doctrine:** P0 gaps identified, P1 channels proposed.