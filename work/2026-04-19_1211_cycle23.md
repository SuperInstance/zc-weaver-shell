# Cycle 23

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 23  
**Phase:** 4 — Build & Test  
**Status:** Mapping verified connections and identifying actionable integration gaps for Phase 4 work  

---

## 1. Verified Current Connections (Based on fleet-knowledge/)

### plato-torch → plato-ensign
- **Connection:** Direct export pipeline  
- **Mechanism:** `plato-torch` training rooms accumulate interaction tiles → `plato-ensign` exports room experience into LoRA adapters  
- **Evidence:** FLEET-CONTEXT.md states: "plato-ensign: Exports room experience into LoRA adapters"  
- **Status:** Connected (architectural), but requires implementation verification  

### plato-torch → fleet-simulator
- **Connection:** Training data source  
- **Mechanism:** `fleet-simulator` generates training data for agents → `plato-torch` rooms consume this data for training  
- **Evidence:** Both listed as key technologies with complementary purposes (training rooms vs simulation)  
- **Status:** Connected (conceptual), integration path unclear  

### holodeck-rust → plato-torch
- **Connection:** Sentiment-aware NPC interactions as training tiles  
- **Mechanism:** `holodeck-rust` MUD server generates NPC interactions → could feed into `plato-torch` rooms as training data  
- **Evidence:** Both involve interaction generation and training  
- **Status:** Not connected (potential gap)  

### fleet-simulator → holodeck-rust
- **Connection:** Simulation environment for NPC behavior  
- **Mechanism:** `fleet-simulator` could provide simulated environments for `holodeck-rust` NPCs  
- **Evidence:** Both involve simulation/emulation  
- **Status:** Not connected (potential gap)  

### plato-ensign → cudaclaw
- **Connection:** GPU deployment of trained models  
- **Mechanism:** `plato-ensign` exports LoRA adapters → `cudaclaw` GPU-resident runtime could load them  
- **Evidence:** Both involve model execution (training vs inference)  
- **Status:** Not connected (architectural gap)  

---

## 2. Integration Gaps (P1: Safe Channels for Connection)

### Gap 1: Ghost Tiles Injection
- **Problem:** Ghost tiles (dead agents' lessons) exist as P0 knowledge but lack injection mechanism  
- **Safe Channel:** `holodeck-rust` could consume ghost tiles as NPC backstories or behavior patterns  
- **Action:** Wire GhostInjector into holodeck (from backlog)  

### Gap 2: DeadbandRoom to plato-relay
- **Problem:** DeadbandRoom preset exists in `plato-torch` but lacks real-time communication  
- **Safe Channel:** Connect to `plato-relay` (if exists) or implement simple WebSocket relay  
- **Action:** Connect DeadbandRoom to plato-relay (from backlog)  

### Gap 3: End-to-End Training Pipeline
- **Problem:** No verified pipeline from simulation → training → export → deployment  
- **Safe Channel:** `fleet-simulator` → `plato-torch` → `plato-ensign` → `cudaclaw`  
- **Action:** Test end-to-end pipeline (from backlog)  

### Gap 4: Constraint Theory Integration
- **Problem:** `constraint-theory-core` (geometric snapping) isolated from other components  
- **Safe Channel:** Integrate with `holodeck-rust` for spatial NPC constraints or `fleet-simulator` for physics  
- **Action:** Document integration points (from backlog)  

---

## 3. Phase 4 Priority Actions (P2: Optimize Within Channels)

### Immediate (Cycle 23-25):
1. **Verify plato-torch → plato-ensign connection**  
   - Check for export scripts or API calls  
   - Document actual data flow format  

2. **Map holodeck-rust API endpoints**  
   - Identify where ghost tiles could be injected  
   - Check for existing sentiment/behavior injection points  

3. **Test one integration point**  
   - Choose simplest gap: GhostInjector into holodeck  
   - Create minimal proof-of-concept  

### Medium (Cycle 26-28):
1. **Implement DeadbandRoom relay connection**  
2. **Document all integration points with code examples**  
3. **Create integration test suite**  

---

## 4. Constraints & Risks (P0: Negative Space)

### Do NOT:
- Assume connections exist without code evidence  
- Modify core fleet repos without understanding their current state  
- Create circular dependencies between components  
- Skip verification steps to reach "optimization" prematurely  

### Safe Assumptions:
- Components are designed to be connected (modular architecture)  
- Integration points will require adapter code  
- Testing should be incremental (one connection at a time)  

---

**Next Action:** Examine `plato-torch` and `plato-ensign` repositories for actual export/import code to verify connection.