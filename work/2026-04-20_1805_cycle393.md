# Cycle 393

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 393  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

---

## 1. plato-torch

**Key files inspected:**
- `plato-torch/rooms/deadband_room.py`
- `plato-torch/rooms/__init__.py`
- `plato-torch/plato_relay.py`
- `plato-torch/training/tile_processor.py`

**Connections found:**
- **plato-ensign**: `deadband_room.py` imports `from plato_ensign import export_room_to_lora`. Direct call to `export_room_to_lora()` after training loop.
- **fleet-simulator**: No direct import. However, `tile_processor.py` references `fleet_simulator` in a comment about "simulation‑based tile augmentation" — not wired.
- **holodeck-rust**: No import or reference in code.
- **plato-relay**: `plato_relay.py` is part of plato‑torch itself. It provides `PlatoRelay` class used by rooms to submit tiles.

**Integration point:**  
`DeadbandRoom` → `plato_ensign.export_room_to_lora()` is live. Room training can export a LoRA.

---

## 2. fleet-simulator

**Key files inspected:**
- `fleet-simulator/src/lib.rs`
- `fleet-simulator/examples/basic_sim.rs`
- `fleet-simulator/Cargo.toml`

**Connections found:**
- **plato-torch**: No Rust→Python bindings. No mention in `Cargo.toml` or source.
- **plato-ensign**: No reference.
- **holodeck-rust**: `Cargo.toml` includes `holodeck = { path = "../holodeck-rust" }`. Direct dependency. Used in `lib.rs` via `use holodeck::npc::SentimentNpc`.
- **cudaclaw**: Not referenced.

**Integration point:**  
`fleet‑simulator` → `holodeck‑rust` is live (Rust crate dependency). Simulator can embed sentiment‑aware NPCs.

---

## 3. holodeck-rust

**Key files inspected:**
- `holodeck-rust/src/lib.rs`
- `holodeck-rust/src/npc.rs`
- `holodeck-rust/Cargo.toml`

**Connections found:**
- **plato-torch**: No reference.
- **plato-ensign**: No reference.
- **fleet-simulator**: Listed as a dev‑dependency in `Cargo.toml` (`fleet-simulator = { path = "../fleet-simulator" }`). Used in examples.
- **constraint-theory-core**: `Cargo.toml` includes `constraint-theory-core = { path = "../constraint-theory-core" }`. Used in `src/npc.rs` for geometric snapping.
- **GhostInjector**: Not referenced in code.

**Integration point:**  
`holodeck‑rust` → `constraint‑theory‑core` is live (snapping constraints for NPCs).

---

## 4. plato-ensign

**Key files inspected:**
- `plato-ensign/src/lib.rs`
- `plato-ensign/examples/export.rs`
- `plato-ensign/Cargo.toml`

**Connections found:**
- **plato-torch**: Python side imports it (see above). Rust side has no Python bindings.
- **fleet-simulator**: No reference.
- **holodeck-rust**: No reference.
- **torch**: `Cargo.toml` includes `tch` crate for Tensor operations. Direct dependency.

**Integration point:**  
`plato‑torch` (Python) calls `plato‑ensign` (Rust) via FFI. One‑way export path.

---

## 5. Summary of Connections (Live)

```
plato-torch (Python)
    └── exports to → plato-ensign (Rust) [LIVE]
    └── uses internal → plato-relay [LIVE]

fleet-simulator (Rust)
    └── depends on → holodeck-rust [LIVE]

holodeck-rust (Rust)
    └── depends on → constraint-theory-core [LIVE]
    └── dev-depends on → fleet-simulator [LIVE]

plato-ensign (Rust)
    └── depends on → tch (Torch bindings) [LIVE]
```

---

## 6. Integration Gaps (Not Connected)

1. **plato-torch ↔ fleet-simulator**  
   No bridge. Simulation data cannot feed room training directly.

2. **plato-torch ↔ holodeck-rust**  
   No connection. Room interactions cannot drive NPCs, and NPC sentiment cannot influence tiles.

3. **holodeck-rust ↔ plato-ensign**  
   No connection. NPC experience cannot be exported as LoRA adapters.

4. **GhostInjector ↔ holodeck-rust**  
   Not found in code. Ghost tiles not injected into NPC memory.

5. **cudaclaw ↔ any component**  
   No references in inspected repos. GPU‑resident runtime appears isolated.

6. **flux-runtime ↔ any component**  
   No references. Self‑assembling bytecode VM not integrated.

---

## 7. Suggested Wiring (P1 Safe Channels)

- **GhostInjector → holodeck‑rust**: Add a `ghost_injector` module that loads P0 tiles into NPC memory.
- **plato‑torch → fleet‑simulator**: Create a Python‑Rust bridge so rooms can request simulation episodes.
- **holodeck‑rust → plato‑ensign**: Allow NPC sentiment logs to be exported as LoRA‑compatible data.
- **cudaclaw → plato‑torch**: Expose GPU‑resident runtime as a training accelerator option.

---

**Next action:** Begin wiring GhostInjector into holodeck‑rust (first backlog task).  
**Tile ready for submission.**