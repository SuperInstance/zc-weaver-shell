# Cycle 370

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 370  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch → fleet-simulator
**Connection:** **Direct import**  
- `plato-torch/src/rooms/deadband_room.py` imports `from fleet_simulator.simulator import FleetSimulator` (line 7).  
- `DeadbandRoom` instantiates `FleetSimulator` as `self.simulator` (line 32).  
- Room calls `self.simulator.step()` each training iteration (line 89).  
- Simulator state is logged to room metrics (line 91).  

**Data flow:**  
Room → passes config to simulator → simulator returns step metrics → room logs metrics as training data.

**Integration gap:**  
- Simulator outputs are not yet formatted as training tiles.  
- No two‑way coupling: simulator does not read room state.

## 2. plato-torch → plato-ensign
**Connection:** **Configuration‑based**  
- `plato-torch/config/ensign_export.yaml` defines LoRA adapter export settings.  
- `plato-torch/src/training/trainer.py` has a placeholder `export_to_ensign()` method (line 145‑150).  
- No actual import of `plato-ensign` in code; export is not wired.

**Integration gap:**  
- Training loop does not call `export_to_ensign()`.  
- No pipeline from room experience → LoRA adapter.

## 3. holodeck-rust → (any)
**Connection:** **None found**  
- `holodeck-rust/src/main.rs` has no imports from other fleet repos.  
- `Cargo.toml` lists only standard Rust dependencies (serde, tokio, etc.).  
- No references to `plato-torch`, `fleet-simulator`, or `plato-ensign` in source.  

**Integration gap:**  
- Holodeck is isolated; no sentiment‑aware NPC data flows to other components.

## 4. fleet-simulator → (any)
**Connection:** **Only imported by plato-torch**  
- `fleet-simulator/src/lib.rs` exports `FleetSimulator` struct.  
- No outgoing imports to other fleet repos.  
- Simulator generates internal agent‑state vectors but does not push them elsewhere.

**Integration gap:**  
- Simulator data is consumed only by plato‑torch rooms; not sent to holodeck or ensign.

## 5. plato-ensign → (any)
**Connection:** **Stand‑alone**  
- `plato-ensign/src/lora_exporter.py` has no imports from other fleet repos.  
- Designed to read a generic “room experience” format, but no actual input pipeline exists.

**Integration gap:**  
- No source of room experience data (e.g., from plato‑torch) feeds into ensign.

## 6. GhostInjector (mentioned in task backlog)
**Location:** `plato-torch/src/utils/ghost_injector.py`  
**Status:** Not imported anywhere.  
- Contains `GhostTile` class and `inject_ghost_tiles()` function.  
- No calls from `DeadbandRoom` or other rooms.  

**Integration gap:**  
- Ghost tiles are defined but not injected into training streams.

## Summary Map
```
plato-torch ────→ fleet-simulator (live data flow)
      │
      ├───×──→ plato-ensign (config only, not wired)
      │
      └───×──→ holodeck-rust (no connection)
```

**Isolated components:**  
- holodeck‑rust  
- plato‑ensign  
- GhostInjector

## Recommended Integration Steps (P1 safe channels)
1. **Wire GhostInjector into DeadbandRoom** – call `inject_ghost_tiles()` during room initialization.  
2. **Connect DeadbandRoom to plato‑ensign** – implement `export_to_ensign()` after training loops.  
3. **Pipe simulator metrics to holodeck** – send fleet‑state snapshots to holodeck as NPC context.  

**P0 negative space:** Do not modify core room‑simulator coupling (it works). Do not rewrite holodeck from scratch.