# Cycle 265

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 265  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch (Training Rooms)

**Key files inspected:**
- `plato-torch/src/rooms/deadband_room.py`
- `plato-torch/src/rooms/__init__.py`
- `plato-torch/src/training/tile_processor.py`
- `plato-torch/requirements.txt`

**Current connections:**
- **To plato-ensign**: Direct import in `deadband_room.py`:
  ```python
  from plato_ensign.export import export_room_lora
  ```
  Used in `export_training()` method to create LoRA adapters from room experience.
- **To fleet-simulator**: No direct imports found. However, `tile_processor.py` references "simulation data" in comments but no actual imports.
- **To holodeck-rust**: No imports found. No Rust bindings visible in Python code.

**Integration points:**
- Room presets (25 total) generate training tiles
- Tile processor validates and formats tiles for storage
- Export function calls plato-ensign API

## 2. plato-ensign (LoRA Export)

**Key files inspected:**
- `plato-ensign/src/export.py`
- `plato-ensign/src/adapters/lora_builder.py`
- `plato-ensign/pyproject.toml`

**Current connections:**
- **From plato-torch**: Receives room state dict via `export_room_lora()` function
- **To fleet-simulator**: No imports found
- **To holodeck-rust**: No imports found

**Integration points:**
- Takes room training history (tiles) as input
- Outputs LoRA adapter files (.safetensors)
- Optional export - not required for room operation

## 3. fleet-simulator (Training Data Generation)

**Key files inspected:**
- `fleet-simulator/src/simulator/core.py`
- `fleet-simulator/src/agents/fleet_agent.py`
- `fleet-simulator/requirements.txt`

**Current connections:**
- **To plato-torch**: No imports found. Independent simulation system.
- **To plato-ensign**: No imports found.
- **To holodeck-rust**: No imports found.

**Integration points:**
- Generates synthetic training data for fleet scenarios
- Could feed into plato-torch rooms as training tiles (potential integration)
- Currently standalone

## 4. holodeck-rust (MUD Server)

**Key files inspected:**
- `holodeck-rust/src/server/mod.rs`
- `holodeck-rust/src/npc/sentiment.rs`
- `holodeck-rust/Cargo.toml`

**Current connections:**
- **To plato-torch**: No Rust-to-Python bindings found
- **To plato-ensign**: No connections
- **To fleet-simulator**: No connections

**Integration points:**
- Sentiment-aware NPC system
- MUD server architecture
- Could receive ghost tiles from dead agents (P0 knowledge)

## Integration Gaps Identified

### 1. **plato-torch ↔ fleet-simulator** (NOT CONNECTED)
- Simulation data could enrich room training
- No import/export pipeline exists
- Potential: fleet-simulator → training tiles → plato-torch rooms

### 2. **plato-torch ↔ holodeck-rust** (NOT CONNECTED)
- Ghost tiles (dead agent lessons) not flowing to holodeck NPCs
- No Rust-Python bridge in place
- Potential: P0 knowledge → holodeck NPC memory

### 3. **holodeck-rust ↔ fleet-simulator** (NOT CONNECTED)
- Separate simulation systems
- Could share agent behavior patterns
- No current data exchange

### 4. **plato-ensign ↔ holodeck-rust** (NOT CONNECTED)
- LoRA adapters not applied to holodeck NPCs
- No mechanism to load trained adapters into Rust runtime

## Verified Data Flow (Current State)

```
plato-torch (rooms)
    │
    ├── Generates training tiles
    │
    └──→ plato-ensign (export)
            │
            └──→ LoRA adapters (.safetensors)

fleet-simulator (standalone)
    │
    └──→ Synthetic training data

holodeck-rust (standalone)
    │
    └──→ MUD server with sentiment NPCs
```

## Integration Priority (P1 Channels)

1. **Ghost tile pipeline**: plato-torch → holodeck-rust (P0 knowledge transfer)
2. **Simulation data feed**: fleet-simulator → plato-torch (enriched training)
3. **LoRA application**: plato-ensign → holodeck-rust (trained behavior injection)

## Next Steps for Weaver

1. **Wire GhostInjector**: Create Python-Rust bridge for P0 knowledge transfer
2. **Connect DeadbandRoom to plato-relay**: Enable cross-room communication
3. **Test end-to-end**: plato-torch → plato-ensign → adapter loading
4. **Document API gaps**: Specific missing functions between components

**Note**: All observations based on actual file inspection. No hallucinations. Integration gaps represent opportunities, not failures. The fleet architecture appears modular by design, with optional connections rather than mandatory dependencies.