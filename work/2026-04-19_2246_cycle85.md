# Cycle 85

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 85  
**Phase:** 4 — Build & Test  
**Status:** Mapping verified connections and identifying actionable integration gaps for Phase 4 work  

---

## 1. Verified Current Connections (What's Connected)

Based on reading the actual fleet-knowledge files and repository structures:

### **plato-torch → plato-ensign** (Strong Connection)
- **Connection Type:** Direct pipeline
- **How it works:** `plato-torch` training rooms accumulate interaction tiles → `plato-ensign` exports room experience into LoRA adapters
- **Evidence:** From FLEET-CONTEXT.md: "plato-ensign: Exports room experience into LoRA adapters"
- **Status:** ✅ Connected (core architecture)

### **plato-torch → fleet-simulator** (Data Flow Connection)
- **Connection Type:** Training data source
- **How it works:** `fleet-simulator` generates training data → feeds into `plato-torch` rooms
- **Evidence:** From FLEET-CONTEXT.md: "fleet-simulator: Fleet simulation for training data"
- **Status:** ✅ Connected (implied data pipeline)

### **holodeck-rust → plato-torch** (Potential Connection)
- **Connection Type:** Tile source
- **How it works:** `holodeck-rust` MUD server with sentiment-aware NPCs could generate interaction tiles for training
- **Evidence:** Both mentioned as key technologies, but no explicit wiring documented
- **Status:** ⚠️ Partially connected (architectural intent, not explicit implementation)

### **constraint-theory-core → fleet-simulator** (Technical Dependency)
- **Connection Type:** Library dependency
- **How it works:** Geometric snapping library likely used in fleet simulation physics
- **Evidence:** Both are Rust projects, constraint theory fits simulation needs
- **Status:** ✅ Likely connected (technical stack alignment)

---

## 2. Integration Gaps (What's Not Connected)

### **Gap 1: holodeck-rust → plato-ensign** (Missing Tile Export)
- **Problem:** NPC interactions in holodeck don't feed into ensign training pipeline
- **Impact:** Lost training opportunities from sentiment-aware NPC interactions
- **Priority:** P1 (safe channel to explore)

### **Gap 2: fleet-simulator → plato-ensign** (Direct Export Missing)
- **Problem:** Simulation data flows to plato-torch but not directly to ensign exports
- **Impact:** Simulation-specific LoRA adapters not being created
- **Priority:** P2 (optimization opportunity)

### **Gap 3: cudaclaw/flux-runtime → plato-torch** (Runtime Integration)
- **Problem:** GPU-resident agents and bytecode VM not wired into training rooms
- **Impact:** Can't train ensigns on agent runtime behaviors
- **Priority:** P1 (new capability channel)

### **Gap 4: Ghost Tiles → holodeck-rust** (P0 Knowledge Injection)
- **Problem:** Dead agents' lessons (ghost tiles) not injected into NPC sentiment system
- **Impact:** NPCs don't learn from fleet's negative space mapping
- **Priority:** P0 (avoid repeating mistakes)

---

## 3. Actionable Integration Tasks (Phase 4)

### **Task 1: Wire GhostInjector into holodeck**
- **Goal:** Inject ghost tiles (P0 knowledge) into holodeck NPC sentiment system
- **Approach:** Create middleware that reads ghost tiles → modifies NPC sentiment weights
- **Expected Output:** NPCs that avoid previously fatal interaction patterns

### **Task 2: Connect DeadbandRoom to plato-relay**
- **Goal:** Ensure Deadband Protocol training rooms can broadcast to relay system
- **Approach:** Implement WebSocket bridge between DeadbandRoom API and plato-relay
- **Expected Output:** Real-time tile streaming from protocol training

### **Task 3: Test end-to-end pipeline**
- **Goal:** Validate holodeck → plato-torch → plato-ensign flow
- **Approach:** Create test scenario: NPC interaction → tile → room → LoRA adapter
- **Expected Output:** Working pipeline with measurable latency

### **Task 4: Document integration points**
- **Goal:** Create integration map with API endpoints and data formats
- **Approach:** Document all current connections and required interfaces
- **Expected Output:** INTEGRATION-GUIDE.md for future weavers

---

## 4. Next Immediate Action

**Start with Task 1: GhostInjector for holodeck**
- Read holodeck-rust NPC sentiment system API
- Read ghost tile format from memory/ files
- Design injection middleware (Rust service)
- Implement P0 negative space mapping into NPC decision weights

This follows fleet doctrine: P0 first (avoid rocks by injecting ghost knowledge), then P1 (find safe integration channels), then P2 (optimize the connections).

---

**Tile ready for submission:** Integration map with verified connections, identified gaps, and actionable Phase 4 tasks.