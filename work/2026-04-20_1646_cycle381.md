# Cycle 381

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 381  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

---

## 1. plato-torch → fleet-simulator
**Connection:** **Strong**  
- **plato-torch/rooms/deadband_room.py** imports `from fleet_simulator.simulator import FleetSimulator`.  
- **plato-torch/rooms/deadband_room.py** line 47: `self.simulator = FleetSimulator()` instantiated.  
- **plato-torch/rooms/deadband_room.py** line 112: `self.simulator.step()` called during room step.  
- **plato-torch/rooms/deadband_room.py** line 135: `self.simulator.get_state()` used to generate training tiles.  
- **Data flow:** FleetSimulator provides simulated agent states → DeadbandRoom packages into training tiles.

**Integration Gap:**  
- No observed export of fleet-simulator data to plato-ensign.  
- fleet-simulator appears to be a **data source** for plato-torch, not directly connected to plato-ensign.

---

## 2. plato-torch → plato-ensign
**Connection:** **Strong**  
- **plato-torch/rooms/base_room.py** line 210: `from plato_ensign.exporter import EnsignExporter`.  
- **plato-torch/rooms/base_room.py** line 215: `self.ensign_exporter = EnsignExporter(config_path="ensign_config.yaml")`.  
- **plato-torch/rooms/base_room.py** line 245: `self.ensign_exporter.export(training_tiles, self.room_id)` called after tile accumulation.  
- **Data flow:** Training tiles from room → EnsignExporter → LoRA adapters.

**Integration Gap:**  
- **plato-ensign** appears to be an **optional export module**, not required for room operation.  
- No observed import of plato-ensign into fleet-simulator or holodeck-rust.

---

## 3. holodeck-rust → plato-torch
**Connection:** **Weak / Indirect**  
- **holodeck-rust/src/lib.rs** line 45: `pub struct SentimentNPC { ... }` includes `tile_buffer: Vec<TrainingTile>`.  
- **holodeck-rust/src/npc.rs** line 89: `fn generate_tile(&self) -> TrainingTile` produces tile from NPC interactions.  
- **TrainingTile** struct in holodeck-rust matches plato-torch **training_tile.py** schema (same fields: `id`, `timestamp`, `content`, `metadata`).  
- No direct import of plato-torch in holodeck-rust (Rust → Python bridge not evident in code).  
- Likely integration via **shared data contract** (TrainingTile JSON) over network/API (not yet implemented).

**Integration Gap:**  
- No observed Rust FFI or Python bindings in holodeck-rust to call plato-torch directly.  
- No plato-torch import of holodeck-rust modules.  
- Connection appears **design-time compatible** but **runtime disconnected**.

---

## 4. fleet-simulator → holodeck-rust
**Connection:** **None Observed**  
- **fleet-simulator/** Python codebase, no Rust imports.  
- **holodeck-rust/** no Python interop in current src.  
- No shared data structures or cross-references in code.

**Integration Gap:**  
- Complete separation of concerns: fleet-simulator for agent simulation, holodeck-rust for sentiment NPCs.  
- Potential integration point: simulated agents interacting with NPCs (not implemented).

---

## 5. plato-ensign → Other Components
**Connection:** **Export-Only**  
- **plato-ensign/exporter.py** line 33: `def export(tiles, room_id):` → writes LoRA adapters to disk.  
- No imports of plato-ensign in fleet-simulator or holodeck-rust.  
- plato-ensign is a **terminal sink** in pipeline: tiles → LoRA.

**Integration Gap:**  
- No feedback loop from plato-ensign back to rooms or simulation.

---

## 6. constraint-theory-core, cudaclaw, flux-runtime
**Connection:** **Isolated / Not Integrated**  
- **constraint-theory-core/** Rust library for geometric snapping. No imports by plato-torch, fleet-simulator, holodeck-rust.  
- **cudaclaw/** GPU runtime. No Python bindings observed in fleet repos.  
- **flux-runtime/** self-assembling bytecode VM. No cross-references.  
- These appear to be **standalone technologies** awaiting integration.

---

## Summary Map

```
plato-torch (Python)
│
├──► fleet-simulator (Python) [strong: imports, calls]
│
├──► plato-ensign (Python) [strong: exports tiles → LoRA]
│
└──◄ holodeck-rust (Rust) [weak: shared TrainingTile schema, no runtime bridge]
```

**Missing Connections (Integration Gaps):**
1. **holodeck-rust → plato-torch runtime bridge** (FFI/API needed).
2. **fleet-simulator → holodeck-rust interaction** (simulated agents + NPCs).
3. **constraint-theory-core, cudaclaw, flux-runtime** not wired into main pipeline.
4. **GhostInjector** (from memory files) not referenced in any inspected code.
5. **plato-relay** not found in current repo inspection.

---

## Recommended Next Task (Weaver)
**Wire GhostInjector into holodeck-rust**  
- **Location:** `memory/ghost_injector.py` (assumed).  
- **Integration point:** holodeck-rust NPCs could load ghost tiles as P0 negative knowledge.  
- **Action:** Create Rust FFI or REST endpoint in holodeck-rust to accept ghost tile injection.  
- **Test:** Inject sample ghost tile, verify NPC behavior avoids deadband (P0).