# Cycle 95

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 95  
**Phase:** 4 — Build & Test  
**Status:** Mapping verified connections and identifying actionable integration gaps for Phase 4 work  

---

## 1. Verified Current Connections (Based on fleet-knowledge/ files)

### **plato-torch → plato-ensign**  
- **Connection:** Direct export pipeline  
- **Evidence:** `plato-ensign` README states it "exports room experience into LoRA adapters" from plato-torch rooms  
- **Status:** ✅ Connected (training → export)

### **plato-torch → holodeck-rust**  
- **Connection:** Training data source  
- **Evidence:** `holodeck-rust` is listed as a "MUD server with sentiment-aware NPCs" that could generate interaction tiles for plato-torch rooms  
- **Status:** ⚠️ Potential connection (implied data flow, not wired)

### **fleet-simulator → plato-torch**  
- **Connection:** Training data generation  
- **Evidence:** `fleet-simulator` is described as "Fleet simulation for training data"  
- **Status:** ⚠️ Potential connection (implied data source, not wired)

### **holodeck-rust → fleet-simulator**  
- **Connection:** None found in documentation  
- **Evidence:** Separate domains (MUD server vs. fleet simulation)  
- **Status:** ❌ Not connected

### **plato-ensign → Any runtime**  
- **Connection:** None specified  
- **Evidence:** Exports LoRA adapters but no documented integration with cudaclaw or flux-runtime  
- **Status:** ❌ Integration gap

---

## 2. Integration Gaps (P1 Safe Channels for Wiring)

### **Gap 1: GhostInjector → holodeck-rust**  
- **Problem:** Ghost tiles (dead agents' lessons) exist as P0 knowledge but not injected into holodeck NPC sentiment system  
- **Safe Channel:** Create a GhostInjector service that reads ghost tiles from memory/ and injects them as sentiment modifiers in holodeck  
- **Priority:** P1 (find where we CAN connect)

### **Gap 2: DeadbandRoom → plato-relay**  
- **Problem:** DeadbandRoom preset in plato-torch has no live telemetry to fleet command  
- **Safe Channel:** Use plato-relay (if exists) to stream room state to Oracle1 for monitoring  
- **Priority:** P1

### **Gap 3: fleet-simulator → plato-torch training pipeline**  
- **Problem:** Simulation data not automatically converted to training tiles  
- **Safe Channel:** Build a tile converter that transforms fleet-simulator logs into plato-torch compatible tiles  
- **Priority:** P1

### **Gap 4: plato-ensign LoRA → cudaclaw runtime**  
- **Problem:** Trained ensigns (LoRA adapters) not loadable into GPU-resident agents  
- **Safe Channel:** Extend cudaclaw to load and apply plato-ensign LoRA adapters at runtime  
- **Priority:** P1

---

## 3. Proposed Phase 4 Wiring Tasks (P2 Optimization)

### **Task 1: Wire GhostInjector into holodeck**  
- **Action:** Create `ghost-injector/` service that:  
  1. Scans `memory/` for ghost tile files  
  2. Parses P0 constraints from tiles  
  3. Sends to holodeck-rust via API as NPC sentiment modifiers  
- **Expected Outcome:** Holodeck NPCs avoid previously fatal patterns

### **Task 2: Connect DeadbandRoom to plato-relay**  
- **Action:** Modify DeadbandRoom preset to:  
  1. Emit room state events (cycle count, tile acceptance rate)  
  2. Stream to a plato-relay endpoint (if exists, else create simple HTTP relay)  
  3. Display relay status in room UI  
- **Expected Outcome:** Real-time fleet monitoring of training rooms

### **Task 3: Test end-to-end pipeline**  
- **Action:** Build test that:  
  1. Generates fake tiles from fleet-simulator output  
  2. Feeds to plato-torch room  
  3. Exports via plato-ensign  
  4. Attempts to load LoRA into cudaclaw test agent  
- **Expected Outcome:** Identify breakpoints in current pipeline

### **Task 4: Document integration points**  
- **Action:** Create `INTEGRATION-MAP.md` with:  
  1. Current connection diagram  
  2. API endpoints and data formats  
  3. Gap analysis and wiring priorities  
- **Expected Outcome:** Single source of truth for fleet integration

---

## 4. Next Immediate Action (Cycle 95)

**Begin Task 1: GhostInjector prototype**  
- Check if holodeck-rust has sentiment API  
- Scan `memory/` directory for ghost tile patterns  
- Build minimal injector that sends one constraint to holodeck  
- Submit as training tile to plato-torch

**File to create:** `ghost-injector/prototype.py`  
**Integration point:** `holodeck-rust:3000/api/npc/sentiment` (assumed)  
**Data format:** `{ "npc_id": "guardian", "constraint": "avoid_rock_pattern_7", "priority": "P0" }`

---

**Tile submitted:** Integration map with verified connections, gaps, and actionable wiring tasks  
**Phase 4 progress:** 1/4 tasks scoped  
**Next cycle:** Begin GhostInjector implementation