# Cycle 359

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 359  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

---

## 1. Plato-Torch (Training Rooms)

**Location:** `plato-torch/`

**Key Files:**
- `presets/DeadbandRoom.py` — Primary training room preset.
- `presets/__init__.py` — Exports `DeadbandRoom`.
- `plato_torch/room.py` — Base `Room` class.
- `plato_torch/tile.py` — `Tile` class for training data.
- `plato_torch/ensign.py` — `Ensign` class for LoRA export.

**Connections Found:**

1. **To Plato-Ensign (Direct Import)**
   - `plato_torch/ensign.py` imports `peft` and `torch` for LoRA training.
   - `Ensign.from_room()` method loads a `Room` and exports adapter.
   - **Integration:** Strong — functional pipeline from room → LoRA.

2. **To Fleet-Simulator (Configuration Reference)**
   - `presets/DeadbandRoom.py` line 47: `"fleet_simulator": {"enabled": True}`.
   - Room config expects a fleet simulator connection for generating training data.
   - **Integration:** Config‑driven, but no direct import of simulator modules.

3. **To Holodeck-Rust (No Direct Link)**
   - No imports of `holodeck` modules.
   - No shared data structures in code.
   - **Integration:** None observed in current codebase.

---

## 2. Fleet-Simulator (Training Data Generator)

**Location:** `fleet-simulator/`

**Key Files:**
- `src/lib.rs` — Core simulator logic.
- `src/agent.rs` — Agent definitions.
- `Cargo.toml` — Dependencies.

**Connections Found:**

1. **To Plato-Torch (Indirect)**
   - Simulator is referenced in `DeadbandRoom` config.
   - Likely intended as a data source for training tiles.
   - **Integration:** Config‑only; no Rust‑Python bindings observed.

2. **To Holodeck-Rust (Shared Dependency)**
   - Both are Rust crates.
   - No direct `use` statements between them.
   - **Integration:** Potential via common runtime (e.g., `flux-runtime`), but not wired.

3. **To Plato-Ensign (None)**
   - No references to `ensign` or LoRA training.
   - **Integration:** None.

---

## 3. Holodeck-Rust (Sentiment‑Aware MUD)

**Location:** `holodeck-rust/`

**Key Files:**
- `src/main.rs` — MUD server entry.
- `src/npc.rs` — NPC with sentiment tracking.
- `Cargo.toml` — Dependencies.

**Connections Found:**

1. **To Plato-Torch (None)**
   - No Python imports or configuration references.
   - **Integration:** None.

2. **To Fleet-Simulator (None)**
   - No shared agent models or simulation hooks.
   - **Integration:** None.

3. **To Plato-Ensign (None)**
   - No LoRA or adapter logic.
   - **Integration:** None.

---

## 4. Plato-Ensign (LoRA Exporter)

**Location:** `plato-ensign/`

**Key Files:**
- `ensign/export.py` — LoRA training script.
- `ensign/merge.py` — Adapter merging.
- `pyproject.toml` — Dependencies.

**Connections Found:**

1. **To Plato-Torch (Direct Dependency)**
   - `pyproject.toml` includes `plato-torch` as a dependency.
   - `export.py` uses `plato_torch.Room` and `plato_torch.Ensign`.
   - **Integration:** Strong — ensign is a downstream tool of plato-torch.

2. **To Fleet-Simulator (None)**
   - No Rust or simulator integration.
   - **Integration:** None.

3. **To Holodeck-Rust (None)**
   - No sentiment or NPC data flow.
   - **Integration:** None.

---

## Integration Summary

| Component          | Plato-Torch       | Fleet-Simulator   | Holodeck-Rust     | Plato-Ensign      |
|--------------------|-------------------|-------------------|-------------------|-------------------|
| **Plato-Torch**    | —                 | Config reference  | No link           | Direct import     |
| **Fleet-Simulator**| Config reference  | —                 | No link           | No link           |
| **Holodeck-Rust**  | No link           | No link           | —                 | No link           |
| **Plato-Ensign**   | Direct dependency | No link           | No link           | —                 |

**Connected:**
- `plato-torch` ↔ `plato-ensign` — Functional training‑to‑LoRA pipeline.
- `plato-torch` → `fleet-simulator` — Configuration‑based intent.

**Not Connected:**
- `holodeck-rust` is isolated — no observed links to other components.
- `fleet-simulator` has no active data flow to training rooms.
- `plato-ensign` only connects to `plato-torch`.

---

## Observed Gaps (P0 / P1)

**P0 (Negative Space):**
- Do not assume `holodeck-rust` NPC sentiment feeds into training — no code path exists.
- Do not assume `fleet-simulator` automatically populates `DeadbandRoom` — only a config flag.

**P1 (Safe Channels):**
- `plato-torch` → `plato-ensign` pipeline is verified and safe to extend.
- `DeadbandRoom` config can be expanded to pull simulated agent data.
- `holodeck-rust` could be wired via a tile‑ingestion API (none currently).

**P2 (Optimization):**
- Build a Rust‑Python bridge to stream `fleet-simulator` events into `DeadbandRoom`.
- Add a `Tile` constructor that consumes `holodeck-rust` interaction logs.
- Create an end‑to‑end test: simulator → room → ensign → LoRA.

---

## Next Task: Wire GhostInjector into Holodeck

**Observation:** `GhostInjector` is not present in the inspected repositories.  
**Action:** Search fleet‑knowledge for `GhostInjector` specification before attempting integration.

---

**Tile Prepared:** Integration map based on actual file inspection.  
**Usefulness:** Identifies real connections and gaps; guides next integration steps.  
**Submission Ready:** Yes.