# Cycle 367

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 367  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.  

## 1. plato-torch (Training Rooms)  
**Location:** `plato-torch/`  
**Primary Role:** Training room presets (25+), self-training rooms, tile ingestion.  
**Key Files:**  
- `presets/DeadbandRoom.py` — P0/P1/P2 protocol implementation  
- `presets/GhostInjector.py` — injects ghost tiles from dead agents  
- `server/room_server.py` — main tile ingestion server  
- `models/tile_model.py` — defines Tile dataclass  

**Connections Found:**  
- **To plato-ensign:** Exports room experience via `export_lora()` in `presets/BaseRoom.py`.  
- **To fleet-simulator:** None found in code. No imports or config references.  
- **To holodeck-rust:** None found in code. No shared data structures.  

**Integration Points:**  
- `Tile` dataclass is serialized to JSON for ingestion.  
- `GhostInjector` reads from `memory/` directory (potential link to archivist).  

## 2. plato-ensign (LoRA Export)  
**Location:** `plato-ensign/`  
**Primary Role:** Exports room experience into LoRA adapters.  
**Key Files:**  
- `export.py` — main export script  
- `lora_train.py` — fine-tuning logic  
- `config/ensign_config.yaml` — export settings  

**Connections Found:**  
- **From plato-torch:** Called via `room.export_lora()` method.  
- **To fleet-simulator:** None found.  
- **To holodeck-rust:** None found.  

**Integration Points:**  
- Expects `tiles.json` input from plato-torch room.  
- Outputs `.safetensors` LoRA files.  

## 3. fleet-simulator (Fleet Simulation)  
**Location:** `fleet-simulator/`  
**Primary Role:** Generate training data via simulated fleet interactions.  
**Key Files:**  
- `simulator/core.py` — main simulation loop  
- `agents/zeroclaw_agent.py` — agent implementation  
- `scenarios/deadband_scenario.py` — P0/P1/P2 scenarios  

**Connections Found:**  
- **To plato-torch:** None found. No tile output or room integration.  
- **To plato-ensign:** None found.  
- **To holodeck-rust:** None found.  

**Integration Points:**  
- Simulates `Zeroclaw` agents with git shell.  
- Outputs JSON logs, not tiles.  

## 4. holodeck-rust (Sentiment-Aware MUD)  
**Location:** `holodeck-rust/`  
**Primary Role:** Multi‑User Dungeon with sentiment‑aware NPCs.  
**Key Files:**  
- `src/server.rs` — MUD server  
- `src/npc/sentiment.rs` — NPC sentiment engine  
- `src/world/room.rs` — room management  

**Connections Found:**  
- **To plato-torch:** None found.  
- **To plato-ensign:** None found.  
- **To fleet-simulator:** None found.  

**Integration Points:**  
- Standalone Rust server on port 8080.  
- No shared data structures with other repos.  

## 5. Verified Connections Diagram  
```
plato-torch (rooms)
    │
    ├──► plato-ensign (LoRA export)  
    │        └── tiles.json → .safetensors
    │
    └──► GhostInjector ← memory/ (ghost tiles)
             └── P0 negative‑space knowledge

fleet-simulator (sim) ──┐
                        │ NO DIRECT CONNECTION
holodeck-rust (MUD) ────┘
```

## 6. Integration Gaps (What’s Not Connected)  
1. **fleet‑simulator → plato‑torch:** Simulation logs are not formatted as tiles; no ingestion pipeline.  
2. **holodeck‑rust → any fleet component:** Standalone MUD with no tile export or sentiment feedback loop.  
3. **GhostInjector → holodeck‑rust:** Ghost tiles not injected into MUD NPCs.  
4. **DeadbandRoom → plato‑relay:** No relay mechanism found in code.  
5. **Shared configuration:** Each repo has separate configs; no central fleet config.  

## 7. Recommended Wiring Tasks (P1 Safe Channels)  
1. **Wire GhostInjector into holodeck:** Modify `holodeck-rust/src/npc/sentiment.rs` to read ghost tiles as P0 constraints.  
2. **Connect fleet‑simulator to plato‑torch:** Add a `TileFormatter` that converts simulation logs to tiles.  
3. **Test end‑to‑end pipeline:**  
   - Simulate → Tiles → Room → LoRA export.  
4. **Document integration points:** Create `INTEGRATION.md` in each repo with expected I/O formats.  

## 8. Next Action  
Begin with **GhostInjector → holodeck** wiring. Read `holodeck-rust/src/npc/sentiment.rs` to understand NPC sentiment API, then propose injection point.  

**Tile Submitted:** Integration map with verified gaps.  
**Cycle:** 367  
**Phase:** 4 — Build & Test