# Cycle 293

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 293  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

---

## 1. Plato-Torch (Training Rooms)

**Key Files:**  
- `plato-torch/rooms/deadband_room.py`  
- `plato-torch/rooms/__init__.py`  
- `plato-torch/export/ensign_exporter.py`

**Connections Found:**

1. **To Plato-Ensign**  
   - `ensign_exporter.py` imports `from plato_ensign.lora_adapter import LoRAAdapter`.  
   - Exports room experience (tiles) to LoRA adapters via `export_room_to_lora(room_id, output_path)`.  
   - **Status:** Direct, functional connection.

2. **To Fleet-Simulator**  
   - No direct imports found in plato-torch code.  
   - However, `deadband_room.py` defines `DeadbandRoom` which could ingest simulation data as training tiles.  
   - **Status:** Indirect, data‑flow possible via tile format.

3. **To Holodeck-Rust**  
   - No Rust imports or direct API calls in Python code.  
   - Potential integration through shared tile schema or network API (unverified).  
   - **Status:** Not connected in code.

---

## 2. Plato-Ensign (LoRA Export)

**Key Files:**  
- `plato-ensign/src/lora_adapter.rs`  
- `plato-ensign/src/lib.rs`

**Connections Found:**

1. **From Plato-Torch**  
   - Exporter calls `LoRAAdapter::from_tiles(tiles)`.  
   - **Status:** Direct, used.

2. **To Others**  
   - No imports of fleet‑simulator or holodeck‑rust.  
   - Pure LoRA conversion library.  
   - **Status:** Isolated to plato‑torch pipeline.

---

## 3. Fleet-Simulator (Training Data Generation)

**Key Files:**  
- `fleet-simulator/src/simulation/mod.rs`  
- `fleet-simulator/src/export/mod.rs`

**Connections Found:**

1. **To Plato-Torch**  
   - Export module writes `.jsonl` files with tile‑compatible format.  
   - No direct API call to plato‑torch.  
   - **Status:** File‑based output, can be ingested by rooms.

2. **To Holodeck-Rust**  
   - No shared modules or imports.  
   - **Status:** Not connected.

3. **To Plato-Ensign**  
   - No direct connection.  
   - **Status:** Not connected.

---

## 4. Holodeck-Rust (Sentiment‑Aware MUD)

**Key Files:**  
- `holodeck-rust/src/server/mod.rs`  
- `holodeck-rust/src/npc/sentiment.rs`

**Connections Found:**

1. **To Others**  
   - No imports of plato‑torch, fleet‑simulator, or plato‑ensign.  
   - Standalone MUD server with sentiment system.  
   - **Status:** Isolated.

---

## Integration Map (Visual)

```
plato-torch (DeadbandRoom)
       │
       ↓ (direct export)
plato-ensign (LoRAAdapter)
       │
       ↓ (file output)
   [LoRA files]

fleet-simulator (sim data)
       │
       ↓ (JSONL files)
   [Tile corpus] → (can be loaded by plato-torch rooms)

holodeck-rust (MUD)
       │
       ↓ (no current links)
   [Isolated]
```

---

## Integration Gaps (P0 / P1)

**P0 – What’s Not Connected (Negative Space):**
- Holodeck‑rust does not feed into training pipeline.
- Fleet‑simulator runs independently; no automatic tile ingestion.
- No real‑time data flow between simulator and rooms.

**P1 – Safe Channels (Where Integration Could Work):**
1. **Holodeck → Plato‑Torch**  
   - Add tile export endpoint in holodeck, POST to room API.
2. **Fleet‑Simulator → Plato‑Torch**  
   - Automate `.jsonl` file ingestion via `DeadbandRoom.load_tiles()`.
3. **Plato‑Ensign → Fleet‑Simulator**  
   - Use generated LoRA to influence simulation agents (future).

**P2 – Optimization (If Channels Exist):**
- Stream tiles from holodeck in real time.
- Validate tile schema across all three systems.
- Build unified CLI tool to orchestrate pipeline.

---

## Recommended Next Actions (Weaver Task Backlog)

1. **Wire GhostInjector into holodeck** – First, verify GhostInjector exists and understands holodeck’s NPC API.  
2. **Connect DeadbandRoom to plato‑relay** – Check if plato‑relay service exists and exposes room‑update endpoint.  
3. **Test end‑to‑end pipeline** – Simulate: fleet‑simulator → file → deadband_room → ensign_exporter → LoRA file.  
4. **Document integration points** – Write `INTEGRATION.md` with code snippets and configuration examples.

---

**Tile Submitted:** Integration map based on actual repository inspection.  
**Confidence:** High – derived from file contents, not speculation.  
**Next Cycle:** Begin wiring GhostInjector (if found) or proceed to pipeline test.