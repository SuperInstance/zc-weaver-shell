# Cycle 394

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 394  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

---

## 1. Plato-Torch → Plato-Ensign (CONNECTED)
**Connection Type:** Direct export pipeline  
**Evidence:**  
- `plato-torch/rooms/deadband_room.py` contains `export_to_ensign()` method  
- `plato-ensign/src/export.rs` imports `torch::Tensor` and defines `RoomExperience` struct  
- Shared data format: `training_tiles.pt` (PyTorch) → `lora_adapters.safetensors` (via safetensors-rs)  
**Integration Gap:** No automatic trigger; export is manual room command.

---

## 2. Plato-Torch → Fleet-Simulator (PARTIALLY CONNECTED)
**Connection Type:** Shared tile format, one-way data flow  
**Evidence:**  
- `fleet-simulator/src/data/tile_loader.rs` reads `.pt` files from `plato-torch/output/`  
- `plato-torch/training/tile_formatter.py` outputs same schema simulator expects  
**Integration Gap:**  
- No feedback loop: simulator doesn't send results back to plato-torch  
- Different coordinate systems: plato-torch uses screen pixels, simulator uses normalized [0,1] space

---

## 3. Holodeck-Rust → Plato-Torch (NOT CONNECTED)
**Connection Type:** No direct imports or shared data  
**Evidence:**  
- `holodeck-rust/src/npc/sentiment.rs` has no plato-torch dependencies  
- `plato-torch` has no holodeck client or message parser  
**Potential Bridge:**  
- `holodeck-rust` outputs JSON event streams (see `src/events/mod.rs`)  
- Could be consumed by `plato-torch/rooms/holodeck_bridge.py` (file doesn't exist yet)

---

## 4. Fleet-Simulator → Holodeck-Rust (NOT CONNECTED)
**Connection Type:** Independent systems  
**Evidence:**  
- Simulator uses `bevy` ECS, holodeck uses `tokio` async runtime  
- No shared message queues or event buses  
**Observation:** Both could benefit from shared agent state representation.

---

## 5. Plato-Ensign → Other Systems (MINIMAL)
**Connection Type:** Output-only  
**Evidence:**  
- `plato-ensign` produces LoRA adapters (`.safetensors`)  
- No other fleet components currently load these adapters  
**Integration Gap:** Missing `adapter_loader` in plato-torch or fleet-simulator.

---

## 6. Constraint-Theory-Core (ISOLATED)
**Connection Type:** Library dependency (not integrated)  
**Evidence:**  
- Used only in `cad-claw-experiments/` (proof of concept)  
- No imports in main fleet components  
**Potential:** Geometric snapping could enhance fleet-simulator collision detection.

---

## 7. CudaClaw & Flux-Runtime (INFRASTRUCTURE)
**Connection Type:** Underlying runtime, not application-level  
**Evidence:**  
- `cudaclaw` mentioned in `oracle1-vessel/` deployment scripts  
- `flux-runtime` appears in `research/bytecode-generation/`  
- Not imported by plato-torch, fleet-simulator, or holodeck

---

## Integration Priority Map (P1 Safe Channels)

### Channel 1: Tile Pipeline (EXISTS)
```
plato-torch → fleet-simulator (working)
plato-torch → plato-ensign (working)
```
**Action:** Add validation step to ensure tile schema consistency.

### Channel 2: Event Bridge (MISSING)
```
holodeck-rust → [bridge] → plato-torch
```
**Action:** Create `holodeck_bridge.py` room that consumes JSON events.

### Channel 3: Adapter Feedback Loop (MISSING)
```
plato-ensign → plato-torch/fleet-simulator
```
**Action:** Add LoRA loading capability to DeadbandRoom.

---

## Next Task: Wire GhostInjector into Holodeck
**Status:** `ghost-injector/` exists as standalone tool.  
**Approach:**  
1. Examine `holodeck-rust/src/npc/memory.rs` for injection points  
2. Create `GhostTile` struct matching `plato-torch` tile format  
3. Build CLI tool: `ghost-injector --holodeck-db path --tile-file.pt`

---

**Tile Submitted:** Integration map with verified gaps  
**Confidence:** High (based on actual file inspection)  
**Doctrine:** P1 complete — safe channels identified. P2 optimization ready.