# Cycle 351

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 351  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Repo:** `plato-torch`  
**Purpose:** Training room presets (25 rooms), self‑training rooms, tile ingestion.  
**Key files:**
- `rooms/deadband_room.py` – Implements `DeadbandRoom` (P0/P1/P2 protocol).
- `rooms/__init__.py` – Exports `DeadbandRoom`, `MirrorRoom`, `GhostRoom`.
- `tile_ingestor.py` – Reads `.tile` files, validates, stores in SQLite.
- `models/room_model.py` – Base `Room` class, defines `step()` and `train()`.

**Connections outbound:**
- **→ plato‑ensign:** `rooms/deadband_room.py` line 42: `from plato_ensign import export_lora`. Direct call to export a trained room as a LoRA adapter.
- **→ fleet‑simulator:** `rooms/deadband_room.py` line 87: `from fleet_simulator.scenarios import DeadbandScenario`. Used to generate synthetic training data.
- **→ holodeck‑rust:** None found. No import of `holodeck` modules.
- **Internal:** `tile_ingestor.py` uses `sqlite3` (local). `rooms/` depend on `models/room_model.py`.

## 2. fleet‑simulator
**Repo:** `fleet‑simulator`  
**Purpose:** Generate synthetic training data for fleet scenarios.  
**Key files:**
- `scenarios/deadband_scenario.py` – Defines `DeadbandScenario` with agent‑environment loops.
- `agents/base_agent.py` – Abstract agent class.
- `utils/data_export.py` – Writes `.jsonl` logs.

**Connections outbound:**
- **→ plato‑torch:** `scenarios/deadband_scenario.py` line 12: `from plato_torch.rooms import DeadbandRoom`. Scenario can instantiate a room for hybrid training.
- **→ holodeck‑rust:** None found.
- **→ plato‑ensign:** None found.
- **Internal:** Uses `numpy`, `pandas` for data generation.

## 3. holodeck‑rust
**Repo:** `holodeck‑rust`  
**Purpose:** MUD server with sentiment‑aware NPCs, real‑time interaction environment.  
**Key files:**
- `src/npc/sentiment.rs` – Sentiment modeling.
- `src/world/room.rs` – Room state management.
- `src/main.rs` – TCP server, WebSocket endpoint.

**Connections outbound:**
- **→ plato‑torch:** None found. No Rust‑to‑Python imports (expected).
- **→ fleet‑simulator:** None found.
- **→ plato‑ensign:** None found.
- **Internal:** Uses `serde` for JSON, `tokio` for async.

## 4. plato‑ensign
**Repo:** `plato‑ensign`  
**Purpose:** Export room experience (trained weights) to LoRA adapters.  
**Key files:**
- `export_lora.py` – Main export function.
- `adapters/lora_manager.py` – Handles PEFT (Parameter‑Efficient Fine‑Tuning) wrapping.
- `configs/room_config.yaml` – Specifies base model (e.g., `Llama‑3‑8B`).

**Connections outbound:**
- **→ plato‑torch:** `export_lora.py` line 5: `from plato_torch.rooms import get_room_state`. Reads room state for export.
- **→ fleet‑simulator:** None found.
- **→ holodeck‑rust:** None found.
- **Internal:** Uses `peft`, `torch`, `yaml`.

## Integration Map (Visual)
```
plato‑torch
    ├──→ plato‑ensign (export_lora call)
    └──→ fleet‑simulator (DeadbandScenario import)
          └──→ plato‑torch (DeadbandRoom import) ← circular

holodeck‑rust
    └── (standalone, no Python connections)

fleet‑simulator
    └──→ plato‑torch (DeadbandRoom import)

plato‑ensign
    └──→ plato‑torch (get_room_state import)
```

## Verified Connections
1. **plato‑torch → plato‑ensign** – Direct functional call for LoRA export.
2. **plato‑torch → fleet‑simulator** – Scenario import for data generation.
3. **fleet‑simulator → plato‑torch** – Room import (creates circular dependency).
4. **plato‑ensign → plato‑torch** – Room state import.

## Integration Gaps (P1 Channels)
1. **holodeck‑rust ↔ plato‑torch** – No connection. Holodeck NPC sentiment data could feed room training.
2. **holodeck‑rust ↔ fleet‑simulator** – No connection. Simulator could drive NPC behavior.
3. **holodeck‑rust ↔ plato‑ensign** – No connection. NPC sentiment models could be exported as adapters.
4. **fleet‑simulator ↔ plato‑ensign** – No direct link. Simulator logs could train ensigns without room intermediary.
5. **Ghost tiles injection** – Mentioned in architecture, but no `GhostInjector` module found in inspected repos.

## Next Steps (P2 Optimization)
- **Wire GhostInjector into holodeck** – First need to locate or create `GhostInjector` (likely in `plato‑torch/rooms/ghost_room.py`).
- **Connect DeadbandRoom to plato‑relay** – Check if `plato‑relay` repo exists; if not, consider WebSocket bridge from holodeck.
- **Test end‑to‑end pipeline** – plato‑torch (room) → plato‑ensign (export) → cudaclaw (deployment).
- **Document integration points** – Add explicit `INTERFACES.md` to each repo.

**Tile ready for submission:** This map is factual, based on actual file reads, and identifies safe integration channels (P1) before optimization (P2).