# Cycle 345

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 345  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch (Training Rooms)

**Key files inspected:**
- `plato-torch/rooms/deadband_room.py`
- `plato-torch/rooms/__init__.py`
- `plato-torch/configs/room_configs.yaml`

**Connections found:**
- **Internal:** DeadbandRoom imports `BaseRoom`, `Tile`, `AgentState` from `plato_torch.core`.
- **To plato-ensign:** `deadband_room.py` contains `export_to_lora()` method that calls `plato_ensign.export.export_room_experience()`. This is a direct, functional integration.
- **To fleet-simulator:** No imports found. No shared data structures observed in code.
- **To holodeck-rust:** No imports found. No configuration references.

**Integration point:**  
`plato-torch` → `plato-ensign` via explicit export function. Room training loops generate tiles; tiles can be exported as LoRA adapters.

## 2. fleet-simulator (Fleet Simulation)

**Key files inspected:**
- `fleet-simulator/src/simulator.rs`
- `fleet-simulator/Cargo.toml`

**Connections found:**
- **Internal:** Uses `bevy` engine, `serde` for serialization.
- **To plato-torch:** No Rust → Python imports. No shared data formats observed.
- **To plato-ensign:** No references.
- **To holodeck-rust:** No references in code. Both are Rust projects but no crate dependencies observed.

**Integration gap:**  
`fleet-simulator` appears isolated. It generates training data (simulated fleet interactions) but no clear pipeline to feed `plato-torch` rooms or `holodeck-rust` NPCs.

## 3. holodeck-rust (Sentiment-Aware MUD)

**Key files inspected:**
- `holodeck-rust/src/npc/sentiment.rs`
- `holodeck-rust/src/server/mod.rs`
- `holodeck-rust/Cargo.toml`

**Connections found:**
- **Internal:** Uses `tokio`, `serde`, `sqlx` (SQLite).
- **To plato-torch:** No references. NPC sentiment system does not import tile structures.
- **To plato-ensign:** No references.
- **To fleet-simulator:** No references.

**Integration gap:**  
`holodeck-rust` NPCs have sentiment states but no apparent connection to training tiles from `plato-torch`. No import of `Tile` or `AgentState` structures.

## 4. plato-ensign (LoRA Exporter)

**Key files inspected:**
- `plato-ensign/src/export.rs`
- `plato-ensign/py/ensign.py`

**Connections found:**
- **To plato-torch:** `export.rs` expects `Tile` struct (likely same as `plato-torch`'s `Tile`). Python wrapper `ensign.py` imports `plato_torch` to access room objects.
- **To fleet-simulator:** No references.
- **To holodeck-rust:** No references.

**Integration point:**  
`plato-ensign` ← `plato-torch` is the primary supported flow. Exports room experience (tiles) to LoRA adapters.

## Summary Map

```
plato-torch (training rooms)
        ↓ exports tiles
plato-ensign (LoRA exporter)
        ↑
fleet-simulator (isolated)   holodeck-rust (isolated)
```

**Connected:**
- `plato-torch` → `plato-ensign` (explicit export path)

**Not connected (gaps):**
- `fleet-simulator` → `plato-torch` (simulated data not feeding rooms)
- `holodeck-rust` → `plato-torch` (NPC sentiment not training rooms)
- `plato-torch` → `holodeck-rust` (tiles not influencing NPC behavior)
- `fleet-simulator` ↔ `holodeck-rust` (no shared simulation context)

## Recommended Integration Tasks (P1 Channels)

1. **Wire `fleet-simulator` output to `plato-torch` tile ingestion** — simulated fleet interactions could become training tiles.
2. **Connect `holodeck-rust` NPC sentiment logs to `plato-torch`** — NPC interactions as training data.
3. **Add `plato-torch` tile playback to `holodeck-rust`** — trained behavior influences NPC actions.

These are safe channels (P1) before optimization (P2).