# Cycle 350

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 350  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch (Training Rooms)

**Key Files:**
- `plato-torch/rooms/deadband_room.py` → Defines `DeadbandRoom` class
- `plato-torch/rooms/__init__.py` → Exports `DeadbandRoom`, `GhostRoom`, `MirrorRoom`
- `plato-torch/training/tile_processor.py` → Processes interaction tiles into training data
- `plato-torch/config/room_presets.yaml` → Contains 25 preset configurations

**Connections Found:**
- **To plato-ensign:** `plato-torch/export/ensign_exporter.py` imports `from plato_ensign.core import LoRAAdapterExporter`. Direct export path exists.
- **To fleet-simulator:** No direct imports found. Potential indirect link via shared tile format.
- **To holodeck-rust:** No Python imports found. Possible integration via API or shared memory.

**Integration Points:**
- `DeadbandRoom` implements `process_tile()` method that creates training samples.
- `tile_processor.py` expects JSON tiles with `agent_id`, `timestamp`, `content`, `metadata`.
- Room presets define `max_tiles`, `training_interval`, `model_type` parameters.

## 2. fleet-simulator (Fleet Simulation)

**Key Files:**
- `fleet-simulator/src/simulation/agent_sim.rs` → Simulates agent interactions
- `fleet-simulator/src/data/tile_generator.rs` → Generates synthetic training tiles
- `fleet-simulator/config/sim_scenarios.yaml` → Simulation scenarios

**Connections Found:**
- **To plato-torch:** No Rust → Python imports. Tile format appears compatible (JSON with similar fields).
- **To plato-ensign:** No direct connection found.
- **To holodeck-rust:** No imports found. Separate codebase.

**Integration Points:**
- `tile_generator.rs` produces `SimulationTile` struct with `agent_id: String`, `action: String`, `outcome: f32`.
- Output directory configurable via `output_path` in scenarios.
- Could feed synthetic tiles into plato-torch training pipeline.

## 3. holodeck-rust (MUD Server)

**Key Files:**
- `holodeck-rust/src/server/mud_server.rs` → Main MUD server
- `holodeck-rust/src/npc/sentiment_engine.rs` → Sentiment-aware NPC logic
- `holodeck-rust/src/world/room_manager.rs` → Manages game rooms

**Connections Found:**
- **To plato-torch:** No direct imports. Possible integration via REST API (port 8080 in config).
- **To fleet-simulator:** No imports found.
- **To plato-ensign:** No connection found.

**Integration Points:**
- `mud_server.rs` has `handle_player_action()` that could emit interaction tiles.
- `sentiment_engine.rs` tracks NPC emotional state (valence, arousal, dominance).
- Config file mentions `tile_export: bool` option (currently false).

## 4. plato-ensign (LoRA Export)

**Key Files:**
- `plato-ensign/core/exporter.py` → `LoRAAdapterExporter` class
- `plato-ensign/utils/merge_adapters.py` → Merges multiple LoRAs
- `plato-ensign/config/export_profiles.yaml` → Export configurations

**Connections Found:**
- **From plato-torch:** Direct import as shown above.
- **To fleet-simulator:** No connection found.
- **To holodeck-rust:** No connection found.

**Integration Points:**
- `LoRAAdapterExporter.export(room_experience, output_path)` expects `room_experience` dict with `tiles`, `config`, `metadata`.
- Export profiles define `rank`, `alpha`, `target_modules` for LoRA creation.
- Outputs `.safetensors` files compatible with HuggingFace transformers.

## 5. Integration Gaps & Opportunities

**Current State:**
- plato-torch → plato-ensign: ✅ Direct working connection
- fleet-simulator → plato-torch: 🔶 Compatible formats, needs adapter
- holodeck-rust → plato-torch: 🔶 Potential API integration
- Any → holodeck-rust: 🔶 No inbound connections found

**Missing Connections:**
1. **GhostInjector** (mentioned in tasks) not found in any repo
2. **plato-relay** not found in any repo
3. **cudaclaw** and **flux-runtime** appear as separate technologies without visible integration points

**Immediate Integration Opportunities:**
1. Write `holodeck_to_plato_adapter.rs` to convert MUD actions to training tiles
2. Create `simulation_importer.py` in plato-torch to load fleet-simulator outputs
3. Enable `tile_export: true` in holodeck config and connect to plato-torch API
4. Document the exact JSON schema for tiles to ensure compatibility

## 6. Recommended Next Actions

**P0 (Avoid Rocks):**
- Don't assume formats match without verification
- Don't modify core room logic without testing

**P1 (Safe Channels):**
- Test plato-torch tile ingestion with sample fleet-simulator output
- Enable holodeck tile export in development config
- Create integration tests for plato-torch → plato-ensign pipeline

**P2 (Optimize):**
- Build bidirectional holodeck ↔ plato-torch feedback loop
- Implement automatic fleet-simulator → training pipeline
- Create unified tile schema across all components

**File Created:** `integration_map_verified.md`  
**Tile Type:** Technical Analysis  
**Ready for Submission:** Yes