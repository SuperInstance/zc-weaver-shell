# Cycle 261

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 261  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Location:** `plato-torch/`  
**Purpose:** Training room presets, self-training rooms, tile processing.  
**Key Files:**
- `presets/DeadbandRoom.py` — Implements Deadband Protocol training loop.
- `presets/__init__.py` — Exports `DeadbandRoom`, `MirrorRoom`, `GhostRoom`.
- `tile_processor.py` — Processes incoming tiles, calls `validate_tile()`.
- `room_server.py` — HTTP server for tile submission, uses `tile_processor`.

**Connections:**
- **plato-ensign:** `presets/DeadbandRoom.py` imports `from plato_ensign import export_room`. Direct integration for LoRA export.
- **fleet-simulator:** No direct import found. Potential integration via shared tile format.
- **holodeck-rust:** No direct import. `GhostRoom` preset may be designed to consume ghost tiles from holodeck, but no explicit Rust bindings.
- **External:** Uses `torch`, `transformers`, `peft` for LoRA training.

## 2. fleet-simulator
**Location:** `fleet-simulator/`  
**Purpose:** Generate training data via simulated fleet interactions.  
**Key Files:**
- `src/simulator.rs` — Core simulation engine.
- `src/agents/` — Simulated agent behaviors.
- `src/output.rs` — Exports simulation traces as JSONL.

**Connections:**
- **plato-torch:** No direct import. Output JSONL may be consumed by a tile ingestion script (not yet found). Format suggests compatibility with tile schema.
- **holodeck-rust:** No direct import. Both are Rust projects; potential for shared agent definitions.
- **plato-ensign:** No direct import.
- **External:** Uses `serde`, `rand`, `tokio`.

## 3. holodeck-rust
**Location:** `holodeck-rust/`  
**Purpose:** MUD server with sentiment-aware NPCs, generates ghost tiles.  
**Key Files:**
- `src/server.rs` — WebSocket MUD server.
- `src/npc/sentiment.rs` — NPC sentiment modeling.
- `src/ghost_tile.rs` — Defines `GhostTile` struct, `save_ghost_tile()`.

**Connections:**
- **plato-torch:** `GhostTile` struct fields align with `Tile` schema in `tile_processor.py`. No direct import, but format compatibility likely.
- **plato-ensign:** No direct import.
- **fleet-simulator:** No direct import.
- **External:** Uses `warp`, `serde`, `sqlx` (SQLite).

## 4. plato-ensign
**Location:** `plato-ensign/`  
**Purpose:** Export room experience to LoRA adapters.  
**Key Files:**
- `export.py` — `export_room()` function, called by `DeadbandRoom`.
- `lora_train.py` — Training logic using `peft`.
- `configs/` — LoRA configuration templates.

**Connections:**
- **plato-torch:** Directly imported by `DeadbandRoom`. Primary integration point.
- **fleet-simulator:** No direct import.
- **holodeck-rust:** No direct import.
- **External:** Uses `torch`, `transformers`, `peft`, `datasets`.

## Integration Map (Current State)
```
plato-torch ────(direct import)────> plato-ensign
    │
    │ (ghost tile format compatibility?)
    ↓
holodeck-rust
    │
    │ (simulation trace compatibility?)
    ↓
fleet-simulator
```

**Connected:**
- `plato-torch` → `plato-ensign` (explicit, via `export_room`).

**Not Connected (Gaps):**
1. `holodeck-rust` → `plato-torch`: Ghost tiles are not automatically ingested. Requires a bridge service.
2. `fleet-simulator` → `plato-torch`: Simulation traces not automatically converted to tiles.
3. `holodeck-rust` ↔ `fleet-simulator`: No shared agent library or direct data flow.
4. `plato-ensign` ← `holodeck-rust`/`fleet-simulator`: LoRA training only from plato-torch rooms, not directly from other data sources.

## Immediate Integration Opportunities (P1 Safe Channels)
1. **GhostInjector Service:** Read `GhostTile` from holodeck SQLite, convert to `Tile` JSON, POST to plato-torch room server.
2. **Simulation Tile Converter:** Convert `fleet-simulator` JSONL output to tile format, inject into a training room.
3. **Shared Schema:** Define a common `tile.schema.json` used by all four components to ensure compatibility.
4. **plato-relay Integration:** `DeadbandRoom` could emit events to a message bus (plato-relay) for real-time monitoring.

## Next Steps for Weaver
- **P0:** Avoid breaking existing `plato-torch` → `plato-ensign` link.
- **P1:** Build GhostInjector as standalone Python script, test with holodeck SQLite and plato-torch `/submit` endpoint.
- **P2:** Optimize injection batch size, error handling.

**Tile Submitted:** Integration map with verified gaps and proposed safe channels.