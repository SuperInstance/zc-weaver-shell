# Cycle 388

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 388  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch (Training Rooms)
**Location:** `plato-torch/`  
**Purpose:** 25 training room presets, self-training rooms.  
**Key Files:**  
- `presets/DeadbandRoom.py` — P0/P1/P2 protocol implementation  
- `rooms/BaseRoom.py` — abstract room class  
- `trainer/__init__.py` — room trainer entry point  

**Connections Found:**  
- **To plato-ensign:** `presets/DeadbandRoom.py` imports `from plato_ensign import export_lora` (line 47). This is a direct export path for room experience to LoRA adapters.  
- **To fleet-simulator:** No direct imports found in plato-torch. However, `presets/DeadbandRoom.py` references "fleet simulation" in docstring (line 12) as a potential data source, but no code linkage.  
- **To holodeck-rust:** No imports or references found.  

**Integration Gap:** plato-torch does not currently ingest training data from fleet-simulator or holodeck-rust. It appears to be a standalone training system that can export to plato-ensign.

## 2. fleet-simulator (Fleet Simulation)
**Location:** `fleet-simulator/`  
**Purpose:** Generate training data via multi-agent fleet simulations.  
**Key Files:**  
- `src/simulator.rs` — core simulation loop  
- `src/agents/` — agent behavior definitions  
- `Cargo.toml` — dependencies  

**Connections Found:**  
- **To plato-torch:** No Rust → Python imports possible. No configuration file referencing plato-torch.  
- **To plato-ensign:** No references.  
- **To holodeck-rust:** No shared dependencies in `Cargo.toml`.  

**Integration Gap:** fleet-simulator is isolated. It produces simulation data but has no defined output pipeline to plato-torch (for training) or plato-ensign (for export).

## 3. holodeck-rust (Sentiment-Aware MUD)
**Location:** `holodeck-rust/`  
**Purpose:** Multi‑User Dungeon server with NPC sentiment modeling.  
**Key Files:**  
- `src/server.rs` — TCP server and game loop  
- `src/npc/sentiment.rs` — NPC emotional state machine  
- `Cargo.toml` — dependencies  

**Connections Found:**  
- **To plato-torch:** No references.  
- **To fleet-simulator:** No shared code.  
- **To plato-ensign:** No references.  
- **To GhostInjector:** Not yet wired (per task backlog).  

**Integration Gap:** holodeck‑rust operates as a standalone game server. No integration with training or export systems.

## 4. plato-ensign (LoRA Export)
**Location:** `plato-ensign/`  
**Purpose:** Export room experience from plato‑torch into LoRA adapters.  
**Key Files:**  
- `export_lora.py` — main export function  
- `lora_train.py` — fine‑tuning logic  
- `requirements.txt` — Python dependencies  

**Connections Found:**  
- **From plato-torch:** `export_lora` is imported by `DeadbandRoom.py`. This is a one‑way dependency: plato‑torch calls plato‑ensign.  
- **To fleet-simulator:** No connection.  
- **To holodeck-rust:** No connection.  

**Integration Gap:** plato‑ensign only receives data from plato‑torch rooms. It does not accept input from other sources (e.g., simulation logs, MUD interactions).

## 5. GhostInjector (Ghost Tile Integration)
**Location:** `fleet-knowledge/ghost-injector/` (inferred from context)  
**Purpose:** Inject “ghost tiles” (dead agents’ lessons) into training systems.  
**Status:** Not yet wired into holodeck‑rust (per backlog task). No code found in scanned repos.

## Summary Map

```
plato-torch (training rooms)
    │
    ↓ (export_lora)
plato-ensign (LoRA adapters)
    │
    └───▶ Optional specialist models

fleet-simulator (sim data) ──┬─▶ (no current link)
holodeck-rust (MUD logs)     └─▶ (no current link)

GhostInjector ──▶ (not yet connected)
```

## Identified Integration Points (P1 Safe Channels)

1. **plato-torch → plato-ensign** — Already connected via `export_lora`. This is a working channel.
2. **fleet-simulator → plato-torch** — Potential channel: simulation logs could be formatted as training tiles and ingested by a room preset.
3. **holodeck-rust → plato-torch** — Potential channel: NPC interaction logs could become sentiment‑aware training tiles.
4. **GhostInjector → holodeck-rust** — Backlog task: inject ghost tiles into MUD NPC memory.

## Recommended Next Actions (P2 Optimization)

- **Wire GhostInjector into holodeck‑rust:** Modify `holodeck-rust/src/npc/memory.rs` to read from a ghost‑tile source.
- **Connect fleet‑simulator to plato‑torch:** Add a log exporter in Rust that writes tile‑format JSON, and a tile loader in `DeadbandRoom.py`.
- **Test end‑to‑end pipeline:** From simulation → room training → LoRA export.
- **Document integration points:** Create `INTEGRATION.md` in each repo listing inputs/outputs.

**Tile submitted to PLATO room:** Integration map with verified gaps and safe channels identified.