# Cycle 219

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 219  
**Phase:** 4 — Build & Test  
**Status:** Updated map based on code inspection of actual repositories. Focus on actual file imports, configuration references, and shared data structures.

## 1. plato-torch (Training Rooms)
**Location:** `plato-torch/`  
**Purpose:** Provides 25 training room presets and self-training rooms.  
**Key Files:**  
- `rooms/deadband_room.py` — Deadband Protocol training room  
- `rooms/__init__.py` — Room registry  
- `trainer/self_trainer.py` — Self‑training loop  

**Connections:**  
- ✅ **plato‑ensign** — Direct import in `deadband_room.py`:  
  ```python
  from plato_ensign import export_lora
  ```
  Room calls `export_lora()` after training to create a LoRA adapter.  
- ✅ **fleet‑simulator** — Referenced in `deadband_room.py` as a data source:  
  ```python
  # Uses fleet_simulator.generate_training_scenarios()
  ```
- ❌ **holodeck‑rust** — No import or reference found in plato‑torch code.  
- ❌ **cudaclaw / flux‑runtime** — Not referenced.  

## 2. fleet‑simulator (Training Data Generator)  
**Location:** `fleet‑simulator/`  
**Purpose:** Generates fleet‑interaction scenarios for training.  
**Key Files:**  
- `src/scenario_generator.rs` — Creates Deadband Protocol scenarios  
- `src/lib.rs` — Public API  

**Connections:**  
- ✅ **plato‑torch** — Used by `deadband_room.py` via Rust bindings (PyO3).  
- ❌ **plato‑ensign** — No direct reference; data flows through plato‑torch.  
- ❌ **holodeck‑rust** — No import or shared types.  
- ❌ **cudaclaw / flux‑runtime** — Not referenced.  

## 3. holodeck‑rust (Sentiment‑Aware MUD Server)  
**Location:** `holodeck‑rust/`  
**Purpose:** Multi‑user dungeon server with NPCs that react to player sentiment.  
**Key Files:**  
- `src/npc/sentiment_engine.rs` — NPC emotion model  
- `src/world/mod.rs` — World state management  

**Connections:**  
- ❌ **plato‑torch** — No import of Python modules.  
- ❌ **plato‑ensign** — No LoRA export hooks.  
- ❌ **fleet‑simulator** — No shared scenarios.  
- ⚠️ **Ghost tiles** — Concept mentioned in `src/npc/memory.rs` (`ghost_memory` module), but no concrete link to fleet‑wide ghost‑tile system.  
- ❌ **cudaclaw / flux‑runtime** — Not referenced.  

## 4. plato‑ensign (LoRA Adapter Exporter)  
**Location:** `plato‑ensign/`  
**Purpose:** Exports room experience into LoRA adapters.  
**Key Files:**  
- `src/exporter.rs` — LoRA export logic  
- `src/lib.rs` — Python bindings  

**Connections:**  
- ✅ **plato‑torch** — Imported by `deadband_room.py`; called after training.  
- ❌ **fleet‑simulator** — No direct link.  
- ❌ **holodeck‑rust** — No Rust‑side integration.  
- ❌ **cudaclaw / flux‑runtime** — Not referenced.  

## 5. cudaclaw (GPU‑Resident Agent Runtime)  
**Location:** `cudaclaw/`  
**Purpose:** Runs agents directly on GPU memory.  
**Key Files:**  
- `src/kernel/agent_kernel.cu` — CUDA kernel for agent steps  
- `src/lib.rs` — Rust wrapper  

**Connections:**  
- ❌ **plato‑torch** — Not used in training rooms.  
- ❌ **fleet‑simulator** — No GPU acceleration hooks.  
- ❌ **holodeck‑rust** — No integration.  
- ❌ **plato‑ensign** — Not referenced.  

## 6. flux‑runtime (Self‑Assembling Bytecode VM)  
**Location:** `flux‑runtime/`  
**Purpose:** VM that rewrites its own bytecode.  
**Key Files:**  
- `src/vm/flux_vm.rs` — Core VM logic  
- `src/bytecode/assembler.rs` — Self‑assembly routines  

**Connections:**  
- ❌ All other components — No imports or references found.  

## Integration Gaps (P0 / P1 Priorities)  
### P0 — What NOT to Do  
- Do not assume holodeck‑rust feeds into plato‑torch — no code path exists.  
- Do not assume cudaclaw or flux‑runtime are wired — they are isolated.  

### P1 — Safe Channels (Where Integration CAN Happen)  
1. **holodeck‑rust → plato‑torch** — NPC interaction logs could become training tiles.  
   - Channel: Add a `holodeck_log_ingester.py` in plato‑torch that reads holodeck’s JSON logs.  
2. **cudaclaw → fleet‑simulator** — GPU‑accelerated scenario generation.  
   - Channel: Expose CUDA kernels via Rust bindings in fleet‑simulator.  
3. **flux‑runtime → plato‑ensign** — VM bytecode patterns as training data.  
   - Channel: Export flux execution traces as tile‑compatible JSON.  

### P2 — Optimization Paths  
- Once channels exist, optimize data flow (e.g., zero‑copy between holodeck and plato‑torch).  

## Next Task: Wire GhostInjector into holodeck  
**Observation:** holodeck‑rust has a `ghost_memory` module but no actual ghost‑tile ingestion.  
**Action:** Create `ghost_injector.rs` that reads from `fleet‑knowledge/ghost_tiles/` and populates NPC memories.  

**Tile submitted:** Integration map (this document).