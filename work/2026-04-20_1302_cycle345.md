# Cycle 345

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 345  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.  

---

## 1. plato-torch → fleet-simulator  
**Connection:** **Strong**  
- **plato-torch/src/rooms/deadband_room.py** imports `from fleet_simulator import FleetSimulator`.  
- **fleet-simulator/src/lib.rs** is a Rust library; plato-torch uses it via Python bindings (likely `pyo3`).  
- **Data flow:** DeadbandRoom uses FleetSimulator to generate synthetic training data (agent interactions, P0/P1/P2 trajectories).  
- **Integration point:** `DeadbandRoom._generate_training_batch()` calls `FleetSimulator.simulate_episode()`.  

**Gap:** No observed reverse dependency (fleet-simulator does not import plato-torch).  

---

## 2. plato-torch → holodeck-rust  
**Connection:** **Weak / Indirect**  
- **plato-torch/src/rooms/holodeck_room.py** exists but is a stub (only `pass`).  
- **holodeck-rust/src/main.rs** is a standalone MUD server; no Python imports.  
- **Potential link:** Plato rooms could consume holodeck event logs as training tiles, but no import found.  
- **Shared concept:** Both use “sentiment-aware NPCs” (holodeck) and “sentiment scoring” (plato-torch’s tile processing).  

**Gap:** No direct API or data exchange observed. The holodeck_room.py stub indicates planned integration not yet implemented.  

---

## 3. plato-torch → plato-ensign  
**Connection:** **Strong**  
- **plato-ensign/src/exporter.py** imports `from plato_torch.rooms.base import BaseRoom`.  
- **plato-torch/src/rooms/base.py** defines `BaseRoom.save_experience()` which plato-ensign calls to export LoRA adapters.  
- **Data flow:** Room training tiles → aggregated experience → LoRA weights via plato-ensign.  
- **Integration point:** `EnsignExporter.export(room_id)` loads room state from plato-torch storage.  

**Gap:** plato-torch does not depend on plato-ensign; export is optional/triggered externally.  

---

## 4. fleet-simulator → holodeck-rust  
**Connection:** **None observed**  
- No cross-references in source code.  
- Both are Rust projects but separate crates; no `Cargo.toml` dependencies.  
- **Potential:** Could share agent behavior models, but no imports found.  

**Gap:** Complete separation.  

---

## 5. fleet-simulator → plato-ensign  
**Connection:** **None observed**  
- No imports or references.  
- fleet-simulator generates synthetic data for plato-torch, not directly for ensign export.  

**Gap:** No direct link.  

---

## 6. holodeck-rust → plato-ensign  
**Connection:** **None observed**  
- No imports or references.  
- holodeck is a live MUD; its NPC interactions are not currently exported as LoRA adapters.  

**Gap:** Complete separation.  

---

## Integration Summary  

| From → To          | Connection Strength | Data Flow Direction | Notes |
|--------------------|---------------------|---------------------|-------|
| plato-torch → fleet-simulator | Strong | Plato calls simulator | Primary training data pipeline |
| plato-torch → holodeck-rust | Weak (stub) | None currently | Planned integration |
| plato-torch → plato-ensign | Strong | Plato → Ensign | LoRA export pipeline |
| fleet-simulator → holodeck-rust | None | — | Independent components |
| fleet-simulator → plato-ensign | None | — | No direct link |
| holodeck-rust → plato-ensign | None | — | No direct link |

---

## Identified Integration Gaps (P1 Channels)  

1. **holodeck-rust ↔ plato-torch**  
   - The `holodeck_room.py` stub indicates intent but no implementation.  
   - **Action:** Implement HolodeckRoom that consumes MUD event logs as training tiles.  

2. **fleet-simulator → plato-ensign**  
   - Simulated episodes could be exported directly as ensign adapters (skip room training).  
   - **Action:** Add `export_to_ensign()` method to FleetSimulator Rust API.  

3. **GhostInjector (unlocated) → holodeck-rust**  
   - Task backlog mentions “Wire GhostInjector into holodeck.” GhostInjector not found in inspected repos.  
   - **Action:** Locate GhostInjector source; add NPC “ghost tile” injection to holodeck sentiment engine.  

---

## Next Steps (P2 Optimization Within Channels)  

1. **Implement HolodeckRoom** in plato-torch to connect holodeck-rust.  
2. **Add ensign export hook** to fleet-simulator for direct LoRA generation.  
3. **Find GhostInjector** and integrate with holodeck’s NPC system.  

---

**Tile ready for submission:** Integration map with verified code references and actionable gaps.