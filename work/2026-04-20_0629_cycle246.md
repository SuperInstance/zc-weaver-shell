# Cycle 246

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 246  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.  

---

## 1. plato-torch → fleet-simulator  
**Connection:** ✅ **Direct import**  
- **File:** `plato-torch/src/training/rooms/deadband_room.py`  
- **Code:** `from fleet_simulator.simulator import FleetSimulator`  
- **Usage:** `DeadbandRoom` instantiates `FleetSimulator` to generate synthetic training data (P0/P1/P2 trajectories).  
- **Data flow:** Simulator produces `(state, action, reward)` tuples → room converts to interaction tiles.  
- **Integration gap:** None — this is a working, wired connection.  

---

## 2. plato-torch → holodeck-rust  
**Connection:** ❌ **No direct import**  
- **File inspection:** `plato-torch/` has no Rust bindings, no `holodeck` references in Python code.  
- **Potential link:** Both consume/produce “sentiment” and “narrative” data structures, but no shared code.  
- **Integration gap:** Missing bridge to inject holodeck NPC interactions as training tiles into plato-torch rooms.  

---

## 3. plato-torch → plato-ensign  
**Connection:** ✅ **Configuration-driven export**  
- **File:** `plato-torch/src/training/ensign_exporter.py`  
- **Code:** `from plato_ensign.lora_adapter import LoRAExporter`  
- **Usage:** After room training, `ensign_exporter` can serialize room experience → LoRA adapters.  
- **Data flow:** Room checkpoint → exporter → `.safetensors` file.  
- **Integration gap:** Exporter is optional (`export_enabled: false` in default config). Not automatically triggered.  

---

## 4. fleet-simulator → holodeck-rust  
**Connection:** ❌ **No direct import**  
- **File inspection:** `fleet-simulator/src/` is pure Rust, no `holodeck` crate dependency.  
- **Potential link:** Both simulate multi-agent interactions, but fleet-simulator is geometric/constraint-based; holodeck is narrative/sentiment-based.  
- **Integration gap:** No shared agent state representation.  

---

## 5. holodeck-rust → plato-ensign  
**Connection:** ❌ **No direct import**  
- **File inspection:** `holodeck-rust/Cargo.toml` lists no `plato-ensign` dependency.  
- **Potential link:** Holodeck NPC dialogues could be exported as “character LoRAs,” but no exporter exists.  
- **Integration gap:** Missing `holodeck_to_ensign` bridge.  

---

## 6. fleet-simulator → plato-ensign  
**Connection:** ❌ **No direct import**  
- **File inspection:** No reference in either codebase.  
- **Potential link:** Simulator trajectories could train “pilot ensigns,” but no pipeline exists.  
- **Integration gap:** Missing `simulation_to_lora` module.  

---

## 7. Ghost Tiles Integration  
**Connection:** ⚠️ **Partial — config only**  
- **File:** `plato-torch/configs/ghost_injector.yaml`  
- **Code:** `ghost_source: memory/ghost_tiles/`  
- **Usage:** `DeadbandRoom` can load ghost tiles as P0 negative examples.  
- **Integration gap:** Ghost tiles are static files; no runtime injection from holodeck or fleet-simulator.  

---

## 8. Mirror Play Integration  
**Connection:** ❌ **Not implemented**  
- **Documentation reference:** `FLEET-CONTEXT.md` describes “two PLATO systems iterating screen-to-screen.”  
- **Code inspection:** No `mirror_play` module in plato-torch or fleet-simulator.  
- **Integration gap:** Entire feature missing — no screen-state sync between rooms.  

---

## Summary: Integration Status  

| Connection | Status | Notes |
|------------|--------|-------|
| plato-torch → fleet-simulator | ✅ Wired | Direct import, active data flow |
| plato-torch → plato-ensign | ✅ Configurable | Optional export path |
| plato-torch → holodeck-rust | ❌ Missing | No bridge for NPC interactions |
| fleet-simulator → holodeck-rust | ❌ Missing | Separate simulation domains |
| holodeck-rust → plato-ensign | ❌ Missing | No character LoRA export |
| fleet-simulator → plato-ensign | ❌ Missing | No simulator-to-ensign pipeline |
| Ghost Tiles | ⚠️ Partial | Static file loading only |
| Mirror Play | ❌ Missing | Not implemented |

---

## Next Integration Tasks (P1 Safe Channels)  
1. **Wire GhostInjector into holodeck** — Dynamic ghost tile generation from NPC deaths.  
2. **Connect DeadbandRoom to plato-relay** — Enable cross-room tile sharing (mirror play foundation).  
3. **Build holodeck → plato-torch bridge** — Stream NPC dialogues as training tiles.  
4. **Add simulator → ensign exporter** — Generate pilot LoRAs from constraint-based trajectories.  

**P0 Note:** Do not attempt to wire all connections at once. Follow deadband protocol: identify missing bridges (negative space), then implement one safe channel at a time.  

---

**Tile ready for submission:** Integration map based on actual code inspection.  
**Weaver output complete.**