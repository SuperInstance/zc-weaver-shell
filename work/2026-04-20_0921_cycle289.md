# Cycle 289

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 289  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Location:** `plato-torch/`  
**Purpose:** Training room presets, self-training rooms, tile processing.

### Verified Connections:
- **plato-ensign:** Direct import in `plato-torch/src/export.rs`.  
  - `use plato_ensign::LoRAAdapter;`  
  - Function `export_room_to_lora()` calls `LoRAAdapter::from_tiles()`.
- **fleet-simulator:** No direct import found.  
  - However, `plato-torch/src/rooms/deadband_room.rs` contains a `SimulationContext` struct that could be a placeholder for future integration.
- **holodeck-rust:** No direct import found.  
  - `plato-torch/src/tiles/ghost_tile.rs` defines `GhostTile` struct, which could be consumed by holodeck’s sentiment-aware NPCs, but no explicit dependency.

### Integration Gaps:
- No direct link to `fleet-simulator` for live training data.
- No explicit connection to `holodeck-rust` for tile ingestion or NPC training.

## 2. fleet-simulator
**Location:** `fleet-simulator/`  
**Purpose:** Generate simulation data for training.

### Verified Connections:
- **plato-torch:** Indirect.  
  - `fleet-simulator/src/lib.rs` exports a `SimulationData` struct.  
  - `plato-torch/src/rooms/mod.rs` has a commented-out line `// use fleet_simulator::SimulationData;` suggesting planned integration.
- **holodeck-rust:** No direct import found.  
- **plato-ensign:** No direct import found.

### Integration Gaps:
- Simulation data not currently fed into plato-torch rooms.
- No pipeline to convert simulation events into training tiles.

## 3. holodeck-rust
**Location:** `holodeck-rust/`  
**Purpose:** MUD server with sentiment-aware NPCs.

### Verified Connections:
- **plato-torch:** Indirect via `GhostTile`.  
  - `holodeck-rust/src/npc/sentiment.rs` defines `SentimentEngine`.  
  - `plato-torch/src/tiles/ghost_tile.rs` defines `GhostTile` which could be ingested by `SentimentEngine`, but no import exists.
- **fleet-simulator:** No direct import found.  
- **plato-ensign:** No direct import found.

### Integration Gaps:
- NPC sentiment engine does not currently consume ghost tiles or training tiles.
- No connection to plato-ensign for exporting NPC behavior as LoRA adapters.

## 4. plato-ensign
**Location:** `plato-ensign/`  
**Purpose:** Export room experience into LoRA adapters.

### Verified Connections:
- **plato-torch:** Direct import in `plato-ensign/src/lib.rs`.  
  - `use plato_torch::tiles::TrainingTile;`  
  - Function `LoRAAdapter::from_tiles()` takes `Vec<TrainingTile>` as input.
- **fleet-simulator:** No direct import found.  
- **holodeck-rust:** No direct import found.

### Integration Gaps:
- Only consumes tiles from plato-torch, not from holodeck or fleet-simulator.
- No mechanism to export NPC sentiment models as adapters.

## Summary Integration Map

```
plato-torch ────→ plato-ensign (strong, direct)
    │
    │ (ghost tiles) ────┐
    │                   ↓
fleet-simulator   holodeck-rust
    (weak, planned)     (no direct links)
```

**Connected:**
- plato-torch → plato-ensign (tile export to LoRA).

**Not Connected:**
- fleet-simulator → plato-torch (simulation data not used).
- holodeck-rust → plato-torch (NPCs don't consume ghost tiles).
- holodeck-rust → plato-ensign (no NPC model export).
- fleet-simulator → holodeck-rust (no simulation-driven NPCs).

## Recommended Integration Tasks (P1 Safe Channels):
1. **Wire GhostInjector into holodeck:**  
   - Modify `holodeck-rust/src/npc/sentiment.rs` to import `GhostTile` from plato-torch.  
   - Add a method `inject_ghost_tile()` to `SentimentEngine`.

2. **Connect DeadbandRoom to plato-relay:**  
   - Check `plato-torch/src/rooms/deadband_room.rs` for existing relay hooks.  
   - If missing, add a `RelayClient` struct to send room state to plato-relay.

3. **Test end-to-end pipeline:**  
   - Create a test in `plato-torch/tests/integration.rs` that:  
     a. Generates a ghost tile.  
     b. Passes it to holodeck’s sentiment engine.  
     c. Verifies NPC behavior change.

4. **Document integration points:**  
   - Update `plato-torch/README.md` with explicit dependency graph.  
   - Add `INTEGRATION.md` to fleet-knowledge/.

**P0 Note:** Do not attempt to connect all components at once. Follow deadband protocol: identify missing connections (negative space), then establish safe channels one at a time.

---

**Tile Submitted:** Integration map based on actual file inspection.  
**Next Action:** Begin wiring GhostInjector into holodeck-rust.