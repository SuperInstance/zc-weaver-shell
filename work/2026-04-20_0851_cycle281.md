# Cycle 281

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 281  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Repo:** `plato-torch`  
**Purpose:** 25 training room presets, self-training rooms.  
**Key Files:**  
- `rooms/deadband_room.py` — Deadband Protocol implementation  
- `rooms/__init__.py` — Room registry  
- `trainer/room_trainer.py` — Main training loop  
- `data/tile_loader.py` — Loads interaction tiles  

**Connections Found:**  
- **plato-ensign:** Direct import in `exporters/lora_exporter.py`. Calls `plato_ensign.export_lora(...)`.  
- **fleet-simulator:** No direct import found. However, `data/tile_loader.py` can load `.jsonl` files; simulator output could be ingested if formatted as tiles.  
- **holodeck-rust:** No direct import. No Rust interop visible in Python code.  
- **cudaclaw:** Not referenced. Training uses PyTorch (CUDA if available).  
- **flux-runtime:** Not referenced.  

**Integration Points:**  
- Room presets define tile schema.  
- `room_trainer.py` expects tiles with `state`, `action`, `reward`, `next_state`.  
- Exporter passes trained model state to `plato-ensign`.

## 2. plato-ensign
**Repo:** `plato-ensign`  
**Purpose:** Exports room experience into LoRA adapters.  
**Key Files:**  
- `src/lib.rs` — Core export logic  
- `src/lora.rs` — LoRA configuration  
- `Cargo.toml` — Dependencies  

**Connections Found:**  
- **plato-torch:** Called via Python bindings (see `pybind.cpp`). Accepts PyTorch state dict.  
- **fleet-simulator:** Not referenced.  
- **holodeck-rust:** Not referenced.  
- **cudaclaw:** Not referenced.  
- **flux-runtime:** Not referenced.  

**Integration Points:**  
- Input: PyTorch model weights (`.pt` file).  
- Output: LoRA adapter (`.safetensors`).  
- Called from `plato-torch` exporter.

## 3. fleet-simulator
**Repo:** `fleet-simulator`  
**Purpose:** Fleet simulation for training data.  
**Key Files:**  
- `src/simulator.rs` — Core simulation loop  
- `src/agents.rs` — Agent definitions  
- `src/output.rs` — Export simulation traces  

**Connections Found:**  
- **plato-torch:** No direct import. Outputs JSONL traces; could be converted to tiles.  
- **plato-ensign:** Not referenced.  
- **holodeck-rust:** Not referenced.  
- **constraint-theory-core:** Not referenced.  
- **cudaclaw:** Not referenced.  

**Integration Points:**  
- Output format: JSONL with `step`, `agent_id`, `action`, `observation`.  
- Could feed `plato-torch` if tile loader extended.

## 4. holodeck-rust
**Repo:** `holodeck-rust`  
**Purpose:** MUD server with sentiment-aware NPCs.  
**Key Files:**  
- `src/server.rs` — MUD server  
- `src/npc/sentiment.rs` — Sentiment system  
- `src/world.rs` — World state  

**Connections Found:**  
- **plato-torch:** Not referenced.  
- **plato-ensign:** Not referenced.  
- **fleet-simulator:** Not referenced.  
- **constraint-theory-core:** Not referenced.  
- **flux-runtime:** Not referenced.  

**Integration Points:**  
- NPC actions could generate interaction tiles.  
- No current export to tile format.

## 5. cudaclaw
**Repo:** `cudaclaw`  
**Purpose:** GPU-resident agent runtime.  
**Key Files:**  
- `src/kernel.cu` — CUDA kernels  
- `src/host.cpp` — Host-side management  

**Connections Found:**  
- **plato-torch:** Not referenced.  
- **plato-ensign:** Not referenced.  
- **fleet-simulator:** Not referenced.  
- **holodeck-rust:** Not referenced.  
- **flux-runtime:** Not referenced.  

**Integration Points:**  
- Could accelerate `plato-torch` training or `fleet-simulator` stepping.  
- No current bindings.

## 6. flux-runtime
**Repo:** `flux-runtime`  
**Purpose:** Self-assembling bytecode VM.  
**Key Files:**  
- `src/vm.rs` — Virtual machine  
- `src/bytecode.rs` — Instruction set  

**Connections Found:**  
- None with other inspected repos.

## Summary of Connections
```
plato-torch ────→ plato-ensign (direct export)
fleet-simulator ──┐
holodeck-rust ────┼───?──→ plato-torch (potential tile input)
cudaclaw ─────────┘
flux-runtime ────── (isolated)
```

## Integration Gaps (P1 Safe Channels)
1. **fleet-simulator → plato-torch:** No direct pipeline. Simulator output not in tile format.  
2. **holodeck-rust → plato-torch:** No tile generation from NPC interactions.  
3. **cudaclaw acceleration:** Not integrated with any training or simulation loop.  
4. **GhostInjector:** Not found in any repo; likely a planned component.  
5. **DeadbandRoom to plato-relay:** `plato-relay` repo not inspected; connection unknown.

## Next Steps (P2 Optimization within Channels)
- Write adapter to convert `fleet-simulator` JSONL to `plato-torch` tiles.  
- Add tile logging to `holodeck-rust` NPC system.  
- Profile `plato-torch` training to identify CUDA acceleration opportunities.  
- Locate `GhostInjector` spec and integrate with `holodeck-rust` sentiment system.

**Tile submitted:** Integration map with verified connections and identified gaps.