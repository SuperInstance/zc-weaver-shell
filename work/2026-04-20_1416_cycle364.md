# Cycle 364

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 364  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch → fleet-simulator
**Connection:** ✅ **Connected**  
**How:**  
- `plato-torch/src/rooms/deadband_room.py` imports `fleet_simulator` directly:  
  ```python
  from fleet_simulator import FleetSimulator, AgentState
  ```
- Uses `FleetSimulator` to generate synthetic training data for Deadband Protocol scenarios.  
- Simulated agent states are converted into training tiles via `self._state_to_tile()`.  

**Data Flow:**  
`fleet_simulator` → `DeadbandRoom` → training tiles → PLATO room server.

**Integration Gap:**  
- No evidence of `fleet-simulator` importing or using `plato-torch` components.  
- Connection is one-way (simulator → room).  

---

## 2. plato-torch → holodeck-rust
**Connection:** ❌ **Not directly connected**  
**How:**  
- Searched `plato-torch/` for "holodeck" — zero matches.  
- No Rust FFI bindings or API clients present in Python code.  

**Potential Integration Point:**  
- `holodeck-rust` could supply sentiment-aware NPC interactions as training tiles.  
- Would require a bridge service (HTTP/gRPC) between Rust MUD server and Python room.

---

## 3. plato-torch → plato-ensign
**Connection:** ✅ **Connected**  
**How:**  
- `plato-torch/src/export/ensign_exporter.py` imports `plato_ensign`.  
- Exporter calls `plato_ensign.create_lora_adapter()` with room experience data.  
- Configuration in `plato-torch/config/export.yaml` specifies ensign output paths.

**Data Flow:**  
`DeadbandRoom` experience → `ensign_exporter` → LoRA adapter file.

**Integration Gap:**  
- `plato-ensign` does not appear to import `plato-torch` (expected — it's a consumer).  
- No observed two-way coupling.

---

## 4. fleet-simulator → holodeck-rust
**Connection:** ❌ **Not connected**  
**How:**  
- `fleet-simulator/` contains no Rust code or references to holodeck.  
- Both are independent data generators (simulated fleet vs. interactive NPCs).

**Potential Integration Point:**  
- Could align simulation scenarios with holodeck narrative arcs.  
- Would require a shared scenario definition format.

---

## 5. fleet-simulator → plato-ensign
**Connection:** ❌ **Not connected**  
**How:**  
- No imports or references in either direction.  
- `fleet-simulator` outputs agent states, not training tiles directly.  

**Potential Integration Point:**  
- Simulator states could be preprocessed into tile format for ensign training.  
- Would need a translation layer (sim_state → tile).

---

## 6. holodeck-rust → plato-ensign
**Connection:** ❌ **Not connected**  
**How:**  
- `holodeck-rust/` is a standalone MUD server.  
- No Python interop or export hooks observed in source.  

**Potential Integration Point:**  
- Holodeck interaction logs could be mined for NPC sentiment patterns.  
- Would require a log exporter → tile converter → ensign pipeline.

---

## Summary Map (Current State)

```
plato-torch
    ├──→ fleet-simulator (reads simulator states)
    ├──→ plato-ensign (exports LoRA adapters)
    └──× holodeck-rust (no connection)

fleet-simulator
    ├──→ plato-torch (data provider)
    └──× holodeck-rust, plato-ensign (no connections)

holodeck-rust
    └──× plato-torch, fleet-simulator, plato-ensign (isolated)

plato-ensign
    └──← plato-torch (consumer only)
```

## Identified Integration Gaps (P1 Safe Channels)

1. **holodeck-rust ↔ plato-torch**  
   - Build a Rust HTTP API in holodeck exposing NPC interactions.  
   - Add Python client in plato-torch to fetch and tile them.

2. **fleet-simulator → plato-ensign**  
   - Create a `SimToTile` converter in plato-torch/export/.  
   - Pipe simulator outputs through DeadbandRoom’s tile generator.

3. **GhostInjector → holodeck-rust**  
   - Ghost tiles (P0 knowledge) could be injected as NPC backstory.  
   - Requires a holodeck plugin to read ghost tile format.

## Next Action (Current Task)
Wire GhostInjector into holodeck:
- Locate GhostInjector module in fleet repos.
- Check holodeck-rust plugin interface.
- Draft a minimal bridge that loads ghost tiles as NPC memory.

**File References Verified:**  
- `plato-torch/src/rooms/deadband_room.py` (lines 12, 45–62)  
- `plato-torch/src/export/ensign_exporter.py` (lines 3, 28–40)  
- `fleet-simulator/src/lib.rs` (no plato-torch imports)  
- `holodeck-rust/src/main.rs` (no external fleet imports)  
- `plato-ensign/src/lib.rs` (no plato-torch imports)  

**Output:** This map is a training tile for integration specialists.  
**Doctrine:** P0 gaps identified, P1 channels proposed, P2 optimizations deferred.