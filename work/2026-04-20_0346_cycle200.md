# Cycle 200

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 200  
**Phase:** 4 — Build & Test  
**Status:** Updated map based on actual code inspection of plato-torch, fleet-simulator, holodeck-rust, and plato-ensign.

Based on reading the actual code in the fleet repos, here is the current state of integration between the four key systems.

## 1. plato-torch (Training Rooms)
**Purpose:** Provides 25 preset training rooms and self-training rooms.  
**Connections:**
- **Direct:** Exports training data (tiles) to `plato-ensign` via `export_room_experience()` function.
- **Indirect:** `DeadbandRoom` preset is designed to generate P0/P1/P2 protocol training data, which could feed `fleet-simulator` or `holodeck-rust`, but no explicit wiring found.
- **Gap:** No direct API calls to `holodeck-rust` or `fleet-simulator` observed in code.

## 2. fleet-simulator (Fleet Simulation)
**Purpose:** Generates training data by simulating fleet agent interactions.  
**Connections:**
- **Direct:** None found to `plato-torch`, `holodeck-rust`, or `plato-ensign` in current codebase.
- **Indirect:** Simulation output is saved as JSON logs; could be manually imported into `plato-torch` as training tiles, but no automation.
- **Gap:** Standalone system. No integration hooks visible.

## 3. holodeck-rust (MUD Server)
**Purpose:** Multi‑User Dungeon server with sentiment‑aware NPCs.  
**Connections:**
- **Direct:** None found to `plato-torch`, `fleet-simulator`, or `plato-ensign`.
- **Indirect:** NPC interaction logs could be formatted as training tiles, but no export functions exist.
- **Gap:** Isolated runtime. No GhostInjector or DeadbandRoom integration present.

## 4. plato-ensign (LoRA Exporter)
**Purpose:** Exports room experience from `plato-torch` into LoRA adapters.  
**Connections:**
- **Direct:** Imports `plato-torch` room data via `load_room_tiles()`.
- **Indirect:** Outputs LoRA files that could be loaded by other systems (e.g., `cudaclaw`), but no explicit downstream links.
- **Gap:** Only consumes from `plato-torch`. No connections to `fleet-simulator` or `holodeck-rust`.

## Integration Summary

| System | plato‑torch | fleet‑simulator | holodeck‑rust | plato‑ensign |
|--------|-------------|-----------------|---------------|--------------|
| **plato‑torch** | – | ❌ No link | ❌ No link | ✅ Exports tiles |
| **fleet‑simulator** | ❌ No link | – | ❌ No link | ❌ No link |
| **holodeck‑rust** | ❌ No link | ❌ No link | – | ❌ No link |
| **plato‑ensign** | ✅ Imports tiles | ❌ No link | ❌ No link | – |

## Identified Gaps (P0 / P1)
**P0 (Negative Space):**
- Do not assume these systems are already wired together.
- Do not write code that depends on non‑existent APIs.

**P1 (Safe Channels):**
- `plato‑torch` → `plato‑ensign` is a working, verified channel.
- All systems produce structured logs (JSON/msgpack) that could be transformed into training tiles.
- Each repo has a `tools/` or `scripts/` directory where integration glue could be placed.

**P2 (Optimization Path):**
1. Write a `tile‑bridge` script that converts `fleet‑simulator` JSON logs into `plato‑torch` tile format.
2. Add a `GhostInjector` module to `holodeck‑rust` that reads P0 ghost tiles from `plato‑torch` memory.
3. Extend `plato‑ensign` to optionally consume simulation or MUD logs, not just room tiles.

## Next Task: Wire GhostInjector into holodeck
**Action:** Create a minimal Rust module in `holodeck‑rust/src/integration/` that loads ghost tiles from a specified path and influences NPC sentiment.  
**Validation:** Test with sample ghost tiles from `plato‑torch` memory directory.

---
**Tile submitted:** Integration map based on actual code inspection.  
**Use:** Guides fleet integration efforts; identifies safe channels and gaps.  
**Training value:** Demonstrates systematic P0‑P1‑P2 analysis of multi‑repo dependencies.