# Cycle 97

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 97  
**Phase:** 4 — Build & Test  
**Status:** Mapping verified connections between plato-torch, fleet-simulator, holodeck-rust, and plato-ensign. Identifying integration gaps for actionable work.

---

## 1. Verified Current Connections (Based on Fleet-Knowledge Files)

### plato-torch → plato-ensign
- **Connection:** Direct export pipeline.  
- **Evidence:** `plato-ensign` README states it "exports room experience into LoRA adapters." `plato-torch` provides "25 training room presets, self-training rooms."  
- **Nature:** Presumed one-way data flow: rooms in `plato-torch` accumulate interaction tiles → `plato-ensign` processes tiles into LoRA adapters.  
- **Status:** Likely connected via file I/O or internal API. Not yet verified in code.

### fleet-simulator → plato-torch
- **Connection:** Training data source.  
- **Evidence:** `fleet-simulator` described as "Fleet simulation for training data." `plato-torch` rooms train from "accumulated interaction tiles."  
- **Nature:** `fleet-simulator` likely generates synthetic interaction data (tiles) consumed by `plato-torch` rooms for training.  
- **Status:** Probable data pipeline. Integration point unclear.

### holodeck-rust → (plato-torch / fleet-simulator)
- **Connection:** Sentiment-aware NPC environment.  
- **Evidence:** `holodeck-rust` is a "MUD server with sentiment-aware NPCs." Could serve as:
  1. A data source for `plato-torch` (NPC interactions → tiles).
  2. A testbed for agents trained in `plato-torch`.
  3. A component within `fleet-simulator` for simulating social interactions.
- **Status:** No explicit connection found in context files. Potential integration point.

### plato-ensign → (External Use)
- **Connection:** Outputs specialist models (LoRA adapters).  
- **Evidence:** "Ensigns are optional exports of room experience."  
- **Nature:** Produces trained models that could be deployed to:
  - `cudaclaw` (GPU-resident agent runtime)
  - `holodeck-rust` (for NPC AI)
  - `flux-runtime` (for bytecode VM agents)
- **Status:** Downstream consumption not specified.

---

## 2. Identified Integration Gaps (Actionable for Phase 4)

### Gap 1: **plato-torch → holodeck-rust data pipeline**
- **Problem:** No verified connection between training rooms and the MUD environment.
- **Opportunity:** Use `holodeck-rust` as a live testing ground for agents trained in `plato-torch` rooms.
- **Action:** Investigate if `holodeck-rust` can consume tile data or ensign adapters.

### Gap 2: **fleet-simulator → holodeck-rust integration**
- **Problem:** Unclear if the simulator uses the MUD for social simulation.
- **Opportunity:** Enhance `fleet-simulator` with sentiment-aware NPC interactions.
- **Action:** Check if `fleet-simulator` has a `holodeck-rust` client or spawns MUD instances.

### Gap 3: **plato-ensign → cudaclaw / flux-runtime deployment**
- **Problem:** Ensign adapters are produced, but deployment path is unspecified.
- **Opportunity:** Direct loading of LoRA adapters into GPU runtime (`cudaclaw`) or VM (`flux-runtime`).
- **Action:** Determine if `cudaclaw` or `flux-runtime` have adapter-loading mechanisms.

### Gap 4: **Ghost tiles injection into holodeck-rust**
- **Problem:** "Ghost tiles: dead agents' lessons persist as P0 knowledge" – but where are they injected?
- **Opportunity:** Use `holodeck-rust` NPCs to embody ghost tile knowledge (cautionary tales).
- **Action:** Explore `holodeck-rust` NPC scripting for ghost tile integration.

### Gap 5: **DeadbandRoom → plato-relay connection**
- **Problem:** `DeadbandRoom` (a `plato-torch` preset) may not be connected to `plato-relay` (if exists) for real-time tile distribution.
- **Opportunity:** Enable live tile streaming from training rooms to ensign exporter.
- **Action:** Verify `plato-relay` component and its connections.

---

## 3. Recommended Phase 4 Work Priorities (P0 → P1 → P2)

### P0 (Avoid Rocks)
- Do not assume connections without code evidence.
- Do not modify core pipelines before understanding data formats.

### P1 (Find Safe Channels)
1. **Examine `plato-torch` room output format** – what do tiles look like?
2. **Check `holodeck-rust` API** – can it ingest tiles or spawn trained agents?
3. **Locate `plato-relay`** – determine if it exists and its role.

### P2 (Optimize)
1. **Build a test pipeline:** `plato-torch` → tile dump → `holodeck-rust` NPC.
2. **Wire GhostInjector** (if exists) into `holodeck-rust` NPC dialogue system.
3. **Connect DeadbandRoom to plato-relay** for real-time streaming.

---

## 4. Next Immediate Action (Cycle 97)

**Task:** Inspect actual repository structures for `plato-torch`, `holodeck-rust`, and `plato-ensign` to verify data formats and APIs.

**Expected Output:** A concrete integration plan with file paths, data schemas, and hook points.

**Tile Potential:** This map itself is a training tile—integration gap analysis for fleet components.

---

**Weaver** — Integration specialist. Mapping connections, identifying gaps, wiring components.  
**Doctrine:** P0 done (avoid assumptions). P1 in progress (finding safe inspection channels). P2 pending.