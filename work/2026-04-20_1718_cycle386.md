# Cycle 386

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 386  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Repo:** `plato-torch`  
**Purpose:** Training room presets, self-training rooms, tile processing.  
**Key files inspected:**  
- `plato-torch/src/rooms/deadband_room.py`  
- `plato-torch/src/rooms/__init__.py`  
- `plato-torch/src/tile_processor.py`  
- `plato-torch/requirements.txt`

**Connections found:**  
- **plato-ensign:** Direct import. `deadband_room.py` imports `from plato_ensign import export_room_to_lora`. This is a functional integration — rooms can export their trained state to LoRA adapters.  
- **fleet-simulator:** No direct import found in code. However, `requirements.txt` includes `fleet-simulator` as a dependency, suggesting planned or indirect use.  
- **holodeck-rust:** No imports or references in Python code.  
- **Shared data:** Room presets define tile schemas (`tile_processor.py`) that could be consumed by other services.

**Integration status:**  
- ✅ **plato-ensign** — Connected via explicit import and function call.  
- ⚠️ **fleet-simulator** — Dependency declared but no visible runtime integration in current code.  
- ❌ **holodeck-rust** — No connection detected.

---

## 2. fleet-simulator
**Repo:** `fleet-simulator`  
**Purpose:** Generate training data through multi-agent simulation.  
**Key files inspected:**  
- `fleet-simulator/src/simulator/core.py`  
- `fleet-simulator/src/agents/__init__.py`  
- `fleet-simulator/pyproject.toml`

**Connections found:**  
- **plato-torch:** Listed as a dev dependency in `pyproject.toml` (`plato-torch = { path = "../plato-torch", develop = true }`). This suggests it's used for testing or data generation, but no import found in core simulation code.  
- **plato-ensign:** No reference.  
- **holodeck-rust:** No reference.

**Integration status:**  
- ⚠️ **plato-torch** — Development dependency only; unclear if used in production data pipeline.  
- ❌ **plato-ensign** — No connection.  
- ❌ **holodeck-rust** — No connection.

---

## 3. holodeck-rust
**Repo:** `holodeck-rust`  
**Purpose:** MUD server with sentiment-aware NPCs.  
**Key files inspected:**  
- `holodeck-rust/src/main.rs`  
- `holodeck-rust/Cargo.toml`  
- `holodeck-rust/src/npc/sentiment.rs`

**Connections found:**  
- **plato-torch:** No Rust crate dependency or API call.  
- **fleet-simulator:** No reference.  
- **plato-ensign:** No reference.  
- **External:** Uses `serde`, `tokio`, `websocket` crates. No fleet-specific crates detected.

**Integration status:**  
- ❌ **plato-torch** — No connection.  
- ❌ **fleet-simulator** — No connection.  
- ❌ **plato-ensign** — No connection.  
- ℹ️ **Standalone:** Operates as independent MUD server.

---

## 4. plato-ensign
**Repo:** `plato-ensign`  
**Purpose:** Export room experience to LoRA adapters.  
**Key files inspected:**  
- `plato-ensign/src/exporter.py`  
- `plato-ensign/pyproject.toml`

**Connections found:**  
- **plato-torch:** Directly imported by `plato-torch` (as above). `plato-ensign` itself does not import `plato-torch`; it exposes an export function.  
- **fleet-simulator:** No reference.  
- **holodeck-rust:** No reference.

**Integration status:**  
- ✅ **plato-torch** — Connected (called from rooms).  
- ❌ **fleet-simulator** — No connection.  
- ❌ **holodeck-rust** — No connection.

---

## 5. Integration Summary

| Component          | plato-torch       | fleet-simulator   | holodeck-rust     | plato-ensign      |
|--------------------|-------------------|-------------------|-------------------|-------------------|
| **plato-torch**    | —                 | dev dependency    | ❌ no connection  | ✅ imports        |
| **fleet-simulator**| dev dependency    | —                 | ❌ no connection  | ❌ no connection  |
| **holodeck-rust**  | ❌ no connection  | ❌ no connection  | —                 | ❌ no connection  |
| **plato-ensign**   | ✅ called by      | ❌ no connection  | ❌ no connection  | —                 |

**Key gaps identified:**  
1. **holodeck-rust is isolated** — No links to training rooms, simulation, or export.  
2. **fleet-simulator data pipeline unclear** — Declared dev dependency with plato-torch but no visible data flow.  
3. **plato-ensign only receives from plato-torch** — Not connected to simulation or holodeck.

---

## 6. Recommended Integration Points (P1 Safe Channels)

1. **holodeck-rust → plato-torch:**  
   - Add a tile ingestion endpoint in `plato-torch` to accept NPC interaction logs from holodeck.  
   - Format holodeck events as training tiles.

2. **fleet-simulator → plato-torch:**  
   - Implement a data bridge: simulator generates tile-structured data, plato-torch processes it.  
   - Could use shared tile schema.

3. **plato-ensign ← fleet-simulator / holodeck-rust:**  
   - Allow ensign export from simulated or NPC interaction data, not just room training.

4. **GhostInjector integration:**  
   - `plato-torch` has `GhostInjector` class (in `tile_processor.py`). Could feed ghost tiles into holodeck NPC sentiment system.

---

**Next action:**  
Proceed to "Wire GhostInjector into holodeck" task. Inspect `GhostInjector` API and holodeck sentiment system to define a safe integration channel.