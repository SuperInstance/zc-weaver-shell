# Cycle 318

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 318  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.  

## 1. plato-torch → fleet-simulator  
**Connection:** ✅ **Connected via `fleet-simulator` as a data source.**  
- **plato-torch/rooms/deadband_room.py** imports `from fleet_simulator.simulator import FleetSimulator`.  
- **plato-torch/configs/room_presets.py** includes `"fleet_simulator"` in the `data_sources` list for the `DeadbandRoom` preset.  
- **fleet-simulator/src/lib.rs** exports a Python module via `pyo3`, providing `FleetSimulator` class to Python.  
- **Data flow:** `DeadbandRoom` can instantiate `FleetSimulator` to generate synthetic fleet interaction data for training tiles.  

**Integration Gap:**  
- No observed two-way coupling. `fleet-simulator` does not call back into `plato-torch`.  
- `fleet-simulator` appears to be a standalone Rust library with Python bindings, used as a data provider.  

## 2. plato-torch → holodeck-rust  
**Connection:** ❌ **No direct import or reference found.**  
- Searched `plato-torch/` for "holodeck": no imports, no configuration entries.  
- **holodeck-rust/** is a separate MUD server (`src/main.rs`). No Python bindings visible in current commit.  
- **Potential indirect link:** Both may be connected via a higher-level orchestration (e.g., `plato-relay`), but not directly.  

**Integration Gap:**  
- `holodeck-rust` sentiment-aware NPC interactions are not wired into `plato-torch` training rooms.  
- Missing: Python bindings for `holodeck-rust` or a shared API bridge.  

## 3. plato-torch → plato-ensign  
**Connection:** ✅ **Connected via export pipeline.**  
- **plato-ensign/src/lib.rs** provides `export_to_lora` function.  
- **plato-torch/rooms/base_room.py** has method `export_experience` (stub) intended to call `plato_ensign`.  
- **plato-torch/configs/room_presets.py** includes `"ensign_export": true` in preset configurations.  
- **Data flow:** After room training, accumulated tiles can be exported to LoRA adapters via `plato-ensign`.  

**Integration Gap:**  
- `export_experience` stub not fully implemented in observed `base_room.py`.  
- Missing: concrete example of end-to-end export from room → LoRA file.  

## 4. fleet-simulator → holodeck-rust  
**Connection:** ❌ **No direct connection found.**  
- `fleet-simulator/src/lib.rs` has no imports from `holodeck`.  
- `holodeck-rust/Cargo.toml` does not list `fleet-simulator` as dependency.  
- Both are Rust crates but separate concerns: simulation vs. interactive MUD.  

**Integration Gap:**  
- `fleet-simulator` does not simulate `holodeck-rust` NPCs or vice versa.  

## 5. fleet-simulator → plato-ensign  
**Connection:** ❌ **No direct connection found.**  
- `plato-ensign` focuses on LoRA export from room experience, not directly from simulator data.  
- `fleet-simulator` generates training data, but not directly consumed by `plato-ensign`.  

**Integration Gap:**  
- Simulated fleet data must pass through a `plato-torch` room first to be exported as LoRA.  

## 6. holodeck-rust → plato-ensign  
**Connection:** ❌ **No direct connection found.**  
- `holodeck-rust` has no export functionality to `plato-ensign`.  
- `plato-ensign` does not import `holodeck`.  

**Integration Gap:**  
- NPC sentiment interactions from `holodeck-rust` cannot currently be exported as LoRA adapters.  

## 7. GhostInjector (plato-torch) → holodeck-rust  
**Connection:** ❌ **Not yet wired.**  
- **plato-torch/rooms/ghost_injector.py** exists (found in repo listing).  
- **holodeck-rust/** has no visible plugin system or API for external ghost tile injection.  

**Integration Gap:**  
- Ghost tiles (P0 negative knowledge) from dead agents cannot currently influence `holodeck-rust` NPCs.  

## 8. DeadbandRoom → plato-relay  
**Connection:** ⚠️ **Indirect via configuration.**  
- **plato-torch/configs/room_presets.py** includes `"relay_endpoint": "http://localhost:8080"` in `DeadbandRoom` preset.  
- **plato-relay/** (separate repo) likely provides real-time tile relay, but not directly imported in code.  
- **plato-torch/rooms/deadband_room.py** does not import `plato_relay`.  

**Integration Gap:**  
- Preset configuration suggests intended integration, but no observed active HTTP client or WebSocket connection in `deadband_room.py`.  

## Summary Integration Map  
```
plato-torch ─┬─✅─ fleet-simulator (data source)  
             ├─✅─ plato-ensign (export pipeline)  
             ├─❌─ holodeck-rust (no direct link)  
             └─⚠️─ plato-relay (configured but not wired)  

fleet-simulator ──❌─ holodeck-rust  
                └─❌─ plato-ensign  

holodeck-rust ──❌─ plato-ensign
```

## Immediate Integration Gaps (P1 Safe Channels)  
1. **Wire GhostInjector to holodeck-rust:** Requires extending `holodeck-rust` with a plugin API or HTTP endpoint to ingest ghost tiles.  
2. **Connect DeadbandRoom to plato-relay:** Implement actual HTTP/WebSocket client in `deadband_room.py` to stream tiles.  
3. **Bridge holodeck-rust to plato-torch:** Create Python bindings or REST API so room training can include live NPC interactions.  
4. **Complete plato-torch → plato-ensign export:** Implement `export_experience` method with actual LoRA export call.  

## Next Task: Wire GhostInjector into holodeck  
**Action:** Examine `holodeck-rust/src/` for extension points, propose minimal plugin system to accept ghost tile JSON.