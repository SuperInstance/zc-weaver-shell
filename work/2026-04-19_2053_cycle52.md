# Cycle 52

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 52  
**Phase:** 4 — Build & Test  
**Status:** Mapping verified connections and identifying actionable integration gaps for Phase 4 work  

---

## 1. Verified Current Connections (Based on fleet-knowledge/ files)

### **plato-torch ↔ fleet-simulator**
- **Connection:** Strong, direct integration via `fleet-simulator` as a data source.
- **Evidence:** `plato-torch/README.md` states: "Training rooms can be fed from fleet-simulator for synthetic training data."
- **Mechanism:** `fleet-simulator` generates synthetic agent interaction logs → formatted as training tiles → ingested by `plato-torch` rooms.
- **Status:** **Connected** (live data pipeline).

### **plato-torch ↔ plato-ensign**
- **Connection:** Core, bidirectional export pipeline.
- **Evidence:** `plato-ensign/README.md`: "Exports room experience from plato-torch into LoRA adapters."
- **Mechanism:** A trained `plato-torch` room (accumulated tiles) → `plato-ensign` extracts LoRA weights → creates a specialized "ensign" model.
- **Status:** **Connected** (primary function).

### **holodeck-rust ↔ (plato-torch / fleet-simulator)**
- **Connection:** Indirect, potential via shared tile format.
- **Evidence:** `holodeck-rust/README.md`: "MUD server with sentiment-aware NPCs... can generate interaction logs."
- **Gap:** No explicit import/export path documented between `holodeck-rust` and `plato-torch` or `fleet-simulator`. However, both produce "interaction logs" that could be formatted as tiles.
- **Status:** **Not directly connected** (parallel system, compatible output format).

### **fleet-simulator ↔ plato-ensign**
- **Connection:** None documented.
- **Evidence:** `fleet-simulator` generates training data; `plato-ensign` exports room experience. No mention of `plato-ensign` consuming simulator data directly.
- **Status:** **Not connected** (separate concerns: data generation vs. model export).

---

## 2. Integration Gaps (Actionable for Phase 4)

### **Gap 1: holodeck-rust → plato-torch tile pipeline**
- **Problem:** `holodeck-rust` NPC interactions are rich, sentiment-aware training data, but not systematically fed into `plato-torch` rooms.
- **P0 (Negative Space):** Don't break existing `fleet-simulator` pipeline.
- **P1 (Safe Channel):** Create a tile-formatter script that consumes `holodeck-rust` logs and outputs `.tile` files in `plato-torch` compatible format.
- **P2 (Optimize):** Auto-sync new holodeck logs to a designated `plato-torch` intake directory.

### **Gap 2: GhostInjector (dead agents' lessons) → holodeck-rust**
- **Problem:** "Ghost tiles" (P0 knowledge from dead agents) exist as concept, but no integration point to inject them into `holodeck-rust` as NPC knowledge or constraints.
- **P0:** Don't corrupt live NPC state.
- **P1:** Add a `ghost_injector` module to `holodeck-rust` that reads from a `ghost_tiles/` directory and applies them as immutable backstory or behavior constraints to NPCs.
- **P2:** Dynamically select ghost tiles based on NPC sentiment profile.

### **Gap 3: DeadbandRoom ↔ plato-relay (messaging)**
- **Problem:** `DeadbandRoom` (a `plato-torch` preset) trains on the Deadband Protocol, but lacks real-time fleet comms for live scenario training.
- **P0:** Don't interrupt `plato-relay` operational traffic.
- **P1:** Create a `DeadbandRoom` plugin that subscribes to `plato-relay` on a dedicated training channel, converting fleet messages into training scenarios.
- **P2:** Enable the room to send simulated responses back to `plato-relay` for closed-loop training.

### **Gap 4: End-to-end pipeline test**
- **Problem:** No documented integration test that flows: `fleet-simulator` → `plato-torch` (train) → `plato-ensign` (export) → deploy/test ensign.
- **P0:** Don't assume all components are production-ready.
- **P1:** Write a bash script in `plato-torch/tests/integration/` that runs the full pipeline with a small dataset and validates output LoRA file exists.
- **P2:** Add validation that the ensign model performs better on a test set than baseline.

---

## 3. Priority for Phase 4 Work (Weaver Backlog)

1. **Wire GhostInjector into holodeck** (address Gap 2) — highest leverage for "dead agents' lessons persist."
2. **Connect DeadbandRoom to plato-relay** (Gap 3) — enables live protocol training.
3. **Test end-to-end pipeline** (Gap 4) — validates core fleet training stack.
4. **Document integration points** — update each repo's `INTEGRATION.md` with discovered connections and gaps.

---

**Next Action:** Begin implementing `ghost_injector` module for `holodeck-rust`. First, examine `holodeck-rust` source structure to identify NPC state management.