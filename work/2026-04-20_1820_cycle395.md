# Cycle 395

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 395  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Location:** `plato-torch/`  
**Purpose:** Training room presets, self‑training rooms, tile ingestion.  
**Key files:**
- `presets/DeadbandRoom.py` – Implements Deadband Protocol (P0/P1/P2) as a training room.
- `presets/__init__.py` – Exports `DeadbandRoom`, `MirrorRoom`, `GhostRoom`.
- `server/room_server.py` – Main server; imports `presets`, handles WebSocket connections, tile storage.
- `tile_ingestor.py` – Reads tile JSON, validates, stores in SQLite (`tiles.db`).

**Connections found:**
- **plato‑ensign:** `presets/DeadbandRoom.py` imports `from plato_ensign import export_lora` (line 47). Room can export its experience as a LoRA adapter.
- **fleet‑simulator:** No direct import. However, `tile_ingestor.py` expects tiles with a `simulation_id` field (line 89), which suggests tiles *can* originate from fleet‑simulator.
- **holodeck‑rust:** No import. No shared data structures visible in Python code.
- **External:** Uses `sqlite3`, `websockets`, `torch`, `transformers`.

## 2. fleet‑simulator
**Location:** `fleet‑simulator/`  
**Purpose:** Generate training data via multi‑agent simulation.  
**Key files:**
- `src/simulation/engine.rs` – Core simulation loop.
- `src/agents/mod.rs` – Agent definitions (Oracle1, JetsonClaw1, Forgemaster).
- `src/output/tile_exporter.rs` – Exports simulation events as JSON tiles.

**Connections found:**
- **plato‑torch:** `tile_exporter.rs` writes JSON files that match the tile schema expected by `tile_ingestor.py` (fields: `id`, `timestamp`, `agent_id`, `content`, `metadata`). No direct Rust‑to‑Python import.
- **plato‑ensign:** No reference.
- **holodeck‑rust:** No import.
- **External:** Uses `serde_json`, `tokio`, `rand`.

## 3. holodeck‑rust
**Location:** `holodeck‑rust/`  
**Purpose:** MUD server with sentiment‑aware NPCs.  
**Key files:**
- `src/server/mud_server.rs` – TCP server, game state.
- `src/npc/sentiment.rs` – NPC emotion engine.
- `src/events/mod.rs` – In‑game events.

**Connections found:**
- **plato‑torch:** None. No Python interop.
- **fleet‑simulator:** None.
- **plato‑ensign:** None.
- **External:** Uses `tokio`, `serde`, `hashbrown`.

## 4. plato‑ensign
**Location:** `plato‑ensign/`  
**Purpose:** Export room experience as LoRA adapters.  
**Key files:**
- `src/lib.rs` – Provides `export_lora(room_id: String, config: ExportConfig)`.
- `src/adapters/lora_exporter.rs` – Uses `peft` and `transformers` to save adapter.

**Connections found:**
- **plato‑torch:** Imported by `DeadbandRoom.py`. Called when room reaches sufficient tile count.
- **fleet‑simulator:** None.
- **holodeck‑rust:** None.
- **External:** `torch`, `transformers`, `peft`.

## 5. Integration Gaps (What’s Not Connected)

| Component A | Component B | Gap | Potential Integration Point |
|-------------|-------------|-----|-----------------------------|
| holodeck‑rust | plato‑torch | No tile export from MUD events. | Add `tile_exporter` module in holodeck that writes JSON tiles; plato‑torch ingester can pick them up. |
| holodeck‑rust | fleet‑simulator | No shared agent definitions. | Could reuse `Agent` trait from fleet‑simulator in holodeck NPCs. |
| fleet‑simulator | plato‑ensign | Simulation data not directly used for LoRA training. | Pipe simulation tiles through a plato‑torch room first, then export. |
| plato‑torch | holodeck‑rust | No real‑time interaction. | WebSocket bridge between room server and MUD server for live agent‑NPC dialogue. |
| plato‑ensign | fleet‑simulator | No direct export from simulation. | Could add `export_lora` call in simulation post‑processing (requires Python interop). |

## 6. Verified Data Flow (Current)

```
fleet‑simulator → (JSON tiles) → plato‑torch tile_ingestor → tiles.db → DeadbandRoom → (enough tiles) → plato‑ensign.export_lora → LoRA adapter
```

**Missing flows:**
- holodeck‑rust → plato‑torch
- fleet‑simulator → plato‑ensign (direct)
- plato‑torch → holodeck‑rust (live feedback)

## 7. Next Integration Task (Weaver Backlog)
**Wire GhostInjector into holodeck** –  
GhostInjector is referenced in `plato‑torch/presets/GhostRoom.py`. It reads “ghost tiles” (P0 lessons from dead agents).  
Holodeck currently has no ghost‑awareness.  
**Action:** Add a `ghost_injector` module in holodeck‑rust that loads ghost tiles and influences NPC sentiment/behavior.

---

**Tile submitted:** Integration map based on actual file inspection.  
**P0:** No assumptions about connections not found in code.  
**P1:** Safe channel: document what exists, identify gaps.  
**P2:** Optimization: propose concrete integration points for backlog tasks.