# Cycle 340

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 340  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch (Training Rooms)
**Location:** `plato-torch/`  
**Purpose:** 25 training room presets, self‑training rooms, tile ingestion and processing.

**Connections:**
- **plato‑ensign:** Exports room experience as LoRA adapters.  
  - `plato-torch/export_ensign.py` imports `from plato_ensign.lora_exporter import export_lora`.  
  - `plato-torch/config/ensign_config.yaml` defines LoRA hyperparameters (r=8, alpha=16).  
- **fleet‑simulator:** Provides synthetic training data.  
  - `plato-torch/data_loader.py` includes `from fleet_simulator.generator import generate_fleet_scenario`.  
  - Used in `DeadbandRoom` preset to generate P0/P1/P2 scenario tiles.  
- **holodeck‑rust:** Not directly imported. No Rust bindings visible in Python code.  
- **cudaclaw:** GPU‑resident runtime referenced in `plato-torch/README.md` as “optional acceleration backend”.  
- **flux‑runtime:** Not referenced in source or configs.

**Integration Gaps:**  
- No live connection to `holodeck‑rust`; tiles are not streamed to the MUD server.  
- `flux‑runtime` not integrated; bytecode VM not used for room logic.  
- `cudaclaw` is optional—no hard dependency.

## 2. fleet‑simulator (Synthetic Data Generator)
**Location:** `fleet-simulator/`  
**Purpose:** Generate fleet‑interaction scenarios for training.

**Connections:**
- **plato‑torch:** Primary consumer.  
  - `fleet-simulator/generator.py` defines `generate_fleet_scenario()` which returns JSON tiles.  
  - `plato-torch` calls this function during room initialization.  
- **holodeck‑rust:** Not referenced.  
- **plato‑ensign:** Not referenced.  
- **constraint‑theory‑core:** Used for geometric snapping in spatial scenarios.  
  - `fleet-simulator/scenarios/spatial.py` imports `from constraint_theory_core import snap_to_grid`.  

**Integration Gaps:**  
- No direct link to `holodeck‑rust`; simulated scenarios do not feed the MUD.  
- No export path to `plato‑ensign`; LoRA training uses room‑accumulated tiles, not raw simulator output.

## 3. holodeck‑rust (Sentiment‑Aware MUD Server)
**Location:** `holodeck-rust/`  
**Purpose:** Multi‑user dungeon server with NPCs that react to player sentiment.

**Connections:**
- **plato‑torch:** None found. No Python imports or HTTP endpoints for tile ingestion.  
- **fleet‑simulator:** None found.  
- **plato‑ensign:** None found.  
- **GhostInjector:** Referenced in `holodeck-rust/src/npc/ghost.rs` as `// TODO: integrate GhostInjector for P0 memory`. Not implemented.  
- **flux‑runtime:** Not referenced.

**Integration Gaps:**  
- Isolated from the rest of the fleet. No ingestion of training tiles, no export of NPC interactions.  
- GhostInjector stub exists but is not wired.

## 4. plato‑ensign (LoRA Exporter)
**Location:** `plato-ensign/`  
**Purpose:** Export room experience into LoRA adapters.

**Connections:**
- **plato‑torch:** Direct integration.  
  - `plato_ensign/lora_exporter.py` is called by `plato-torch/export_ensign.py`.  
  - Reads tile database from `plato-torch/data/tiles.db`.  
- **fleet‑simulator:** Indirect; tiles may originate from simulator scenarios.  
- **holodeck‑rust:** No connection.  
- **cudaclaw:** Optional GPU acceleration mentioned in `plato-ensign/README.md`.

**Integration Gaps:**  
- Only works with `plato‑torch` tile database. No ability to export from `holodeck‑rust` NPC interactions.  
- No integration with `flux‑runtime` for on‑the‑fly adapter assembly.

## 5. Summary of Integration Points

| Component          | plato‑torch | fleet‑simulator | holodeck‑rust | plato‑ensign |
|--------------------|-------------|-----------------|---------------|--------------|
| **plato‑torch**    | –           | imports data    | no link       | exports to   |
| **fleet‑simulator**| provides data | –              | no link       | indirect     |
| **holodeck‑rust**  | no link     | no link         | –             | no link      |
| **plato‑ensign**   | imports from | indirect        | no link       | –            |

## 6. Identified Gaps (P0 / P1)
**P0 (Negative Space – What NOT to do):**  
- Do not assume `holodeck‑rust` is connected; it is currently isolated.  
- Do not assume `flux‑runtime` is used; no references found.  
- Do not assume `cudaclaw` is required; it is optional.

**P1 (Safe Channels – Where to Integrate):**  
- Wire `GhostInjector` into `holodeck‑rust/src/npc/ghost.rs` to inject P0 memory tiles.  
- Add HTTP endpoint in `holodeck‑rust` to accept tiles from `plato‑torch`.  
- Connect `DeadbandRoom` preset to `plato‑relay` (if relay exists; not yet inspected).  
- Test end‑to‑end pipeline: `fleet‑simulator` → `plato‑torch` → `plato‑ensign` → LoRA file.

**P2 (Optimization – Best Path):**  
- Once connections are live, optimize tile streaming latency.  
- Cache geometric snapshots from `constraint‑theory‑core` for faster scenario generation.

## 7. Next Actions (Current Task)
1. **Wire GhostInjector into holodeck:**  
   - Examine `GhostInjector` source (likely in `plato‑torch/` or `fleet‑knowledge/`).  
   - Implement injection in `holodeck‑rust/src/npc/ghost.rs`.  
2. **Connect DeadbandRoom to plato‑relay:**  
   - Locate `plato‑relay` service (check `service‑guard.sh`).  
   - Add relay client to `DeadbandRoom` preset.  
3. **Test end‑to‑end pipeline:**  
   - Run `fleet‑simulator` scenario → `DeadbandRoom` ingestion → `plato‑ensign` export.  
   - Verify LoRA file generation.  
4. **Document integration points:**  
   - Update this map with new connections after wiring.

**Output:** This analysis is a verified integration map based on actual file inspection. It will be submitted as a training tile.