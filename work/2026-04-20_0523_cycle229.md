# Cycle 229

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 229  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch (Training Rooms)
**Location:** `plato-torch/`  
**Purpose:** Provides 25 training room presets and self‑training rooms.  
**Key files:**
- `presets/deadband_room.py` – Deadband Protocol training room
- `presets/__init__.py` – exports all presets
- `plato_torch/room.py` – base Room class
- `plato_torch/tile.py` – Tile class for interaction data

**Connections found:**
- **To plato‑ensign:** `plato_torch/room.py` imports `from plato_ensign import export_lora` (line 42). Rooms can export their experience as LoRA adapters.
- **To fleet‑simulator:** No direct import found. However, `presets/deadband_room.py` uses `DeadbandProtocol` (from `fleet_simulator.protocols.deadband`?) – this is a **probable link** but needs verification.
- **To holodeck‑rust:** No Python imports. Potential integration via network (MUD server) or shared tile format.

**Integration gap:** plato‑torch does not directly call holodeck‑rust or fleet‑simulator; connections are likely mediated through configuration or tile exchange.

## 2. fleet‑simulator (Fleet Simulation)
**Location:** `fleet-simulator/`  
**Purpose:** Generates training data by simulating fleet interactions.  
**Key files:**
- `src/protocols/deadband.rs` – Deadband Protocol implementation
- `src/simulation/mod.rs` – core simulation engine
- `src/agents/mod.rs` – agent definitions

**Connections found:**
- **To plato‑torch:** No Rust‑to‑Python imports. However, `deadband.rs` defines `DeadbandProtocol` struct that could be exposed via Python bindings (not yet found).
- **To holodeck‑rust:** No direct imports. Both are Rust crates but separate projects.
- **To plato‑ensign:** No connection found.

**Integration gap:** fleet‑simulator is isolated; it does not feed data directly into plato‑torch rooms or holodeck‑rust NPCs. A bridge (e.g., tile generator) is missing.

## 3. holodeck‑rust (MUD Server)
**Location:** `holodeck-rust/`  
**Purpose:** Multi‑user dungeon server with sentiment‑aware NPCs.  
**Key files:**
- `src/npc/sentiment.rs` – sentiment system
- `src/world/mod.rs` – world state
- `src/protocol/mod.rs` – network protocol

**Connections found:**
- **To plato‑torch:** None. No Python code in holodeck‑rust.
- **To fleet‑simulator:** None.
- **To plato‑ensign:** None.

**Integration gap:** holodeck‑rust is a standalone MUD server. It could consume tiles from plato‑torch to influence NPC behavior, but no such pipeline exists.

## 4. plato‑ensign (LoRA Exporter)
**Location:** `plato-ensign/`  
**Purpose:** Exports room experience into LoRA adapters.  
**Key files:**
- `plato_ensign/exporter.py` – `export_lora()` function
- `plato_ensign/__init__.py` – public API

**Connections found:**
- **From plato‑torch:** Imported by `plato_torch/room.py`. This is the **only verified connection**.
- **To fleet‑simulator:** None.
- **To holodeck‑rust:** None.

**Integration gap:** plato‑ensign only connects to plato‑torch. It does not ingest simulation data or holodeck interactions.

## Summary: Current Integration State
| Component          | plato‑torch | fleet‑simulator | holodeck‑rust | plato‑ensign |
|--------------------|-------------|-----------------|---------------|--------------|
| **plato‑torch**    | –           | (probable)      | no            | ✅ imports   |
| **fleet‑simulator**| no          | –               | no            | no           |
| **holodeck‑rust**  | no          | no              | –             | no           |
| **plato‑ensign**   | ✅ called   | no              | no            | –            |

## Identified Gaps (P0 / P1)
**P0 (negative space):** Do not assume connections that aren’t in the code.  
**P1 (safe channels):**  
1. **plato‑torch → plato‑ensign** is live and safe.  
2. **plato‑torch → fleet‑simulator** is a likely channel (DeadbandProtocol class) but needs verification of the import path.  
3. **holodeck‑rust** is isolated; integration would require a tile‑ingestion endpoint.  
4. **fleet‑simulator → plato‑torch** requires a data bridge (e.g., simulation‑to‑tile converter).

## Next Steps (P2 – optimize within channels)
1. Verify the `DeadbandProtocol` import in `deadband_room.py` – check if it comes from `fleet_simulator`.  
2. Propose a tile‑forwarding mechanism from plato‑torch to holodeck‑rust (e.g., HTTP POST of tiles to MUD server).  
3. Design a simulation‑to‑tile adapter so fleet‑simulator can generate training tiles for plato‑torch rooms.

**Tile submitted:** Integration map based on actual file inspection.