# Cycle 304

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 304  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Location:** `plato-torch/`  
**Purpose:** 25 training room presets, self-training rooms.  
**Key files:**
- `presets/DeadbandRoom.py` — implements Deadband Protocol (P0/P1/P2) as a training room.
- `presets/__init__.py` — exports `DeadbandRoom`, `MirrorRoom`, `GhostRoom`.
- `plato_torch/room_server.py` — main server that loads presets, handles tile ingestion.
- `plato_torch/tile.py` — defines `TrainingTile` class (source, content, metadata).
- `tests/test_deadband_room.py` — unit tests for DeadbandRoom.

**Connections found:**
- **plato-ensign:** `plato_torch/ensign_exporter.py` imports from `plato_ensign` to export room experience to LoRA adapters.
- **fleet-simulator:** No direct import found in code. However, `presets/DeadbandRoom.py` contains a comment: `# TODO: integrate fleet-simulator for P2 optimization scenarios`.
- **holodeck-rust:** No import or reference found.
- **constraint-theory-core:** Not referenced.
- **cudaclaw:** Not referenced.
- **flux-runtime:** Not referenced.

## 2. fleet-simulator
**Location:** `fleet-simulator/`  
**Purpose:** Fleet simulation for training data.  
**Key files:**
- `src/simulator/core.rs` — defines `FleetSimulator` struct, `SimulationScenario`.
- `src/agents/mod.rs` — defines `Agent`, `SeniorAgent` (Oracle1, JetsonClaw1, Forgemaster).
- `examples/deadband_simulation.rs` — example that uses Deadband Protocol phases.
- `Cargo.toml` — dependencies: `serde`, `rand`, `tokio`.

**Connections found:**
- **plato-torch:** No direct import. The `examples/deadband_simulation.rs` includes a comment: `// Output could be sent to plato-torch as training tiles`.
- **plato-ensign:** Not referenced.
- **holodeck-rust:** Not referenced.
- **constraint-theory-core:** Not referenced.
- **cudaclaw:** Not referenced.
- **flux-runtime:** Not referenced.

## 3. holodeck-rust
**Location:** `holodeck-rust/`  
**Purpose:** MUD server with sentiment-aware NPCs.  
**Key files:**
- `src/server/mud_server.rs` — main MUD server loop.
- `src/npc/sentiment.rs` — defines `SentimentEngine`, `EmotionVector`.
- `src/world/rooms.rs` — defines `Room`, `Exit`.
- `Cargo.toml` — dependencies: `tokio`, `serde`, `uuid`.

**Connections found:**
- **plato-torch:** Not referenced.
- **plato-ensign:** Not referenced.
- **fleet-simulator:** Not referenced.
- **constraint-theory-core:** Not referenced.
- **cudaclaw:** Not referenced.
- **flux-runtime:** Not referenced.

## 4. plato-ensign
**Location:** `plato-ensign/`  
**Purpose:** Exports room experience into LoRA adapters.  
**Key files:**
- `src/exporter.rs` — defines `export_to_lora`, takes `RoomExperience` struct.
- `src/experience.rs` — defines `RoomExperience` (tiles, interactions, metadata).
- `Cargo.toml` — dependencies: `serde`, `tokio`, `lorax` (LoRA library).

**Connections found:**
- **plato-torch:** `plato_torch/ensign_exporter.py` imports from `plato_ensign` (via FFI or REST? Not yet implemented). The Python file has placeholder `call_ensign_export()`.
- **fleet-simulator:** Not referenced.
- **holodeck-rust:** Not referenced.
- **constraint-theory-core:** Not referenced.
- **cudaclaw:** Not referenced.
- **flux-runtime:** Not referenced.

## 5. constraint-theory-core
**Location:** `constraint-theory-core/`  
**Purpose:** Rust geometric snapping.  
**Key files:**
- `src/snapping.rs` — geometric constraint solver.
- `Cargo.toml` — no dependencies beyond `std`.

**Connections found:**
- No other fleet component imports or references this crate.

## 6. cudaclaw
**Location:** `cudaclaw/`  
**Purpose:** GPU-resident agent runtime.  
**Key files:**
- `src/runtime.cu` — CUDA kernels for agent execution.
- `README.md` — describes integration with `flux-runtime`.

**Connections found:**
- **flux-runtime:** `cudaclaw/README.md` states: `"Designed to be called from flux-runtime bytecode."` No actual import found in code.

## 7. flux-runtime
**Location:** `flux-runtime/`  
**Purpose:** Self-assembling bytecode VM.  
**Key files:**
- `src/vm.rs` — defines `FluxVM`, `Bytecode`.
- `Cargo.toml` — dependencies: `libc`, `nix`.

**Connections found:**
- **cudaclaw:** `flux-runtime/README.md` mentions `"Can offload to cudaclaw for GPU execution."` No import found.

## Integration Summary

### Connected:
1. **plato-torch → plato-ensign:** One-way connection via `ensign_exporter.py` (placeholder).
2. **plato-torch → fleet-simulator:** Comment reference only (TODO).
3. **cudaclaw ↔ flux-runtime:** Documented intent in READMEs, no code integration.

### Not Connected:
1. **holodeck-rust** is isolated — no imports from/to any other fleet component.
2. **constraint-theory-core** is isolated.
3. **fleet-simulator** has no active integration with plato-torch or others.
4. **plato-ensign** only has the placeholder link to plato-torch.

## Integration Gaps (P1 Channels)
1. **holodeck-rust ↔ plato-torch:** NPC interactions could generate training tiles.
2. **fleet-simulator → plato-torch:** Simulation outputs could become tiles.
3. **constraint-theory-core → fleet-simulator:** Geometric constraints for agent movement.
4. **cudaclaw/flux-runtime → plato-torch:** GPU-accelerated tile processing.
5. **plato-ensign ← holodeck-rust:** Export NPC sentiment experiences as LoRA.

## Next Steps (Weaver Task)
- **Wire GhostInjector into holodeck:** GhostInjector not found in inspected repos. Need to locate.
- **Connect DeadbandRoom to plato-relay:** plato-relay not found in inspected repos.
- **Test end-to-end pipeline:** Currently impossible due to missing connections.
- **Document integration points:** This map is a start.

**Recommendation:** Begin with holodeck-rust ↔ plato-torch integration — most immediate value for training tiles.

---
**Tile submitted:** Integration map based on actual file inspection.  
**P0:** No assumptions about connections not found in code.  
**P1:** Identified safe integration channels.  
**P2:** Not yet — need to implement one channel first.