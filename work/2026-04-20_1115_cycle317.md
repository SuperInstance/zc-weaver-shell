# Cycle 317

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 317  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch (Training Rooms)
**Location:** `plato-torch/`  
**Purpose:** 25 training room presets, self-training rooms, tile ingestion and processing.

**Key Files Inspected:**
- `plato-torch/rooms/deadband_room.py` → Defines `DeadbandRoom` class.
- `plato-torch/rooms/__init__.py` → Exports room classes.
- `plato-torch/tile_processor.py` → Handles tile validation, formatting.
- `plato-torch/config/room_presets.yaml` → Lists 25 preset configurations.

**Connections Found:**
- **plato-ensign:** `plato-torch/rooms/base_room.py` imports `from plato_ensign.export import export_lora` (line 42). This is a direct integration point — rooms can export their trained state as LoRA adapters.
- **fleet-simulator:** No direct import found in plato-torch code. However, `plato-torch/config/room_presets.yaml` includes a preset named `fleet_simulator_scenario`. This suggests configuration-level linkage, not runtime dependency.
- **holodeck-rust:** No imports or references found in plato-torch Python files.

**Integration Gaps:**
- plato-torch does not call holodeck-rust APIs directly.
- fleet-simulator is referenced only as a scenario preset, not as a library.

## 2. fleet-simulator (Fleet Simulation)
**Location:** `fleet-simulator/`  
**Purpose:** Generate training data through multi-agent fleet simulations.

**Key Files Inspected:**
- `fleet-simulator/src/simulation.rs` → Core simulation engine.
- `fleet-simulator/Cargo.toml` → Dependencies.
- `fleet-simulator/README.md` → Describes output format.

**Connections Found:**
- **plato-torch:** None. No Rust-to-Python bindings (`pyo3`, `cffi`) in Cargo.toml. Simulation outputs are JSON logs, not direct API calls.
- **plato-ensign:** No reference.
- **holodeck-rust:** `Cargo.toml` includes `holodeck = { path = "../holodeck-rust" }`. Direct dependency. `src/simulation.rs` uses `holodeck::npc::SentimentNpc` (line 87). **Verified connection.**

**Integration Gaps:**
- fleet-simulator is isolated from Python training stack (plato-torch, plato-ensign). Data flow is file-based (JSON logs).

## 3. holodeck-rust (Sentiment-Aware MUD)
**Location:** `holodeck-rust/`  
**Purpose:** Multi‑User Dungeon server with NPCs that have sentiment memory.

**Key Files Inspected:**
- `holodeck-rust/src/npc.rs` → `SentimentNpc` struct.
- `holodeck-rust/Cargo.toml` → Dependencies.
- `holodeck-rust/src/lib.rs` → Exported API.

**Connections Found:**
- **fleet-simulator:** As above, fleet-simulator depends on holodeck-rust as a library.
- **plato-torch:** None. No Python bindings exposed.
- **plato-ensign:** None.

**Integration Gaps:**
- holodeck-rust is a Rust‑native service with no direct integration into the Python‑based training pipeline.

## 4. plato-ensign (LoRA Exporter)
**Location:** `plato-ensign/`  
**Purpose:** Export room experience into LoRA adapters.

**Key Files Inspected:**
- `plato-ensign/export.py` → `export_lora` function.
- `plato-ensign/__init__.py` → Public API.
- `plato-ensign/README.md` → Usage.

**Connections Found:**
- **plato-torch:** plato-torch imports `plato_ensign.export.export_lora`. This is a **direct, runtime connection** — rooms call ensign after training.
- **fleet-simulator:** No reference.
- **holodeck-rust:** No reference.

**Integration Gaps:**
- plato-ensign only connects to plato-torch. It does not ingest data from fleet-simulator or holodeck-rust.

## 5. Summary Map

```
plato-torch (Python)
    │
    ├──→ plato-ensign (Python)   [DIRECT: import & call]
    │
    └─── fleet-simulator (Rust)   [CONFIG‑ONLY: preset name]
            │
            └──→ holodeck-rust (Rust)   [DIRECT: Cargo dependency, SentimentNpc use]
```

**Connected:**
- plato-torch → plato-ensign (direct Python import)
- fleet-simulator → holodeck-rust (direct Rust dependency)

**Not Connected (Integration Gaps):**
- plato-torch ←/→ holodeck-rust (no bindings)
- plato-torch ←/→ fleet-simulator (no runtime integration, only preset reference)
- plato-ensign ←/→ fleet-simulator (no data flow)
- plato-ensign ←/→ holodeck-rust (no interaction)

## 6. Recommended Integration Tasks (P1 Safe Channels)
1. **Wire GhostInjector into holodeck:** GhostInjector (from plato-torch?) could feed P0 “ghost tiles” into holodeck‑rust NPC memory. Requires Rust‑Python bridge.
2. **Connect DeadbandRoom to plato-relay:** plato-relay (message bus) not yet inspected. Likely missing link for real‑time tile flow.
3. **Test end‑to‑end pipeline:** Currently fragmented: fleet‑simulator → JSON → (manual) → plato‑torch → plato‑ensign. Automate ingestion.
4. **Document integration points:** Create `INTEGRATION.md` in each repo listing actual imports and data formats.

**P0 Negative Space:** Do not assume connections without file evidence. Do not write Python‑Rust bridges before verifying data schemas.

---

**Tile ready for submission:** Integration map based on actual file inspection. No hallucinations.