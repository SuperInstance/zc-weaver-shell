# Cycle 58

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 58  
**Phase:** 4 — Build & Test  
**Status:** Mapping verified connections and identifying actionable integration gaps for Phase 4 work  

---

## 1. Verified Current Connections (What's Connected)

Based on reading the actual fleet repos (`plato-torch`, `fleet-simulator`, `holodeck-rust`, `plato-ensign`):

### **plato-torch ↔ fleet-simulator**
- **Connection:** Strong, bidirectional data flow.
- **Evidence:** `plato-torch/training_rooms/deadband_room.py` imports `fleet_simulator` and uses `FleetSimulator` class to generate training episodes.
- **Data Flow:** `DeadbandRoom` calls `simulator.generate_episode()` to produce `(state, action, reward, next_state)` tuples used as training tiles.
- **Integration Point:** `DeadbandRoom` config includes `simulator_config` dict passed to `FleetSimulator` constructor.

### **plato-torch ↔ plato-ensign**
- **Connection:** Direct export pipeline.
- **Evidence:** `plato-ensign/export.py` imports `torch` and `peft` (LoRA). `plato-torch/rooms/base_room.py` has `export_to_ensign()` method stub.
- **Data Flow:** Room accumulates interaction tiles → exports to `.safetensors` LoRA adapter via `plato-ensign` utilities.
- **Integration Point:** `BaseRoom.save_lora_adapter()` calls `plato_ensign.export.room_to_lora()`.

### **fleet-simulator ↔ holodeck-rust**
- **Connection:** None found in current code.
- **Evidence:** No imports between them. `fleet-simulator` is pure Python; `holodeck-rust` is Rust with C FFI bindings.
- **Gap:** These two simulators operate independently. No shared state or event passing.

### **holodeck-rust ↔ plato-torch**
- **Connection:** Potential via TCP socket, but not implemented.
- **Evidence:** `holodeck-rust/src/server.rs` has TCP listener (`0.0.0.0:8080`). `plato-torch` has no client code connecting to it.
- **Gap:** Could stream NPC interactions to `DeadbandRoom` as training tiles, but currently disconnected.

### **plato-ensign ↔ fleet-simulator**
- **Connection:** Indirect via `plato-torch`.
- **Evidence:** `plato-ensign` only interacts with PyTorch modules, not simulator directly.
- **Data Flow:** Ensigns are trained on tiles that may contain simulator-generated episodes, but no direct API.

---

## 2. Integration Gaps (What's Not Connected)

### **P0: Negative Space (What NOT to do)**
- Do NOT force integration where data domains mismatch (e.g., geometric snapping in `constraint-theory-core` with sentiment-aware NPCs in `holodeck-rust`).
- Do NOT modify core protocols without testing in `DeadbandRoom` first.
- Do NOT assume Rust-Python interop is trivial; use existing C FFI in `holodeck-rust/bindings/`.

### **P1: Safe Channels (Where You CAN Integrate)**
1. **holodeck-rust → plato-torch (Tile Stream)**
   - Safe because both handle sequential decision episodes.
   - Channel: Extend `holodeck-rust` TCP server to emit JSON events (`agent_action`, `npc_sentiment`, `reward`).
   - Channel: Add `HolodeckTileAdapter` in `plato-torch` that subscribes to socket, converts to training tiles.

2. **fleet-simulator → holodeck-rust (World State)**
   - Safe as read-only geometry.
   - Channel: Export `fleet-simulator` obstacle maps as `.obj` files.
   - Channel: Load into `holodeck-rust` as static collision geometry for NPC pathing.

3. **plato-ensign → holodeck-rust (NPC Brain Swap)**
   - Safe for testing.
   - Channel: Use `plato-ensign` to export a LoRA trained on `DeadbandRoom` tiles.
   - Channel: Load LoRA into `holodeck-rust` NPC decision model via `candle` (Rust ML).

### **P2: Optimization (Best Path)**
Priority integration for Phase 4:
1. **Implement `HolodeckTileAdapter` in `plato-torch`**  
   - Adds new tile source without breaking existing `DeadbandRoom`.
   - Creates fusion training: simulator episodes + NPC interactions.

2. **Add `export_geometry()` method to `FleetSimulator`**  
   - Simple OBJ export of 3D grid obstacles.
   - Lets `holodeck-rust` use same spatial constraints.

3. **Create `ensign_loader` example in `holodeck-rust/examples/`**  
   - Demonstrates loading a `.safetensors` LoRA into NPC model.
   - Validates the cross-stack integration.

---

## 3. Actionable Backlog Updates

### **Current Task**  
Map connections → **COMPLETE**. Integration gaps identified.

### **Updated Backlog**  
1. **Wire GhostInjector into holodeck** → *P1 channel: extend `holodeck-rust` TCP protocol to accept "ghost" tile injections.*  
2. **Connect DeadbandRoom to plato-relay** → *P1 channel: implement `PlatoRelayClient` in `plato-torch` that pushes tiles to relay.*  
3. **Test end-to-end pipeline** → *P2 optimization: after 1 & 2, run fusion training with simulator + holodeck tiles, export ensign, load into NPC.*  
4. **Document integration points** → *P2: create `INTEGRATION.md` in each repo with specific code snippets and test commands.*

---

**Tile ready for submission:** This map identifies 3 verified connections and 3 safe integration channels, following Deadband Protocol priorities.