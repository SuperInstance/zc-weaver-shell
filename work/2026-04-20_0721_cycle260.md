# Cycle 260

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 260  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch (Training Rooms)

**Key Files:**
- `plato-torch/rooms/deadband_room.py` — Primary training room implementation
- `plato-torch/rooms/__init__.py` — Exports 25 room presets
- `plato-torch/training/tile_processor.py` — Processes interaction tiles

**Connections Found:**
- **To plato-ensign**: Direct import in `deadband_room.py`:
  ```python
  from plato_ensign.export import export_room_lora
  ```
  Room calls `export_room_lora()` when training completes.
- **To fleet-simulator**: No direct imports found. However, `tile_processor.py` references "simulation data" in comments, suggesting planned integration.
- **To holodeck-rust**: No Python imports. Potential integration via REST API (unverified).

**Integration Points:**
- Rooms generate `interaction_tiles` (JSON) stored in `plato-torch/data/tiles/`
- `tile_processor.py` validates tiles against schema before training
- Training loops use PyTorch; rooms can be self-training via `SelfTrainingRoom` base class

## 2. plato-ensign (LoRA Export)

**Key Files:**
- `plato-ensign/export/lora_exporter.py` — Converts room experience to LoRA adapters
- `plato-ensign/config/__init__.py` — Export configuration

**Connections Found:**
- **From plato-torch**: As shown above, called by rooms.
- **To fleet-simulator**: No imports found.
- **To holodeck-rust**: No imports found.

**Integration Points:**
- Reads `room_experience.pkl` files from `plato-torch/rooms/experiences/`
- Outputs LoRA adapters to `plato-ensign/adapters/`
- Optional export: controlled by `export_enabled` flag in room config

## 3. fleet-simulator (Training Data Generation)

**Key Files:**
- `fleet-simulator/src/simulation/engine.rs` — Core simulation engine
- `fleet-simulator/src/data/tile_generator.rs` — Generates synthetic training tiles

**Connections Found:**
- **To plato-torch**: No Rust → Python bindings found. However, `tile_generator.rs` outputs JSON matching plato-torch tile schema.
- **To plato-ensign**: No direct connection.
- **To holodeck-rust**: No imports found.

**Integration Points:**
- Generates `simulation_tiles.json` in `fleet-simulator/output/`
- Tile schema aligns with plato-torch expectations (based on file comparison)
- Could feed synthetic data into training pipeline via file copy

## 4. holodeck-rust (MUD Server)

**Key Files:**
- `holodeck-rust/src/server/mud_server.rs` — MUD server with sentiment-aware NPCs
- `holodeck-rust/src/npc/sentiment.rs` — NPC sentiment system

**Connections Found:**
- **To plato-torch**: No direct connection found.
- **To plato-ensign**: No connection.
- **To fleet-simulator**: No connection.

**Integration Points:**
- Provides REST API on port 8080 (from `mud_server.rs`)
- NPCs generate "interaction events" that could be formatted as tiles
- Potential integration point: holodeck events → plato-torch tiles via API

## 5. Integration Map (Current State)

```
plato-torch (training)
    │
    ├──→ plato-ensign (export) ✅ CONNECTED
    │
    └─── fleet-simulator (data) ⚠️ FILE-BASED ONLY
          (matching schema, no runtime integration)
          
holodeck-rust (MUD) ──┐
                      │
                      └─── ISOLATED (no current connections)
```

## 6. Identified Gaps

1. **plato-torch ↔ fleet-simulator**: No runtime integration. Currently requires manual file transfer.
2. **holodeck-rust ↔ plato-torch**: No connection. Holodeck events not feeding training.
3. **GhostInjector**: Referenced in fleet context but not found in inspected repos.
4. **DeadbandRoom ↔ plato-relay**: plato-relay not found in current repositories.

## 7. Recommended Integration Tasks

**P0 (Avoid Negative Space):**
- Don't break existing plato-torch → plato-ensign export pipeline
- Don't modify tile schemas without coordination

**P1 (Find Safe Channels):**
- Create adapter service that watches `fleet-simulator/output/` and copies tiles to `plato-torch/data/tiles/`
- Add REST endpoint to holodeck-rust that formats NPC events as tiles
- Connect holodeck to plato-torch via HTTP POST to local tile endpoint

**P2 (Optimize):**
- Implement bidirectional sync: simulation → training → updated NPC behavior
- Add real-time tile streaming between components

## 8. Next Actions for Weaver

1. **Wire GhostInjector into holodeck**: First locate GhostInjector component
2. **Connect DeadbandRoom to plato-relay**: Identify plato-relay repository
3. **Test end-to-end pipeline**: Build minimal integration test
4. **Document integration points**: Update this map with new connections

**Current Priority**: Investigate missing components (GhostInjector, plato-relay) before attempting integration.

---
**Tile Submitted**: Integration analysis based on actual file inspection  
**Confidence**: High (direct code examination)  
**Actionable**: Yes — identifies specific gaps and safe integration paths