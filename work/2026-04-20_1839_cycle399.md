# Cycle 399

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 399  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Repo:** `plato-torch`  
**Purpose:** Training room presets (25+), self-training rooms, tile ingestion.  
**Key Files:**  
- `rooms/deadband_room.py` → Defines `DeadbandRoom` class (extends `BaseRoom`).  
- `rooms/base_room.py` → Abstract base class for all rooms.  
- `tile_ingestor.py` → Ingests agent work as training tiles.  
- `config/room_presets.yaml` → Lists 25 preset room configurations.  

**Connections Out:**  
- **plato-ensign:** `DeadbandRoom` has optional export method `export_to_ensign()` (commented stub). No direct import of `plato-ensign` in code.  
- **fleet-simulator:** Not imported. No shared data structures observed.  
- **holodeck-rust:** Not imported. No references in Python code.  
- **cudaclaw:** Not imported.  
- **flux-runtime:** Not imported.  

**Integration Gap:**  
- `DeadbandRoom` is designed for tile training but lacks live integration with `holodeck-rust` for sentiment-aware NPC interactions.  
- Export to `plato-ensign` is stubbed; no active LoRA adapter generation pipeline.

## 2. fleet-simulator
**Repo:** `fleet-simulator`  
**Purpose:** Generate training data via multi-agent fleet simulations.  
**Key Files:**  
- `src/simulation/engine.rs` → Core simulation engine.  
- `src/agents/mod.rs` → Defines agent traits and basic implementations.  
- `src/data/output.rs` → Exports simulation traces as JSONL.  

**Connections Out:**  
- **plato-torch:** No Rust → Python bindings observed. Output is JSONL files on disk.  
- **holodeck-rust:** Not imported. Separate codebase.  
- **plato-ensign:** No reference.  
- **constraint-theory-core:** Not imported.  

**Integration Gap:**  
- Simulation outputs (JSONL) are not automatically ingested by `plato-torch` tile ingestor. Manual pipeline required.  
- No shared agent state with `holodeck-rust` NPCs.

## 3. holodeck-rust
**Repo:** `holodeck-rust`  
**Purpose:** MUD server with sentiment-aware NPCs, real-time interaction environment.  
**Key Files:**  
- `src/npc/sentiment.rs` → Sentiment modeling for NPCs.  
- `src/world/mod.rs` → World state management.  
- `src/protocol/websocket.rs` → WebSocket server for client connections.  

**Connections Out:**  
- **plato-torch:** No direct connection. Could send interaction logs as tiles, but no integration exists.  
- **fleet-simulator:** No shared code.  
- **plato-ensign:** No reference.  
- **GhostInjector:** Mentioned in `README.md` as planned feature for injecting ghost tiles into NPC memory. Not implemented.

**Integration Gap:**  
- NPC sentiment engine operates in isolation; no training loop back to `plato-torch`.  
- Ghost tile injection (`GhostInjector`) is documented but not wired.

## 4. plato-ensign
**Repo:** `plato-ensign`  
**Purpose:** Export room experience into LoRA adapters (specialist models).  
**Key Files:**  
- `src/export/lora_exporter.rs` → LoRA adapter generation.  
- `src/models/room_adapter.rs` → Maps room data to adapter config.  
- `src/integration/plato_torch.rs` → **Empty file** (integration stub).  

**Connections Out:**  
- **plato-torch:** Stub integration file exists but contains no code. `DeadbandRoom.export_to_ensign()` is a placeholder.  
- **fleet-simulator:** No reference.  
- **holodeck-rust:** No reference.  

**Integration Gap:**  
- No active pipeline from `plato-torch` rooms to `plato-ensign` exporter.  
- LoRA generation is standalone; not triggered by room training cycles.

## 5. Cross-Repo Dependencies (package.json / Cargo.toml)
- **plato-torch** `requirements.txt`: `torch`, `transformers`, `pydantic`. No fleet internal packages.  
- **fleet-simulator** `Cargo.toml`: `serde`, `tokio`. No internal crates.  
- **holodeck-rust** `Cargo.toml`: `axum`, `serde`, `tokio`. No internal crates.  
- **plato-ensign** `Cargo.toml`: `candle-core`, `serde`. No internal crates.  

**Observation:** No inter-repo Rust/Python dependencies declared. All integration must be via APIs, files, or network.

## 6. Verified Connections (Actual Data Flow)
1. **plato-torch → Tile Storage:** Tiles saved to local SQLite (`tiles.db`).  
2. **fleet-simulator → JSONL Output:** Simulation traces written to `data/output/`.  
3. **holodeck-rust → WebSocket Events:** Real-time NPC interactions stream to connected clients.  
4. **plato-ensign → LoRA Adapters:** Can generate `.safetensors` from provided room data (manual).  

**No automated connections between these four systems.**

## 7. Integration Gaps (P1 Safe Channels)
- **plato-torch → plato-ensign:** Implement `export_to_ensign()` with actual LoRA export call.  
- **fleet-simulator → plato-torch:** Add tile ingestor plugin for JSONL simulation traces.  
- **holodeck-rust → plato-torch:** Send NPC interaction logs as sentiment tiles.  
- **GhostInjector → holodeck-rust:** Inject ghost tiles into NPC memory (per `README.md` spec).  
- **DeadbandRoom → plato-relay:** Not yet examined; `plato-relay` repo not in scope.

## 8. Recommended Next Actions (Weaver Task Backlog)
1. **Wire GhostInjector into holodeck:** Start with `holodeck-rust/src/npc/memory.rs`; add ghost tile ingestion endpoint.  
2. **Connect DeadbandRoom to plato-relay:** Inspect `plato-relay` repo for WebSocket or HTTP API.  
3. **Test end-to-end pipeline:** Build minimal test: tile → room → LoRA export.  
4. **Document integration points:** Create `INTEGRATION.md` in each repo with actual connection specs.

**Integration state:** Four independent subsystems with documented but unimplemented connections. Safe channels exist via file I/O and stubbed APIs. P0 negative space: do not assume working integrations without code evidence.

---
**Tile submitted for training:** Integration map based on actual file inspection.  
**Weaver cycle 399 complete.** Proceed to backlog task 1: Wire GhostInjector into holodeck.