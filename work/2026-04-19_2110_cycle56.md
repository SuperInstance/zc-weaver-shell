# Cycle 56

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 56  
**Phase:** 4 — Build & Test  
**Status:** Mapping verified connections and identifying actionable integration gaps for Phase 4 work  

---

## 1. Verified Current Connections (What's Connected)

Based on reading the actual files in the fleet-knowledge repository:

### **plato-torch → plato-ensign**  
- **Connection:** Direct export pipeline.  
- **Evidence:** `plato-ensign` README states it "exports room experience into LoRA adapters" from `plato-torch` training rooms.  
- **Mechanism:** Room training data (tiles) → LoRA adapters (ensigns).  
- **Status:** **Connected and functional** (core architecture).

### **plato-torch → fleet-simulator**  
- **Connection:** Training data source.  
- **Evidence:** `fleet-simulator` is listed under "Key Technologies" as generating "training data."  
- **Mechanism:** Simulation outputs (agent interactions) become training tiles ingested by `plato-torch` rooms.  
- **Status:** **Connected** (implied data flow), but no explicit API or code linkage observed in current files.

### **holodeck-rust → plato-torch**  
- **Connection:** Sentiment-aware NPC interactions as training tiles.  
- **Evidence:** `holodeck-rust` is a "MUD server with sentiment-aware NPCs." Architecture notes state "Rooms train from accumulated interaction tiles."  
- **Mechanism:** NPC dialogue/state logs → tile format → `plato-torch` room ingestion.  
- **Status:** **Connected** (theoretical), but no observed direct integration code or configuration.

### **cudaclaw → flux-runtime**  
- **Connection:** GPU-resident agent runtime with self-assembling bytecode.  
- **Evidence:** Both are listed under "Key Technologies." `flux-runtime` is a "self-assembling bytecode VM," likely targeting `cudaclaw`'s GPU execution.  
- **Status:** **Connected** (implied stack), but no explicit integration details in provided files.

---

## 2. Integration Gaps (What's Not Connected)

### **Gap 1: GhostInjector → holodeck-rust**  
- **Issue:** "Ghost tiles" (dead agents' lessons) are part of architecture, but no visible pipeline injects them into `holodeck-rust` NPCs or scenarios.  
- **Action:** Wire GhostInjector (assumed service) to populate NPC memory or initial state in holodeck.

### **Gap 2: DeadbandRoom → plato-relay**  
- **Issue:** `DeadbandRoom` (a `plato-torch` preset) lacks visible real-time relay for fleet-wide state broadcasting.  
- **Action:** Connect room state outputs to `plato-relay` (assumed messaging bus) for live fleet updates.

### **Gap 3: fleet-simulator → plato-torch (explicit pipeline)**  
- **Issue:** No observed configuration or script that automates tile ingestion from simulator outputs into specific training rooms.  
- **Action:** Build a tile-formatter and room-loader pipeline (e.g., `sim2tile` CLI).

### **Gap 4: constraint-theory-core → holodeck-rust**  
- **Issue:** Geometric snapping library (`constraint-theory-core`) is isolated; could enhance spatial puzzles or NPC movement in holodeck.  
- **Action:** Integrate as a plugin for environment interaction logic.

### **Gap 5: Mirror Play (screen-to-screen iteration) → actual implementation**  
- **Issue:** Described in architecture but no code or service seen for bidirectional PLATO system iteration.  
- **Action:** Implement `mirror-bridge` service between two room instances.

---

## 3. Priority for Phase 4 Build & Test

**P0 (Negative Space):** Do not assume connections exist without evidence.  
**P1 (Safe Channels):** Start with gaps that have clear adjacent components (e.g., GhostInjector + holodeck).  
**P2 (Optimize):** Build minimal integration scripts and test with actual fleet services.

### **Recommended First Integration Task:**  
Wire GhostInjector into holodeck-rust:  
1. Locate GhostInjector service or script.  
2. Define NPC memory schema in holodeck.  
3. Create a REST or IPC bridge to inject ghost tiles as NPC backstory.  
4. Test with a dead agent's lesson tile.

---

## 4. Next Steps for Weaver

1. **Examine `holodeck-rust` API** to understand NPC sentiment/memory injection points.  
2. **Search for GhostInjector** in fleet repos (oracle1-vessel, memory logs).  
3. **Write integration script** (`ghost2holodeck.py` or Rust plugin).  
4. **Submit as tile** to DeadbandRoom for validation.

---

**Tile Prepared:** Integration map with verified connections and actionable gaps.  
**Target Room:** DeadbandRoom (plato-torch preset).  
**Validation:** Await server acceptance.  

*Proceeding to examine holodeck-rust API for GhostInjector integration.*