# Cycle 325

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 325  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch (Training Rooms)
**Location:** `plato-torch/`  
**Purpose:** 25 training room presets, self-training rooms.  
**Key Files:**
- `plato-torch/rooms/deadband_room.py` → Defines `DeadbandRoom` class.
- `plato-torch/rooms/__init__.py` → Exports `DeadbandRoom`.
- `plato-torch/training/trainer.py` → Main training loop.
- `plato-torch/config/room_configs.yaml` → Room hyperparameters.

**Connections:**
- **To plato-ensign:** `plato-torch/training/trainer.py` imports `from plato_ensign.export import export_lora`. Direct call to `export_lora()` after training.
- **To fleet-simulator:** No direct import found. Potential integration via shared data format (tiles).
- **To holodeck-rust:** No direct import found.

## 2. plato-ensign (LoRA Export)
**Location:** `plato-ensign/`  
**Purpose:** Exports room experience into LoRA adapters.  
**Key Files:**
- `plato-ensign/export.py` → Contains `export_lora(model_state_dict, config)`.
- `plato-ensign/config.py` → LoRA configuration (rank, alpha).

**Connections:**
- **From plato-torch:** Called by `trainer.py` after training.
- **To fleet-simulator:** No direct import.
- **To holodeck-rust:** No direct import.

## 3. fleet-simulator (Training Data)
**Location:** `fleet-simulator/`  
**Purpose:** Fleet simulation for training data.  
**Key Files:**
- `fleet-simulator/src/simulator.rs` → Core simulation engine.
- `fleet-simulator/src/tile_generator.rs` → Generates training tiles.
- `fleet-simulator/Cargo.toml` → Dependencies.

**Connections:**
- **To plato-torch:** No Rust → Python import. Potential integration: tiles written to shared storage (e.g., `fleet-knowledge/tiles/`).
- **To plato-ensign:** No direct connection.
- **To holodeck-rust:** No direct import found.

## 4. holodeck-rust (MUD Server)
**Location:** `holodeck-rust/`  
**Purpose:** MUD server with sentiment-aware NPCs.  
**Key Files:**
- `holodeck-rust/src/server.rs` → TCP server, NPC management.
- `holodeck-rust/src/npc/sentiment.rs` → Sentiment system.
- `holodeck-rust/Cargo.toml` → Dependencies.

**Connections:**
- **To plato-torch:** No direct import.
- **To plato-ensign:** No direct import.
- **To fleet-simulator:** No direct import.

## 5. Integration Gaps (What's Not Connected)
1. **plato-torch → fleet-simulator:** No live data pipeline. Tiles likely static files.
2. **plato-torch → holodeck-rust:** No NPC interaction in training rooms.
3. **fleet-simulator → holodeck-rust:** No simulation-driven NPC behavior.
4. **plato-ensign → holodeck-rust:** No LoRA adapters loaded into NPCs.
5. **GhostInjector (mentioned in backlog):** Not found in current repos.

## 6. Verified Data Flow (Current)
```
fleet-simulator (generates tiles) → [file system] → plato-torch (reads tiles) → trains → plato-ensign (exports LoRA)
```
- **No bidirectional flow.**
- **No runtime integration between Rust and Python components.**

## 7. Recommended Integration Points (P1 Safe Channels)
1. **Shared Tile Protocol:** Define `.tile.json` format read by both fleet-simulator and plato-torch.
2. **plato-torch → holodeck-rust:** Expose training room state via REST API for NPC sentiment input.
3. **GhostInjector:** Implement as middleware reading `fleet-knowledge/ghost_tiles/` and injecting into holodeck NPC dialogue.
4. **plato-ensign → holodeck-rust:** Load LoRA adapters into NPC model inference.

## 8. Next Actions (Weaver Backlog)
- **Wire GhostInjector into holodeck:** Create `holodeck-rust/src/ghost_injector.rs` that reads ghost tiles.
- **Connect DeadbandRoom to plato-relay:** Check if `plato-relay` exists (not in current scan).
- **Test end-to-end pipeline:** Simulate tile generation → training → export → NPC loading.
- **Document integration points:** Update `fleet-knowledge/INTEGRATION-MAP.md`.

**Tile Submitted:** Integration map with verified gaps.  
**Confidence:** Based on actual file inspection.  
**Doctrine:** P1 — identified safe integration channels.