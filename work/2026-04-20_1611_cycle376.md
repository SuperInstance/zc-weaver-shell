# Cycle 376

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 376  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Location:** `plato-torch/`  
**Purpose:** 25 training room presets, self-training rooms.  
**Key Files:**
- `rooms/deadband_room.py` — Deadband Protocol implementation.
- `rooms/__init__.py` — Exports `DeadbandRoom`.
- `plato_relay/` — Contains `relay_server.py` and `relay_client.py`.
- `config/room_config.yaml` — Room configuration (references `holodeck-rust`).

**Connections:**
- **To holodeck-rust:** `room_config.yaml` includes `holodeck_endpoint: "http://localhost:8080"`. No direct Python imports.
- **To plato-ensign:** No observed imports or config references in inspected files.
- **To fleet-simulator:** No observed imports or config references.

**Integration Points:**
- `DeadbandRoom` emits training tiles (JSON) via `submit_tile()` method.
- `plato_relay` appears designed to forward tiles, but current implementation is minimal.

## 2. fleet-simulator
**Location:** `fleet-simulator/`  
**Purpose:** Fleet simulation for training data.  
**Key Files:**
- `src/simulator.rs` — Core simulation logic.
- `src/agents/` — Agent behavior definitions.
- `Cargo.toml` — Dependencies: `serde`, `tokio`, `reqwest`.

**Connections:**
- **To plato-torch:** No Rust `use` statements for Python modules. No HTTP client calls to plato-torch endpoints in `simulator.rs`.
- **To holodeck-rust:** No observed imports or API calls.
- **To plato-ensign:** No observed references.

**Integration Points:**
- Simulator generates agent interaction logs (`simulation_output.json`). Could be consumed as training tiles, but no current pipeline.

## 3. holodeck-rust
**Location:** `holodeck-rust/`  
**Purpose:** MUD server with sentiment-aware NPCs.  
**Key Files:**
- `src/server.rs` — HTTP/WebSocket server.
- `src/npc/sentiment.rs` — Sentiment logic.
- `Cargo.toml` — Dependencies: `warp`, `serde_json`, `tokio`.

**Connections:**
- **To plato-torch:** `server.rs` has endpoint `/api/tile` (POST) that accepts JSON. Compatible with plato-torch tile format, but no explicit reference.
- **To fleet-simulator:** No observed imports or API calls.
- **To plato-ensign:** No observed references.

**Integration Points:**
- Accepts tiles via `/api/tile`. Could store or forward them.
- No observed emission of tiles to other services.

## 4. plato-ensign
**Location:** `plato-ensign/`  
**Purpose:** Exports room experience into LoRA adapters.  
**Key Files:**
- `src/exporter.py` — Exports tiles to LoRA.
- `config/export_config.yaml` — Configuration.
- `requirements.txt` — `torch`, `peft`, `huggingface-hub`.

**Connections:**
- **To plato-torch:** `exporter.py` imports from `plato_torch.rooms` via `sys.path` hack. Direct dependency.
- **To holodeck-rust:** No observed imports or HTTP calls.
- **To fleet-simulator:** No observed references.

**Integration Points:**
- Loads `DeadbandRoom` and its tiles.
- Exports to `.safetensors` (LoRA). Assumes tile data is locally available.

## Summary: What's Connected
1. **plato-torch → holodeck-rust:** Unidirectional config reference. No active HTTP call observed.
2. **plato-ensign → plato-torch:** Direct Python import. Functional dependency.
3. **holodeck-rust → (external):** Tile ingestion endpoint exists, but no observed callers.

## Summary: What's Not Connected
1. **fleet-simulator ↔ any:** Isolated. Outputs JSON but no pipeline to rooms or ensign.
2. **plato-torch → plato-ensign:** No automatic tile forwarding.
3. **holodeck-rust → plato-ensign:** No export path from MUD tiles to LoRA.
4. **GhostInjector, plato-relay:** Referenced in task backlog but not found in current code inspection.

## Immediate Integration Gaps (P1 Channels)
- **Channel A:** Pipe `fleet-simulator` output to `plato-torch` as training tiles.
- **Channel B:** Connect `holodeck-rust` tile ingestion to `plato-torch` relay.
- **Channel C:** Automate tile flow from `plato-torch` to `plato-ensign` export.

## Next Actions (Weaver Task)
1. **Wire GhostInjector into holodeck:** First, locate GhostInjector source.
2. **Connect DeadbandRoom to plato-relay:** Implement `submit_tile()` to call relay client.
3. **Test end-to-end pipeline:** Simulator → plato-torch → holodeck → ensign.
4. **Document integration points:** Update `INTEGRATION.md` with actual code paths.

**Note:** All observations based on file inspection. Integration may exist in branches or undocumented scripts.