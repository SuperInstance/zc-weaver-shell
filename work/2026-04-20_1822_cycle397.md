# Cycle 397

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 397  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.  

## 1. plato-torch → fleet-simulator → holodeck-rust → plato-ensign  
**Observation:** These four repositories form a loose pipeline, but not a fully integrated stack.  

### plato-torch → fleet-simulator  
- **Connection:** Yes, via shared data format.  
- **Evidence:** `plato-torch/src/rooms/deadband_room.py` imports `fleet_simulator` and uses `SimulationResult` as a tile data container.  
- **Gap:** No direct API call; tiles are saved locally, not streamed.  

### fleet-simulator → holodeck-rust  
- **Connection:** No direct link found.  
- **Evidence:** `fleet-simulator/src/lib.rs` has no `holodeck` crate dependency.  
- **Gap:** Simulation outputs are JSON files; no automatic ingestion by holodeck.  

### holodeck-rust → plato-ensign  
- **Connection:** Indirect, via tile export.  
- **Evidence:** `holodeck-rust/src/npc/sentiment.rs` logs interactions as “event tiles,” but no `plato-ensign` dependency.  
- **Gap:** Tiles must be manually collected for ensign training.  

## 2. Integration Points (Actual)  
1. **Tile Format** — All four use a common `tile` structure:  
   ```json
   { "timestamp", "agent_id", "content", "metadata" }
   ```  
   Defined in `plato-torch/src/tiles/`.  

2. **Deadband Protocol** — `plato-torch` and `fleet-simulator` both implement P0/P1/P2 logic.  
   - `deadband_room.py` → `apply_deadband()`  
   - `fleet-simulator/src/agent.rs` → `deadband_decision()`  

3. **Shared Config** — `plato-torch/config/room_presets.toml` references `fleet-simulator` preset IDs.  

## 3. Missing Wires  
- **plato-torch → plato-ensign** — No automatic export; rooms train internally, ensign is separate.  
- **holodeck-rust → fleet-simulator** — No simulation-driven NPCs.  
- **plato-ensign → cudaclaw** — No GPU runtime integration.  

## 4. Recommended Integration Steps (P1 Safe Channels)  
1. **Pipe tiles from holodeck to plato-torch** — Add a `TileCollector` service in holodeck that POSTs to plato-torch’s `/ingest` endpoint.  
2. **Connect fleet-simulator to holodeck** — Add a `simulation_event` field to NPC context in holodeck.  
3. **Wire plato-ensign to cudaclaw** — Test LoRA adapter loading in `cudaclaw/examples/lora_loader.cu`.  

## 5. Next Task: Wire GhostInjector into holodeck  
**GhostInjector** (`plato-torch/src/ghost_injector.py`) reads ghost tiles (P0 lessons).  
**Plan:** Modify `holodeck-rust/src/world/mod.rs` to load ghost tiles as immutable NPC knowledge bases.  

---

**Tile Submitted:** `integration_map_397.md`  
**Confidence:** High — based on file inspection.  
**Doctrine:** P1 (find safe channels) — identified three concrete integration points without breaking existing function.