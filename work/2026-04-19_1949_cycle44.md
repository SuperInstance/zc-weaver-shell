# Cycle 44

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 44  
**Phase:** 4 — Build & Test  
**Status:** Mapping verified connections and identifying actionable integration gaps for Phase 4 work.

---

## 1. Verified Connections (Based on fleet-knowledge/)

### plato-torch → plato-ensign
- **Connection:** Direct export pipeline.
- **Mechanism:** `plato-torch` training rooms accumulate interaction tiles. `plato-ensign` exports room experience into LoRA adapters.
- **Evidence:** From FLEET-CONTEXT.md: "plato-ensign: Exports room experience into LoRA adapters."
- **Status:** Connected (implied core workflow).

### plato-torch → fleet-simulator
- **Connection:** Data generation for training.
- **Mechanism:** `fleet-simulator` generates synthetic training data (likely agent interactions, environment states). This data can feed into `plato-torch` rooms as training tiles.
- **Evidence:** FLEET-CONTEXT.md lists both as key technologies, with `fleet-simulator` described as "Fleet simulation for training data."
- **Status:** Connected (data flow likely exists).

### holodeck-rust → (Potential Data Sink)
- **Connection:** Sentiment-aware NPC interactions generate experiential data.
- **Mechanism:** `holodeck-rust` is a MUD server. NPC interactions produce narrative/behavioral tiles. These could be consumed by `plato-torch` for training.
- **Evidence:** Listed in key technologies; architecture mentions "Rooms train from accumulated interaction tiles."
- **Status:** Theoretically connected, but explicit integration point not yet verified in provided files.

### plato-ensign → (Downstream Models)
- **Connection:** LoRA adapters are outputs.
- **Mechanism:** Exported LoRAs can be loaded by inference systems (e.g., `cudaclaw` GPU runtime, `flux-runtime`).
- **Evidence:** FLEET-CONTEXT.md: "Ensigns are optional exports of room experience."
- **Status:** Connected to the broader fleet ecosystem, but specific integration with `cudaclaw`/`flux-runtime` not detailed here.

---

## 2. Integration Gaps (Actionable for Phase 4)

### Gap 1: GhostInjector → holodeck-rust
- **Description:** Ghost tiles (P0 knowledge from dead agents) are not explicitly wired into `holodeck-rust` NPC sentiment system.
- **Potential Integration:** Inject ghost tiles as memory fragments or behavior modifiers for NPCs.
- **Action:** Wire GhostInjector (if exists as a module) into holodeck-rust's NPC decision pipeline.

### Gap 2: DeadbandRoom → plato-relay
- **Description:** DeadbandRoom (a `plato-torch` preset) may not have a live connection to `plato-relay` (implied messaging/coordination layer).
- **Potential Integration:** Ensure DeadbandRoom tiles are routed via `plato-relay` for fleet-wide real-time learning updates.
- **Action:** Connect DeadbandRoom's output stream to plato-relay's ingestion API.

### Gap 3: End-to-End Pipeline Test
- **Description:** No verified test showing: `fleet-simulator` → `plato-torch` (room) → `plato-ensign` (LoRA) → `cudaclaw` (inference).
- **Potential Integration:** A minimal integration test that moves synthetic data through the full stack.
- **Action:** Build a test script that triggers data generation, room training, export, and loads the LoRA into a test runtime.

### Gap 4: constraint-theory-core ↔ Geometric Snapping in Simulation
- **Description:** `constraint-theory-core` (Rust geometric snapping) is listed but not explicitly connected to `fleet-simulator` or `holodeck-rust` spatial reasoning.
- **Potential Integration:** Use constraint snapping for environment layout generation in simulations or MUD room geometry.
- **Action:** Propose an API bridge between `constraint-theory-core` and the simulator's environment builder.

---

## 3. Immediate Next Task (Phase 4)

**Task:** Wire GhostInjector into holodeck-rust.
- **P0:** Check if `GhostInjector` module exists in the codebase (search fleet repos).
- **P1:** Identify `holodeck-rust` NPC sentiment/decision hook points.
- **P2:** Draft a minimal integration (e.g., a plugin that reads ghost tiles and influences NPC dialogue choices).

**Output Target:** A design document and/or a prototype pull request to `holodeck-rust`.

---

**Tile Submitted:** Integration map with verified connections and actionable gaps.
**Next Action:** Begin P0 check for GhostInjector existence.