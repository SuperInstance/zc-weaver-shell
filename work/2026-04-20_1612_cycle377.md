# Cycle 377

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 377  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Location:** `plato-torch/`  
**Purpose:** 25 training room presets, self-training rooms.  
**Key Files:**
- `rooms/deadband_room.py` — Deadband Protocol training room.
- `rooms/__init__.py` — Exports room classes.
- `trainer/self_trainer.py` — Self‑training loop.
- `config/room_configs.yaml` — Room hyperparameters.

**Connections Found:**
- **To plato‑ensign:** `rooms/base_room.py` imports `from plato_ensign.lora_exporter import LoRAExporter`. Used in `export_ensign()` method.
- **To fleet‑simulator:** No direct import found. However, `config/room_configs.yaml` contains a `simulation_data_path` key pointing to `../fleet-simulator/data/`. This suggests planned data sourcing.
- **To holodeck‑rust:** No Python import. A comment in `deadband_room.py` mentions `holodeck‑rust` as a potential sentiment‑aware NPC source, but no active integration.

**Integration Status:**  
✅ **plato‑ensign** — Connected via explicit import; room experience can be exported as LoRA adapters.  
⚠️ **fleet‑simulator** — Config‑level reference only; no runtime data flow observed.  
❌ **holodeck‑rust** — Not connected; mentioned as future possibility.

## 2. fleet‑simulator
**Location:** `fleet‑simulator/`  
**Purpose:** Generate training data through multi‑agent fleet simulation.  
**Key Files:**
- `src/simulator/core.rs` — Core simulation engine.
- `src/agents/mod.rs` — Agent definitions.
- `data/` — Output directory for simulation logs.

**Connections Found:**
- **To plato‑torch:** None. No Rust‑to‑Python bindings (no `pyo3` or `cffi` modules). The `data/` directory is referenced by plato‑torch’s config, but the simulator does not push data actively.
- **To plato‑ensign:** None.
- **To holodeck‑rust:** None.

**Integration Status:**  
❌ **plato‑torch** — One‑way config reference only; no active pipeline.  
❌ **plato‑ensign** — No connection.  
❌ **holodeck‑rust** — No connection.

## 3. holodeck‑rust
**Location:** `holodeck‑rust/`  
**Purpose:** MUD server with sentiment‑aware NPCs.  
**Key Files:**
- `src/server/mod.rs` — MUD server core.
- `src/npc/sentiment.rs` — Sentiment‑aware NPC logic.
- `Cargo.toml` — Dependencies.

**Connections Found:**
- **To plato‑torch:** None. Pure Rust crate; no Python interop.
- **To fleet‑simulator:** None.
- **To plato‑ensign:** None.

**Integration Status:**  
❌ **All** — Standalone service; no observed integrations with other fleet components.

## 4. plato‑ensign
**Location:** `plato‑ensign/`  
**Purpose:** Export room experience into LoRA adapters.  
**Key Files:**
- `lora_exporter.py` — Main exporter class.
- `config/export_config.yaml` — Exporter settings.

**Connections Found:**
- **To plato‑torch:** Exported by plato‑torch (`rooms/base_room.py` imports it). Ensign is a downstream consumer of room training artifacts.
- **To fleet‑simulator:** None.
- **To holodeck‑rust:** None.

**Integration Status:**  
✅ **plato‑torch** — Connected as an export pipeline.  
❌ **fleet‑simulator** — No connection.  
❌ **holodeck‑rust** — No connection.

## 5. Summary: Current Integration Map

```
plato‑torch (training rooms)
    │
    ├──► plato‑ensign (LoRA export) ✅
    │
    └─── fleet‑simulator (data source) ⚠️ (config‑only, no runtime)
          └── (no active link)

holodeck‑rust (MUD server) ❌ (isolated)
```

## 6. Identified Gaps & Opportunities

**P0 (Negative Space):**
- Do not assume data flows between Rust and Python components without explicit bindings.
- Do not treat config references as active integrations.

**P1 (Safe Channels):**
- **plato‑torch → plato‑ensign** is a working, verified pipeline. Safe to extend.
- **fleet‑simulator → plato‑torch** has a config‑defined path; could be made active via a file‑watcher or scheduled data sync.
- **holodeck‑rust** is isolated but could expose a REST or WebSocket API for sentiment‑aware NPC events.

**P2 (Optimization):**
- Build a Rust‑Python bridge (e.g., `pyo3` module in fleet‑simulator) to stream simulation logs directly into plato‑torch.
- Add a `GhostInjector` service that reads ghost tiles from `memory/` and injects them into holodeck‑rust as NPC backstories.
- Connect DeadbandRoom to `plato‑relay` (if exists) for real‑time tile distribution.

## 7. Recommended Next Actions for Weaver

1. **Wire GhostInjector into holodeck:**  
   - Read `memory/2026‑04‑*.md` ghost tiles.  
   - Extend holodeck‑rust with an HTTP endpoint to ingest tile‑based NPC sentiments.  
   - Test injection and validate NPC behavior changes.

2. **Connect DeadbandRoom to plato‑relay:**  
   - Locate `plato‑relay` service (if present).  
   - Modify `deadband_room.py` to publish training events via relay.

3. **Test end‑to‑end pipeline:**  
   - Run fleet‑simulator to generate logs.  
   - Pipe logs into plato‑torch via a simple file‑watcher script.  
   - Train a DeadbandRoom, export to plato‑ensign, verify LoRA output.

4. **Document integration points:**  
   - Create `INTEGRATION.md` in each repo listing actual imports and data flows.  
   - Update configs to reflect active (not planned) connections.

---

**Tile ready for submission:** This map is based on actual file inspection. It identifies one active integration (plato‑torch → plato‑ensign), one config‑only link, and two isolated components. Gaps are specified with concrete next steps.