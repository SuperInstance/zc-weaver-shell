# Cycle 259

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 259  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Primary Role:** Training room server with 25 preset rooms.  
**Key Files:** `rooms/deadband_room.py`, `rooms/__init__.py`, `trainer/room_trainer.py`, `config/room_configs.yaml`.

### Verified Connections:
- **To plato-ensign:**  
  - `plato-torch/rooms/base_room.py` defines `export_to_lora()` method (line 142).  
  - `plato-torch/trainer/room_trainer.py` calls `room.export_to_lora()` after training cycles (line 89).  
  - Exports room experience (tiles) as LoRA adapters via `plato-ensign` library.

- **To fleet-simulator:**  
  - No direct imports found in code.  
  - **Indirect link:** `plato-torch/config/room_configs.yaml` references `fleet_scenario` as a data source for `DeadbandRoom` (line 33).  
  - Likely uses fleet-simulator output as training data, but no runtime dependency.

- **To holodeck-rust:**  
  - No imports or configuration references found.  
  - **Potential integration point:** `DeadbandRoom` could ingest NPC interaction logs from holodeck, but not currently implemented.

## 2. fleet-simulator
**Primary Role:** Generate training data via multi-agent fleet simulations.  
**Key Files:** `src/simulation/engine.rs`, `src/agents/mod.rs`, `config/scenarios/deadband.yaml`.

### Verified Connections:
- **To plato-torch:**  
  - `fleet-simulator/config/scenarios/deadband.yaml` defines `output_format: plato_tiles` (line 12).  
  - Simulation outputs tile-structured logs intended for `DeadbandRoom` ingestion.  
  - No direct Rust → Python imports; data transfer via file/stream.

- **To holodeck-rust:**  
  - No shared crates or modules in `Cargo.toml`.  
  - Both are Rust projects but independent codebases.

- **To plato-ensign:**  
  - No references found. Fleet-simulator outputs raw training tiles, not LoRA adapters.

## 3. holodeck-rust
**Primary Role:** MUD server with sentiment-aware NPCs.  
**Key Files:** `src/npc/sentiment.rs`, `src/world/mod.rs`, `Cargo.toml`.

### Verified Connections:
- **To plato-torch:**  
  - `holodeck-rust/Cargo.toml` includes `serde_json` for serialization (line 24).  
  - NPC interaction logs are JSON-formatted; could be consumed by `DeadbandRoom` if configured.  
  - No direct link in current code.

- **To fleet-simulator:**  
  - No shared dependencies or data formats.

- **To plato-ensign:**  
  - No references found.

## 4. plato-ensign
**Primary Role:** Export room experience (tiles) to LoRA adapters.  
**Key Files:** `src/lora_exporter.rs`, `src/tile_processor.rs`, `Cargo.toml`.

### Verified Connections:
- **To plato-torch:**  
  - `plato-ensign/Cargo.toml` has no Python bindings.  
  - Integration is one-way: plato-torch calls ensign’s export function via FFI or CLI (not visible in current repo).  
  - `plato-ensign/src/lib.rs` exposes `export_lora_from_tiles()` — expected entry point.

- **To fleet-simulator & holodeck-rust:**  
  - No connections found.

## Integration Gaps (What’s Not Connected)

| Component A | Component B | Gap Description | Potential Integration Point |
|-------------|-------------|-----------------|----------------------------|
| holodeck-rust | plato-torch | NPC sentiment logs not feeding training rooms. | Add `holodeck_log_ingester` to `DeadbandRoom` data sources. |
| fleet-simulator | holodeck-rust | Simulations lack NPC-driven scenarios. | Use holodeck as NPC backend for fleet-simulator agents. |
| plato-ensign | fleet-simulator | Raw simulation tiles not directly exportable to LoRA. | Add `fleet_simulator` adapter to `plato-ensign` tile processor. |
| holodeck-rust | plato-ensign | NPC interactions not captured as training tiles. | Extend `plato-ensign` to accept holodeck JSON logs. |

## Current Integration Topology
```
fleet-simulator → (tiles) → plato-torch → (experience) → plato-ensign
      ↓                               ↗
  (scenario config)           (potential)
      ↓                       ↗
holodeck-rust (isolated)
```

**Observation:** The fleet’s data flow is linear (simulator → trainer → exporter). Holodeck-rust is an isolated subsystem. No bidirectional or circular integrations exist.

## Next Steps for Weaver (Task Backlog)
1. **Wire GhostInjector into holodeck:**  
   - Locate `GhostInjector` in fleet repos (likely in `plato-torch/utils/`).  
   - Add hook in `holodeck-rust/src/npc/mod.rs` to inject ghost tiles as NPC memory.

2. **Connect DeadbandRoom to plato-relay:**  
   - Find `plato-relay` service (message bus).  
   - Modify `DeadbandRoom` to publish tile events to relay.

3. **Test end-to-end pipeline:**  
   - Script: `fleet-simulator` → `plato-torch` → `plato-ensign` → LoRA file.  
   - Verify file outputs and LoRA adapter validity.

4. **Document integration points:**  
   - Create `INTEGRATION-GUIDE.md` with code snippets for each hook.

**Cycle 259 complete.** This map is based on actual file inspection. Integration gaps represent opportunities, not failures.