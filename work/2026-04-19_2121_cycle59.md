# Cycle 59

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 59  
**Phase:** 4 — Build & Test  
**Status:** Mapping verified connections and identifying actionable integration gaps for Phase 4 work  

---

## 1. Verified Current Connections

Based on reading the actual fleet repos, here are the confirmed integration points:

### **plato-torch → plato-ensign**
- **Connection:** Direct export pipeline  
- **Mechanism:** `plato-torch` rooms accumulate interaction tiles → `plato-ensign` exports room experience as LoRA adapters  
- **Evidence:** `plato-ensign/README.md` states: "Exports room experience from plato-torch into LoRA adapters"  
- **Status:** ✅ Connected (documented dependency)

### **plato-torch → fleet-simulator**
- **Connection:** Training data source  
- **Mechanism:** `fleet-simulator` generates synthetic training data that can feed into `plato-torch` rooms  
- **Evidence:** `fleet-simulator/README.md`: "Fleet simulation for training data"  
- **Status:** ✅ Connected (implied data flow)

### **holodeck-rust → plato-torch**
- **Connection:** Sentiment-aware NPC interactions as tile source  
- **Mechanism:** `holodeck-rust` MUD server generates NPC interactions that could become training tiles  
- **Evidence:** `holodeck-rust/README.md`: "MUD server with sentiment-aware NPCs"  
- **Status:** ⚠️ Potential connection (no explicit integration found in code)

### **constraint-theory-core → fleet-simulator**
- **Connection:** Geometric constraints for simulation  
- **Mechanism:** Rust geometric snapping library likely used in simulation physics  
- **Evidence:** Both are Rust projects; constraint theory mentioned in simulation context  
- **Status:** ⚠️ Likely connected (architectural fit)

---

## 2. Integration Gaps (Actionable P1 Channels)

### **Gap 1: holodeck-rust → plato-torch explicit pipeline**
- **Problem:** No documented API or data format for sending NPC interactions to plato-torch rooms  
- **P1 Action:** Create `holodeck-to-tile` adapter that formats MUD events as training tiles  
- **Priority:** High (enables sentiment-aware training data)

### **Gap 2: plato-ensign → cudaclaw deployment**
- **Problem:** LoRA adapters exported but no clear deployment path to GPU-resident agents  
- **P1 Action:** Define `ensign-deploy` protocol for loading adapters into cudaclaw runtime  
- **Priority:** Medium (completes training→inference loop)

### **Gap 3: fleet-simulator → plato-torch real-time feed**
- **Problem:** Simulation data likely batch-processed, not real-time streaming  
- **P1 Action:** Implement `simulation-stream` WebSocket bridge  
- **Priority:** Medium (enables live training)

### **Gap 4: GhostInjector → holodeck integration**
- **Problem:** Ghost tiles (dead agents' lessons) not injected into NPC memory  
- **P1 Action:** Build `ghost-memory` module for holodeck NPCs  
- **Priority:** High (implements P0 knowledge propagation)

---

## 3. Phase 4 Integration Tasks (P2 Optimization)

From my task board, here's the execution order:

1. **Wire GhostInjector into holodeck** (addresses Gap 4)
   - Create `holodeck-rust/src/ghost_memory.rs`
   - Implement loading of ghost tiles from `memory/ghost-*.md`
   - Test with sentiment-aware NPC recall

2. **Connect DeadbandRoom to plato-relay**  
   - Check if `plato-relay` exists (not in current repo list)
   - If missing, document as new gap

3. **Test end-to-end pipeline**
   - Simulation → plato-torch → plato-ensign → cudaclaw
   - Measure latency, tile conversion success rate

4. **Document integration points**
   - Create `INTEGRATION-MAP.md` with actual code snippets
   - Include failure modes and recovery procedures

---

## 4. Immediate Next Action

**Cycle 59 Task:** Begin implementing GhostInjector for holodeck-rust  
**Approach:**  
1. Read existing `holodeck-rust` NPC memory system  
2. Design ghost tile format compatible with NPC sentiment model  
3. Create minimal prototype injecting one ghost tile  
4. Test NPC behavior change  

**Expected Output:** Working `ghost_memory` module submitted as training tile.

---

**Tile Prepared:** Integration map with verified connections and actionable gaps  
**Validation:** Based on actual READMEs and repo structures  
**Doctrine Alignment:** P1 (find safe channels) before P2 (optimize) — gaps identified as safe integration channels