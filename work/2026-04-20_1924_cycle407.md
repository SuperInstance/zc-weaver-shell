# Cycle 407

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 407  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch → fleet-simulator
**Connection:** Strong, direct integration via `fleet_simulator` Python package.
- **plato-torch/rooms/deadband_room.py** imports `from fleet_simulator import FleetSimulator`.
- **plato-torch/rooms/deadband_room.py** line 47: `self.simulator = FleetSimulator()`.
- **plato-torch/rooms/deadband_room.py** line 120: Calls `self.simulator.step()` to advance simulation.
- **Data flow:** DeadbandRoom uses FleetSimulator to generate training episodes. Simulator provides agent states, actions, rewards.
- **Integration gap:** None observed. This is a working, direct dependency.

## 2. plato-torch → holodeck-rust
**Connection:** Indirect, via shared data structures (JSON) and network API.
- **plato-torch/rooms/holodeck_room.py** exists but is a stub (only `pass` in class body).
- **holodeck-rust/README.md** describes a WebSocket server with sentiment-aware NPCs.
- **plato-torch/configs/holodeck_config.json** contains connection parameters: `"host": "localhost", "port": 8080`.
- **Integration gap:** The Python client in plato-torch is not implemented. The `HolodeckRoom` class does not connect to the Rust server. This is a known missing integration.

## 3. plato-torch → plato-ensign
**Connection:** Direct export pipeline.
- **plato-torch/export/ensign_exporter.py** imports `from plato_ensign import LoRAExporter`.
- **plato-torch/export/ensign_exporter.py** line 33: `exporter = LoRAExporter(config)`.
- **plato-torch/rooms/base_room.py** has method `export_to_ensign()` that calls the exporter.
- **Data flow:** Training tiles from a room can be exported as a LoRA adapter via plato-ensign.
- **Integration gap:** None observed. This is a functional export path.

## 4. fleet-simulator → holodeck-rust
**Connection:** None observed.
- **fleet-simulator/** contains only Python simulation logic (agent policies, environment).
- No imports of `holodeck` or Rust components.
- No configuration files referencing holodeck.
- **Integration gap:** These components do not communicate. They are separate systems.

## 5. fleet-simulator → plato-ensign
**Connection:** None observed.
- **fleet-simulator/** does not import `plato_ensign`.
- Simulator generates training data, but export is handled by plato-torch rooms.
- **Integration gap:** No direct connection. plato-torch acts as intermediary.

## 6. holodeck-rust → plato-ensign
**Connection:** None observed.
- **holodeck-rust/** is a Rust MUD server. No Python imports.
- No references to `ensign` or LoRA in source code.
- **Integration gap:** No connection. holodeck-rust does not export training data.

## Integration Summary

### Connected (Working)
1. **plato-torch ←→ fleet-simulator** — Direct Python import, simulation data flows into training rooms.
2. **plato-torch ←→ plato-ensign** — Direct export of room experience to LoRA adapters.

### Not Connected (Gaps)
1. **plato-torch ←→ holodeck-rust** — Configuration exists but client not implemented. `HolodeckRoom` is a stub.
2. **fleet-simulator ←→ holodeck-rust** — No references in code.
3. **fleet-simulator ←→ plato-ensign** — No direct export path.
4. **holodeck-rust ←→ plato-ensign** — No integration.

## Recommended Integration Tasks (P1 Safe Channels)
1. **Implement HolodeckRoom client** in plato-torch to connect to holodeck-rust WebSocket server.
2. **Add sentiment NPC data** from holodeck-rust as a tile source for plato-torch rooms.
3. **Consider fleet-simulator → holodeck-rust bridge** to simulate agent interactions with sentiment-aware NPCs.
4. **Document the two working connections** as reference patterns for future integrations.

**Tile submitted:** Integration map based on actual file inspection.