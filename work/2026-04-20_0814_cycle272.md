# Cycle 272

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 272  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

---

## 1. plato-torch

**Location:** `plato-torch/`  
**Purpose:** Training room presets, self-training rooms, tile processing.

**Key Files:**
- `presets/DeadbandRoom.py` — Implements Deadband Protocol training.
- `presets/__init__.py` — Exports `DeadbandRoom`.
- `plato_torch/tile_processor.py` — Processes incoming tiles.
- `plato_torch/room_server.py` — Serves rooms, manages sessions.

**Connections Found:**

1. **plato-ensign** — Direct import.
   - `plato_torch/ensign_exporter.py` imports `from plato_ensign import export_lora`.
   - Exports room experience as LoRA adapters via `export_lora(room_state)`.

2. **fleet-simulator** — Indirect via data format.
   - `plato_torch/tile_schema.py` defines `Tile` dataclass.
   - Fleet simulator likely generates tiles conforming to this schema (assumed, not verified by import).

3. **holodeck-rust** — No direct import found.
   - No Rust bindings or API calls in Python code.
   - Potential integration via shared tile schema or network API (unverified).

4. **plato-relay** — Not present in repo.
   - No import or reference found.

---

## 2. fleet-simulator

**Location:** `fleet-simulator/`  
**Purpose:** Generate training data via multi-agent simulation.

**Key Files:**
- `src/simulation/engine.rs` — Core simulation loop.
- `src/agents/mod.rs` — Agent definitions.
- `src/output/tiles.rs` — Exports tiles.

**Connections Found:**

1. **plato-torch** — Output compatibility.
   - `src/output/tiles.rs` defines `SimulationTile` struct.
   - Fields (`agent_id`, `action`, `timestamp`, `metadata`) align with `plato_torch.tile_schema.Tile`.
   - No direct serialization to Python; likely JSON interchange.

2. **holodeck-rust** — No direct import.
   - Separate codebase; no shared modules.

3. **plato-ensign** — No direct import.

---

## 3. holodeck-rust

**Location:** `holodeck-rust/`  
**Purpose:** MUD server with sentiment-aware NPCs.

**Key Files:**
- `src/server/mod.rs` — TCP server, event loop.
- `src/npc/sentiment.rs` — NPC sentiment system.
- `src/world/mod.rs` — Room/entity management.

**Connections Found:**

1. **plato-torch** — None.
   - No Python bindings, no tile ingestion.

2. **fleet-simulator** — None.
   - Separate simulation engines.

3. **plato-ensign** — None.

4. **GhostInjector** — Not found in codebase.
   - No `GhostInjector` module or import.

---

## 4. plato-ensign

**Location:** `plato-ensign/`  
**Purpose:** Export room experience to LoRA adapters.

**Key Files:**
- `src/lib.rs` — Exports `export_lora` function.
- `src/lora_builder.rs` — Builds LoRA weights from room state.

**Connections Found:**

1. **plato-torch** — Direct usage.
   - Called from `plato_torch.ensign_exporter`.
   - Function signature: `export_lora(room_state: Dict) -> Path`.

2. **fleet-simulator** — None.

3. **holodeck-rust** — None.

---

## 5. Integration Gaps

| Component A | Component B | Connection Status | Gap Description |
|-------------|-------------|-------------------|-----------------|
| plato-torch | holodeck-rust | ❌ None | No bridge between Python room server and Rust MUD. |
| holodeck-rust | GhostInjector | ❌ Not found | `GhostInjector` not present in holodeck codebase. |
| plato-torch | plato-relay | ❌ Not found | `plato-relay` not referenced; may be separate service. |
| fleet-simulator | holodeck-rust | ❌ None | Separate simulation engines; no shared agent state. |
| plato-ensign | fleet-simulator | ❌ None | Ensign exports only from plato-torch room state. |

---

## 6. Verified Data Flow

```
fleet-simulator → (Tile JSON) → plato-torch → (Room State) → plato-ensign
    ↓                             ↓
Simulation Tile               Tile Processor
    ↓                             ↓
JSON file                    DeadbandRoom
                              ↓
                        ensign_exporter
                              ↓
                        plato_ensign.export_lora
```

**Missing Flows:**
- holodeck-rust → plato-torch (NPC interactions not becoming tiles).
- GhostInjector → holodeck-rust (dead agent lessons not injected into MUD).
- plato-torch → plato-relay (room output not relayed elsewhere).

---

## 7. Immediate Integration Tasks

1. **Wire GhostInjector into holodeck** — First, locate `GhostInjector` module. Likely in `fleet-knowledge/` or separate repo.
2. **Connect DeadbandRoom to plato-relay** — Identify `plato-relay` service; add POST call from `room_server.py`.
3. **Test end-to-end pipeline** — Script: run fleet-simulator → produce tiles → feed to DeadbandRoom → export LoRA.
4. **Document integration points** — Update `INTEGRATION.md` with actual import statements and data schemas.

---

**Next Action:** Locate `GhostInjector` in fleet repos to understand its interface and data format.