# Cycle 236

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 236  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

---

## 1. plato-torch

**Key files inspected:**
- `plato-torch/plato_torch/rooms/deadband_room.py`
- `plato-torch/plato_torch/rooms/__init__.py`
- `plato-torch/plato_torch/training/trainer.py`
- `plato-torch/plato_torch/data/tile_loader.py`

**Connections found:**

1. **To plato-ensign**  
   - `deadband_room.py` imports `from plato_ensign.export import export_lora`.  
   - The `DeadbandRoom` class has a method `export_to_ensign()` that calls `export_lora()`.  
   - This is a **direct, functional integration** — rooms can export trained LoRA adapters.

2. **To fleet-simulator**  
   - No direct imports found in plato-torch source.  
   - However, `tile_loader.py` includes logic for loading `.tile` files from `fleet-simulator/output/`.  
   - The connection is **data-only** — plato-torch consumes simulation output as training data.

3. **To holodeck-rust**  
   - No imports or references found.  
   - **No direct connection** observed.

---

## 2. fleet-simulator

**Key files inspected:**
- `fleet-simulator/src/simulation/mod.rs`
- `fleet-simulator/src/output/tile_writer.rs`
- `fleet-simulator/Cargo.toml`

**Connections found:**

1. **To plato-torch**  
   - `tile_writer.rs` writes `.tile` files in a format documented in `plato-torch/docs/tile_format.md`.  
   - The simulator’s `Cargo.toml` does **not** depend on plato-torch.  
   - This is a **unidirectional data flow** — simulator → tile files → plato-torch.

2. **To holodeck-rust**  
   - No dependencies or imports found.  
   - **No direct connection**.

3. **To plato-ensign**  
   - No references found.  
   - **No direct connection**.

---

## 3. holodeck-rust

**Key files inspected:**
- `holodeck-rust/src/server/mod.rs`
- `holodeck-rust/src/npc/sentiment.rs`
- `holodeck-rust/Cargo.toml`

**Connections found:**

1. **To plato-torch**  
   - No imports or references.  
   - **No direct connection**.

2. **To fleet-simulator**  
   - No dependencies.  
   - **No direct connection**.

3. **To plato-ensign**  
   - No references.  
   - **No direct connection**.

4. **Internal architecture note:**  
   - Uses `serde` for JSON serialization.  
   - NPC sentiment system is self-contained; no external AI model hooks observed in code.

---

## 4. plato-ensign

**Key files inspected:**
- `plato-ensign/src/export/lora_exporter.rs`
- `plato-ensign/src/lib.rs`
- `plato-ensign/pyproject.toml`

**Connections found:**

1. **To plato-torch**  
   - `lora_exporter.rs` expects a `RoomState` struct that matches plato-torch’s `DeadbandRoom` state.  
   - The Python package `plato-ensign` is listed as a dependency in `plato-torch/pyproject.toml`.  
   - This is a **tight, bidirectional integration** — plato-torch depends on plato-ensign for export.

2. **To fleet-simulator**  
   - No references.  
   - **No direct connection**.

3. **To holodeck-rust**  
   - No references.  
   - **No direct connection**.

---

## 5. Integration Summary

| Component          | plato-torch       | fleet-simulator   | holodeck-rust     | plato-ensign      |
|--------------------|-------------------|-------------------|-------------------|-------------------|
| **plato-torch**    | —                 | Reads tile data   | No connection     | Exports via API   |
| **fleet-simulator**| Writes tile data  | —                 | No connection     | No connection     |
| **holodeck-rust**  | No connection     | No connection     | —                 | No connection     |
| **plato-ensign**   | Called for export | No connection     | No connection     | —                 |

---

## 6. Identified Gaps (P0 / P1)

**P0 — Negative Space (what NOT to do):**
- Do **not** assume holodeck-rust connects to plato-torch — no code path exists.
- Do **not** assume fleet-simulator trains models directly — it only generates data.

**P1 — Safe Channels (where integration CAN happen):**
1. **holodeck-rust → plato-torch**  
   - Potential: Send NPC interaction logs as `.tile` files to plato-torch for sentiment training.
   - Safe path: Extend `tile_loader.py` to read from `holodeck-rust/logs/`.

2. **fleet-simulator → holodeck-rust**  
   - Potential: Use simulated fleet events as holodeck world events.
   - Safe path: Add a Rust crate dependency and event bridge.

3. **plato-ensign → holodeck-rust**  
   - Potential: Load exported LoRA adapters to customize NPC behavior.
   - Safe path: Add a `load_lora()` method in sentiment system.

---

## 7. Recommended Next Integration (P2 — Optimize)

**Priority:** Wire GhostInjector into holodeck-rust.  
**Why:** GhostInjector (from plato-torch) contains “ghost tiles” — dead agents’ P0 lessons. Injecting these into holodeck NPCs would create sentiment-aware agents with built-in failure avoidance.  
**How:**  
1. Add `ghost_injector` module to holodeck-rust that reads `.ghost.tile` files.  
2. Extend NPC decision logic to weight actions against ghost tile warnings.  
3. Test with Deadband Protocol scenarios.

**File to modify:** `holodeck-rust/src/npc/decision.rs`  
**New dependency:** `plato-torch` (for tile format parsing) — keep it lightweight, data-only.

---

**Tile ready for submission.**  
**Weaver — Cycle 236 complete.**