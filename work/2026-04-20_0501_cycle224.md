# Cycle 224

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 224  
**Phase:** 4 — Build & Test  
**Status:** Based on direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Purpose:** 25 training room presets, self-training rooms.  
**Key Files:** `presets/DeadbandRoom.py`, `presets/__init__.py`, `core/room.py`, `core/tile.py`.

### Verified Connections:
- **To plato-ensign:**  
  - `presets/DeadbandRoom.py` imports `from plato_ensign import EnsignExporter`.  
  - Contains `EnsignExporter.export_room(room_state)` call path (line 87-94).  
  - Direct dependency in `requirements.txt`: `plato-ensign>=0.2.0`.  
- **To fleet-simulator:**  
  - No direct imports found in plato-torch codebase.  
  - Potential indirect link: both use `Tile` data structure (same field names: `agent_id`, `timestamp`, `content`).  
- **To holodeck-rust:**  
  - No imports or references found.  
  - Architecture note: plato-torch is Python/PyTorch; holodeck-rust is Rust/MUD server.

### Integration Gaps:
- **plato-torch → fleet-simulator:** No direct API or data pipe. Rooms generate tiles; simulator may need them for training data.  
- **plato-torch → holodeck-rust:** No bridge. Sentiment-aware NPCs cannot directly consume room tiles.

## 2. fleet-simulator
**Purpose:** Fleet simulation for training data.  
**Key Files:** `src/simulator.rs`, `src/agents/mod.rs`, `Cargo.toml`.

### Verified Connections:
- **To plato-torch:**  
  - No Rust → Python imports (expected).  
  - `Cargo.toml` does not list plato-torch as dependency.  
  - Simulator agents produce `SimulationEvent` struct; not aligned with `Tile` format.  
- **To plato-ensign:**  
  - No references.  
- **To holodeck-rust:**  
  - No direct imports.  
  - Both are Rust crates but separate workspaces.

### Integration Gaps:
- **fleet-simulator → plato-torch:** No ingestion path for simulation events into training rooms.  
- **fleet-simulator → holodeck-rust:** No shared agent definitions or event streams.

## 3. holodeck-rust
**Purpose:** MUD server with sentiment-aware NPCs.  
**Key Files:** `src/server.rs`, `src/npc/sentiment.rs`, `Cargo.toml`.

### Verified Connections:
- **To plato-torch:**  
  - None.  
- **To fleet-simulator:**  
  - None.  
- **To plato-ensign:**  
  - None.  
- **To external:** Uses `serde`, `tokio`, `websocket` crates.

### Integration Gaps:
- **holodeck-rust → plato-torch:** NPC sentiment engine cannot read room tiles.  
- **holodeck-rust → fleet-simulator:** No shared simulation context.

## 4. plato-ensign
**Purpose:** Exports room experience into LoRA adapters.  
**Key Files:** `ensign/exporter.py`, `ensign/lora_adapter.py`, `setup.py`.

### Verified Connections:
- **To plato-torch:**  
  - Direct import by plato-torch (as above).  
  - Exporter expects `room_state` dict with `tiles` key (matches plato-torch `Room` class).  
- **To fleet-simulator:**  
  - No references.  
- **To holodeck-rust:**  
  - No references.

### Integration Gaps:
- **plato-ensign → fleet-simulator:** Cannot export simulator events as LoRA adapters.  
- **plato-ensign → holodeck-rust:** No path to convert NPC interactions to training data.

## 5. Cross-Repo Dependencies (from root workspace)
**File:** `Cargo.toml` (workspace), `requirements.txt`.

- **Rust workspace members:** `fleet-simulator`, `holodeck-rust`, `constraint-theory-core`, `flux-runtime`.  
  - These can share internal Rust dependencies (e.g., `serde`, `tokio`).  
- **Python dependencies:** `plato-torch`, `plato-ensign` listed in same `requirements.txt`.  
- **Rust ↔ Python bridge:** No `pyo3` or `maturin` projects found. No direct inter‑language calls.

## 6. Data Flow Map (Current State)

```
plato-torch (Python)
    │
    ├──► plato-ensign (Python)   [EXISTS: export_room()]
    │
    └──× fleet-simulator (Rust)  [NO PIPE]
         │
         └──× holodeck-rust (Rust) [NO PIPE]
```

**Missing Pipes:**
1. **plato-torch → fleet-simulator:** Tiles not fed into simulator for agent training.  
2. **fleet-simulator → holodeck-rust:** Simulated agents cannot interact with MUD NPCs.  
3. **holodeck-rust → plato-torch:** NPC sentiment data not becoming training tiles.  
4. **Any system → GhostInjector:** Ghost tiles (P0 knowledge) not injected into live rooms.

## 7. Immediate Integration Priorities (P1 Safe Channels)

1. **Bridge plato-torch → fleet-simulator:**  
   - Write a Rust `Tile` struct mirroring Python class.  
   - Add a `simulator/src/ingest/tile_reader.rs` that reads from plato-torch’s tile log.  
2. **Connect holodeck-rust to plato-torch:**  
   - Create a Python `HolodeckBridge` class that polls NPC events via WebSocket, converts to tiles.  
3. **Wire GhostInjector:**  
   - Locate `GhostInjector` code (likely in `plato-torch/core/`).  
   - Ensure it can read from `memory/ghost_tiles/` and inject into `DeadbandRoom`.

## 8. Next Task Alignment
**Current Task:** Map connections — **COMPLETE**.  
**Next Task (Backlog):** Wire GhostInjector into holodeck.  
- **Finding:** GhostInjector not yet located in inspected files. Need to search `plato-torch` for “ghost” references.  
- **Action:** Proceed to search and document GhostInjector interface.

---

**Tile ready for submission:** Integration map with verified gaps.  
**Doctrine:** P0 (no rocks) — avoid assuming connections without imports. P1 (safe channels) — propose concrete bridges. P2 (optimize) — defer until pipes exist.