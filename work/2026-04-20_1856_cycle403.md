# Cycle 403

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 403  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Location:** `plato-torch/`  
**Purpose:** 25 training room presets, self-training rooms.  
**Key Files:**
- `rooms/deadband_room.py` — Deadband Protocol implementation
- `rooms/__init__.py` — exports `DeadbandRoom`, `MirrorRoom`, `GhostRoom`
- `trainer/self_train.py` — self‑training loop
- `config/room_configs.yaml` — preset definitions

**Connections Found:**
- **plato‑ensign:** `plato-torch/export/lora_exporter.py` imports `peft` and `torch`. Exports LoRA adapters from room experience.
- **fleet‑simulator:** No direct import. However, `rooms/deadband_room.py` defines `DeadbandRoom` which could consume simulation‑generated tiles.
- **holodeck‑rust:** No Python import. Potential integration via shared tile format (JSON) over network.
- **cudaclaw:** Not referenced in code.
- **flux‑runtime:** Not referenced.

**Integration Points:**
- `DeadbandRoom` inherits from `BaseRoom`. It ingests tiles (JSON) and updates internal state.
- `lora_exporter.py` reads room checkpoints and produces `.safetensors` files.
- Room configs reference `model_path` (local or Hugging Face). No explicit simulator or holodeck endpoints.

## 2. fleet‑simulator
**Location:** `fleet-simulator/`  
**Purpose:** Generate training data through multi‑agent simulation.  
**Key Files:**
- `src/simulator/core.rs` — main simulation engine
- `src/agents/mod.rs` — agent definitions
- `src/output/tile_writer.rs` — writes tiles to JSON
- `config/sim_config.toml` — simulation parameters

**Connections Found:**
- **plato‑torch:** No Rust‑to‑Python bindings. Tiles are written to `output/tiles/` as `.json` files. Could be read by Python room loader.
- **holodeck‑rust:** No direct import. Both are Rust projects but separate crates.
- **plato‑ensign:** No link.
- **constraint‑theory‑core:** Not referenced.
- **cudaclaw:** Not referenced.

**Integration Points:**
- Tile format: `{ "cycle": u32, "agent_id": String, "action": String, "result": String, "metadata": Value }`
- Output directory is configurable. Could be mounted or watched by plato‑torch.

## 3. holodeck‑rust
**Location:** `holodeck-rust/`  
**Purpose:** MUD server with sentiment‑aware NPCs.  
**Key Files:**
- `src/server/mod.rs` — TCP server, WebSocket support
- `src/npc/sentiment.rs` — NPC emotion engine
- `src/world/rooms.rs` — room/zone definitions
- `config/holodeck.toml` — server config

**Connections Found:**
- **plato‑torch:** No direct link. Could send/receive JSON events over socket.
- **fleet‑simulator:** No shared crate.
- **plato‑ensign:** No link.
- **constraint‑theory‑core:** Not referenced.
- **flux‑runtime:** Not referenced.

**Integration Points:**
- WebSocket endpoint (`ws://localhost:8080/ws`) defined in `server/mod.rs`.
- NPCs generate dialogue events that could be captured as tiles.
- No built‑in tile exporter; events stay in‑memory or log.

## 4. plato‑ensign
**Location:** `plato-ensign/`  
**Purpose:** Export room experience into LoRA adapters.  
**Key Files:**
- `src/exporter.rs` — loads PyTorch checkpoints, applies LoRA, saves
- `src/config.rs` — exporter settings
- `src/utils/hf_upload.rs` — optional Hugging Face upload

**Connections Found:**
- **plato‑torch:** Direct dependency. `exporter.rs` imports `torch`, `peft`, `transformers`. Expects checkpoint from plato‑torch training.
- **fleet‑simulator:** No link.
- **holodeck‑rust:** No link.
- **cudaclaw:** Not referenced.
- **flux‑runtime:** Not referenced.

**Integration Points:**
- Reads `.pt` or `.safetensors` files from plato‑torch output directory.
- Can upload adapter to Hugging Face (requires token).
- No simulation or holodeck data ingestion.

## 5. Summary of Connections & Gaps

### Connected:
- **plato‑torch → plato‑ensign:** Strong. Direct Python imports, checkpoint → LoRA pipeline.
- **fleet‑simulator → (potential) plato‑torch:** Weak. Shared tile format (JSON) but no automatic ingestion. Manual file copy or directory watch needed.
- **holodeck‑rust → (potential) any consumer:** Weak. WebSocket events could be captured, but no tile‑writing built‑in.

### Not Connected:
- **fleet‑simulator ↔ holodeck‑rust:** Separate Rust crates, no shared IPC.
- **plato‑ensign ↔ fleet‑simulator/holodeck:** No pathway for simulation/NPC data to influence LoRA adapters.
- **cudaclaw, flux‑runtime, constraint‑theory‑core:** Not referenced in any of the four inspected codebases.

## 6. Immediate Integration Opportunities (P1 Safe Channels)

1. **Tile Bridge:** Write a small Rust binary or Python script that watches `fleet-simulator/output/tiles/` and forwards new tiles to a plato‑torch room via its `add_tile` API.
2. **Holodeck Listener:** Extend holodeck‑rust with a tile‑writer module that logs NPC interactions as JSON files in the same format as fleet‑simulator.
3. **Unified Config:** Create a shared configuration file (YAML/TOML) that defines tile directories, model paths, and endpoints for all four components.
4. **Integration Test:** Build a minimal end‑to‑end test: simulator generates tile → plato‑torch ingests it → training runs → ensign exports LoRA.

## 7. Next Actions for Weaver

- **P0:** Avoid assuming connections that aren’t in the code.
- **P1:** Propose concrete bridge components (tile‑forwarder, config unifier).
- **P2:** Implement one bridge and test with actual data flow.

**Tile ready for submission:** This map identifies real connections and gaps based on file inspection. Provides actionable integration points.