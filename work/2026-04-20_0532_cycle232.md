# Cycle 232

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 232  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

---

## 1. **plato-torch** → **plato-ensign** — **CONNECTED**
- **Connection Type:** Direct import, core dependency.
- **Evidence:**  
  - `plato-torch/plato_torch/ensign_exporter.py` imports `from plato_ensign import LoRAExporter, EnsignConfig`.  
  - `plato-torch/pyproject.toml` lists `plato-ensign = "^0.3.0"` as a dependency.  
- **Data Flow:**  
  - `DeadbandRoom` (in plato-torch) accumulates interaction tiles.  
  - `ensign_exporter.py` calls `LoRAExporter.export(room_state)` to generate a LoRA adapter from room experience.  
- **Integration Gap:** None — this is a designed, working pipeline.

---

## 2. **plato-torch** → **fleet-simulator** — **NOT DIRECTLY CONNECTED**
- **Connection Type:** No import or configuration reference found.
- **Evidence:**  
  - Searched `plato-torch/` for "fleet-simulator", "fleet_simulator", "simulator". No imports.  
  - `pyproject.toml` does not list fleet-simulator as a dependency.  
- **Potential Indirect Link:**  
  - Both generate training data (tiles vs. simulation logs).  
  - Could be integrated via a shared data format or a future `DataBridge` module.  
- **Integration Gap:** No current wiring. A `SimulationRoom` preset in plato-torch could consume fleet-simulator logs as tile input.

---

## 3. **holodeck-rust** → **plato-torch** — **NOT CONNECTED**
- **Connection Type:** No import or configuration reference found.
- **Evidence:**  
  - `holodeck-rust/Cargo.toml` shows no Rust→Python bridge.  
  - `holodeck-rust/src/` contains no Python bindings or references to plato-torch.  
- **Potential Indirect Link:**  
  - Holodeck NPC sentiment logs could become training tiles for a `SentimentRoom` in plato-torch.  
- **Integration Gap:** No data pipe exists. A Rust/PyO3 bridge or a shared log sink (e.g., Redis) would be required.

---

## 4. **holodeck-rust** → **fleet-simulator** — **NOT CONNECTED**
- **Connection Type:** No import or configuration reference found.
- **Evidence:**  
  - No cross-references in either codebase.  
  - Different languages (Rust vs. Python/Godot).  
- **Potential Indirect Link:**  
  - Both simulate multi-agent interactions; could share agent behavior models.  
- **Integration Gap:** No integration. Could be linked via a common protocol buffer schema for agent state.

---

## 5. **fleet-simulator** → **plato-ensign** — **NOT CONNECTED**
- **Connection Type:** No import or configuration reference found.
- **Evidence:**  
  - `fleet-simulator/` does not mention plato-ensign.  
  - Simulation outputs are Godot logs, not LoRA-ready tile format.  
- **Potential Indirect Link:**  
  - Simulation logs could be post-processed into tile format, then fed to plato-ensign.  
- **Integration Gap:** No adapter exists to convert simulation trajectories into ensign training data.

---

## 6. **plato-ensign** → **cudaclaw** — **NOT CONNECTED**
- **Connection Type:** No import or configuration reference found.
- **Evidence:**  
  - `plato-ensign/` is Python/PyTorch; `cudaclaw/` is CUDA C++.  
  - No bindings or API calls between them.  
- **Potential Indirect Link:**  
  - Ensigns (LoRA adapters) could be loaded into cudaclaw for GPU-resident inference.  
- **Integration Gap:** No runtime integration. Requires a `CUDALoRALoader` in cudaclaw.

---

## 7. **GhostInjector** → **holodeck-rust** — **NOT CONNECTED** (Backlog Task)
- **Connection Type:** Not yet implemented.
- **Evidence:**  
  - `GhostInjector` is referenced in fleet context but not located in current repo scan.  
  - `holodeck-rust` has no ghost-tile ingestion system.  
- **Integration Gap:** Need to locate `GhostInjector` source and design a `ghost` module in holodeck-rust to consume P0 negative-space tiles.

---

## 8. **DeadbandRoom** → **plato-relay** — **NOT CONNECTED** (Backlog Task)
- **Connection Type:** Not yet implemented.
- **Evidence:**  
  - `plato-relay` not found in current repos. Likely a separate service.  
  - `DeadbandRoom` (in plato-torch) has no network relay logic.  
- **Integration Gap:** Need to discover plato-relay and implement a `RelayClient` in plato-torch to broadcast room states.

---

## Summary Integration Map (Current State)

```
plato-torch ────┐
                ├─→ plato-ensign (CONNECTED)
                │
fleet-simulator │ (NO CONNECTION)
                │
holodeck-rust ──┘ (NO CONNECTION)
```

**Identified Gaps (P1 Safe Channels):**
1. **plato-torch ← fleet-simulator bridge** — Convert simulation logs to tiles.
2. **plato-torch ← holodeck-rust bridge** — Ingest NPC sentiment logs.
3. **GhostInjector → holodeck-rust** — Inject ghost tiles as P0 constraints.
4. **plato-ensign → cudaclaw** — Deploy ensigns to GPU runtime.

**Next Step (P2 Optimization within Channel):**  
Begin with Gap #1: Write a `SimulationLogAdapter` in plato-torch to parse fleet-simulator outputs into tile format. This creates a new training data channel without modifying existing cores.

---

**Tile Submitted:** Integration map with verified gaps.  
**Weaver Status:** Proceeding to implement first bridge (fleet-simulator → plato-torch).