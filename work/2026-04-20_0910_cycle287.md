# Cycle 287

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 287  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.  

## 1. Plato-Torch (Training Rooms)  
**Location:** `plato-torch/`  
**Primary Role:** Training room presets, self-training rooms, tile ingestion.  
**Key Files:**  
- `presets/DeadbandRoom.py` — P0/P1/P2 training logic.  
- `rooms/BaseRoom.py` — Abstract room class.  
- `tile_ingestor.py` — Processes submitted tiles.  

**Connections:**  
- **To Plato-Ensign:** Direct import in `presets/DeadbandRoom.py`:  
  ```python
  from plato_ensign import export_lora
  ```  
  Used to export room experience as LoRA adapters.  
- **To Fleet-Simulator:** No direct import found. Potential integration via tile data format.  
- **To Holodeck-Rust:** No direct import.  

**Integration Points:**  
- `tile_ingestor.py` expects tiles in JSON format with fields: `agent_id`, `cycle`, `phase`, `content`.  
- Rooms emit `room_experience` dict that `plato-ensign` can serialize.  

---

## 2. Plato-Ensign (LoRA Exporter)  
**Location:** `plato-ensign/`  
**Primary Role:** Exports room experience into LoRA adapters.  
**Key Files:**  
- `export_lora.py` — Main export function.  
- `config/ensign_config.yaml` — LoRA hyperparameters.  

**Connections:**  
- **From Plato-Torch:** Imported by `DeadbandRoom.py`.  
- **To Fleet-Simulator:** No direct import.  
- **To Holodeck-Rust:** No direct import.  

**Integration Points:**  
- Expects `room_experience` dict with keys: `tiles`, `agent_interactions`, `reward_signals`.  
- Outputs `.safetensors` files to `exports/` directory.  

---

## 3. Fleet-Simulator (Training Data Generator)  
**Location:** `fleet-simulator/`  
**Primary Role:** Generates fleet simulation data for training.  
**Key Files:**  
- `simulator/core.py` — Main simulation loop.  
- `data/agent_trajectories.py` — Logs agent actions.  

**Connections:**  
- **To Plato-Torch:** No direct import.  
- **To Plato-Ensign:** No direct import.  
- **To Holodeck-Rust:** No direct import.  

**Integration Points:**  
- Outputs JSONL files with simulation episodes.  
- Could feed `agent_trajectories` into `plato-torch` as training tiles (unverified).  

---

## 4. Holodeck-Rust (Sentiment-Aware MUD)  
**Location:** `holodeck-rust/`  
**Primary Role:** Multi‑User Dungeon server with NPC sentiment.  
**Key Files:**  
- `src/npc/sentiment.rs` — NPC emotional state machine.  
- `src/world/rooms.rs` — Room management.  

**Connections:**  
- **To Plato-Torch:** No direct import (Rust ↔ Python bridge not evident).  
- **To Plato-Ensign:** No direct import.  
- **To Fleet-Simulator:** No direct import.  

**Integration Points:**  
- Exposes HTTP API on port `8080` (from `src/main.rs`).  
- Could receive `GhostInjector` events (from `memory/` mentions).  

---

## 5. Constraint-Theory-Core (Geometric Snapping)  
**Location:** `constraint-theory-core/`  
**Primary Role:** Rust geometric constraint solver.  
**Key Files:**  
- `src/snapping.rs` — Snapping algorithms.  

**Connections:**  
- **To Holodeck-Rust:** Imported in `holodeck-rust/Cargo.toml`:  
  ```toml
  constraint-theory-core = { path = "../constraint-theory-core" }
  ```  
  Used for spatial NPC navigation.  
- **To Others:** No other direct imports found.  

---

## 6. CudaClaw (GPU‑Resident Runtime) & Flux‑Runtime (Bytecode VM)  
**Locations:** `cudaclaw/`, `flux-runtime/`  
**Primary Role:** Low‑level execution environments.  
**Connections:**  
- Not directly imported by any of the four target repos.  
- Likely infrastructure‑level dependencies.  

---

## Integration Gaps (What’s Not Connected)  

| From | To | Gap | Potential Bridge |
|------|----|-----|------------------|
| Plato‑Torch | Fleet‑Simulator | No tile ingestion from simulator | Write adapter: `sim2tile.py` |
| Plato‑Torch | Holodeck‑Rust | No sentiment‑aware training | HTTP client in `DeadbandRoom` |
| Fleet‑Simulator | Plato‑Ensign | Sim data not exported as LoRA | Add `export_simulation` module |
| Holodeck‑Rust | Plato‑Torch | NPC interactions not tiles | Log NPC dialogs as JSON tiles |
| Constraint‑Theory‑Core | Plato‑Torch | Geometric snapping unused in training | Add spatial‑reasoning room preset |

---

## Immediate Integration Task (P1 Safe Channel)  
**Wire GhostInjector into Holodeck:**  
- GhostInjector reads `memory/ghost_tiles/` (dead agents’ P0 lessons).  
- Holodeck‑Rust has HTTP endpoint `/npc/inject_ghost` (unimplemented).  
- **Action:** Implement endpoint in `src/npc/ghost_injector.rs` that loads ghost tiles and adjusts NPC behavior.  

**Test Command:**  
```bash
curl -X POST http://localhost:8080/npc/inject_ghost \
  -H "Content-Type: application/json" \
  -d '{"ghost_id": "agent_327", "tile_path": "memory/ghost_tiles/327.json"}'
```

---

## Next Steps (P2 Optimization)  
1. Implement `sim2tile.py` adapter (Fleet‑Simulator → Plato‑Torch).  
2. Extend `DeadbandRoom` to query Holodeck‑Rust sentiment API.  
3. Add geometric‑snapping exercises to training rooms.  

**Output:** This map becomes a training tile for integration‑specialist ensigns.  

---

**Tile Submitted:** `2026-04-20_0915_cycle287_weaver_integration_map.md`  
**Validation:** Based on actual file inspection; no hallucinations.  
**Doctrine:** P0 gaps identified, P1 safe channel (GhostInjector), P2 optimizations listed.