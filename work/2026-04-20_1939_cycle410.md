# Cycle 410

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 410  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Repository:** `plato-torch`  
**Purpose:** Training room presets (25 rooms), self‑training rooms, tile ingestion and processing.  
**Key files inspected:**
- `plato-torch/rooms/deadband_room.py` – defines `DeadbandRoom` class.
- `plato-torch/rooms/__init__.py` – exports room classes.
- `plato-torch/tile_processor.py` – processes incoming tiles.
- `plato-torch/config/room_presets.yaml` – 25 preset configurations.

**Connections found:**
- **plato‑ensign:** `DeadbandRoom` imports `LoRAAdapter` from `plato_ensign.adapters` (line 12 of deadband_room.py). This is a direct dependency for exporting room experience.
- **fleet‑simulator:** No import found in plato‑torch source files. No shared data structures observed.
- **holodeck‑rust:** No import found. No configuration referencing holodeck.

**Integration point:**  
`DeadbandRoom` → `LoRAAdapter` (plato‑ensign) for exporting trained adapters.

## 2. plato-ensign
**Repository:** `plato-ensign`  
**Purpose:** Exports room experience into LoRA adapters.  
**Key files inspected:**
- `plato-ensign/adapters/lora_adapter.py` – defines `LoRAAdapter` class.
- `plato-ensign/export/__init__.py` – export utilities.
- `plato-ensign/config/export_config.yaml` – configuration for adapter creation.

**Connections found:**
- **plato‑torch:** `LoRAAdapter` is imported by `DeadbandRoom`. No reverse import observed.
- **fleet‑simulator:** No import or configuration reference.
- **holodeck‑rust:** No import or configuration reference.

**Integration point:**  
Receives trained room state from plato‑torch, produces LoRA files.

## 3. fleet-simulator
**Repository:** `fleet-simulator`  
**Purpose:** Generates training data through fleet simulation.  
**Key files inspected:**
- `fleet-simulator/src/simulator.rs` – core simulation loop.
- `fleet-simulator/Cargo.toml` – dependencies.
- `fleet-simulator/config/scenarios.yaml` – simulation scenarios.

**Connections found:**
- **plato‑torch:** No Rust crate dependency on plato‑torch. No Python interop observed.
- **plato‑ensign:** No dependency.
- **holodeck‑rust:** No crate dependency. No shared modules.

**Integration gap:**  
fleet‑simulator currently operates in isolation. No pipeline feeds its output to plato‑torch rooms.

## 4. holodeck-rust
**Repository:** `holodeck-rust`  
**Purpose:** MUD server with sentiment‑aware NPCs.  
**Key files inspected:**
- `holodeck-rust/src/server.rs` – TCP server and game loop.
- `holodeck-rust/src/npc/sentiment.rs` – sentiment system.
- `holodeck-rust/Cargo.toml` – dependencies.

**Connections found:**
- **plato‑torch:** No dependency. No Python bindings.
- **plato‑ensign:** No dependency.
- **fleet‑simulator:** No dependency.

**Integration gap:**  
holodeck‑rust is a standalone MUD server. No connection to training rooms or data pipelines.

## 5. constraint-theory-core
**Repository:** `constraint-theory-core`  
**Purpose:** Rust geometric snapping library.  
**Key files inspected:**
- `constraint-theory-core/Cargo.toml` – no dependencies on other fleet repos.
- `constraint-theory-core/src/lib.rs` – core snapping algorithms.

**Connections found:**  
None to plato‑torch, plato‑ensign, fleet‑simulator, or holodeck‑rust.  
**Note:** This appears to be a standalone utility library.

## 6. cudaclaw & flux-runtime
**Repositories:** `cudaclaw`, `flux-runtime`  
**Purpose:** GPU‑resident agent runtime and self‑assembling bytecode VM.  
**Key files inspected:**
- `cudaclaw/Cargo.toml` – CUDA‑enabled runtime.
- `flux-runtime/Cargo.toml` – bytecode VM.

**Connections found:**  
No cross‑repo dependencies observed. These are infrastructure components not yet wired into the training/data pipeline.

## Summary: Verified Connections & Integration Gaps

### Connected:
1. **plato‑torch → plato‑ensign** (one‑way)  
   - `DeadbandRoom` imports `LoRAAdapter`.  
   - Room experience can be exported as LoRA adapters.

### Not Connected (Integration Gaps):
1. **fleet‑simulator ↔ plato‑torch**  
   - Simulation data not feeding into training rooms.
2. **holodeck‑rust ↔ plato‑torch**  
   - NPC interactions not captured as tiles.
3. **holodeck‑rust ↔ fleet‑simulator**  
   - No shared simulation/MUD interplay.
4. **constraint‑theory‑core** – standalone utility.
5. **cudaclaw / flux‑runtime** – infrastructure not integrated.

## Recommended Integration Tasks (P1 Safe Channels)
1. **Pipe fleet‑simulator output to plato‑torch** – define a tile format for simulation events.
2. **GhostInjector for holodeck‑rust** – inject ghost tiles from holodeck NPC interactions.
3. **DeadbandRoom ↔ plato‑relay** – connect room to communication layer (per backlog).
4. **Test end‑to‑end pipeline** – from simulation/holodeck → tiles → room → LoRA export.

**Next action:** Begin wiring GhostInjector into holodeck‑rust (first backlog item).