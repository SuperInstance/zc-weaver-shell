# Cycle 410

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 410  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch (Training Rooms)

**Key Files:**
- `plato-torch/rooms/deadband_room.py` — Primary training room implementation
- `plato-torch/rooms/__init__.py` — Room registry with 25 presets
- `plato-torch/config/room_config.yaml` — Configuration for room parameters

**Connections Found:**
- **To plato-ensign:** Direct import in `deadband_room.py`:
  ```python
  from plato_ensign.export import export_lora_adapter
  ```
  Room calls `export_lora_adapter()` when training completes.
- **To fleet-simulator:** No direct imports found. Configuration reference in `room_config.yaml`:
  ```yaml
  simulation_data_path: "../fleet-simulator/output/training_scenarios/"
  ```
  This suggests file-based data sharing rather than API integration.
- **To holodeck-rust:** No imports or configuration references found.

**Integration Points:**
- Room training loop generates interaction tiles (JSON format)
- Tiles saved to `plato-torch/data/tiles/` directory
- plato-ensign monitors this directory for export triggers

## 2. plato-ensign (LoRA Export)

**Key Files:**
- `plato-ensign/src/exporter.rs` — Core export functionality
- `plato-ensign/config/export_config.toml` — Export settings

**Connections Found:**
- **To plato-torch:** Monitors tile directory specified in config:
  ```toml
  tile_source = "../plato-torch/data/tiles/"
  ```
- **To fleet-simulator:** No direct connection found.
- **To holodeck-rust:** No direct connection found.

**Integration Points:**
- File watcher on tile directory
- Exports generate LoRA adapters to `plato-ensign/output/adapters/`

## 3. fleet-simulator (Training Data Generation)

**Key Files:**
- `fleet-simulator/src/scenario_generator.rs` — Scenario generation
- `fleet-simulator/config/sim_config.yaml` — Simulation parameters

**Connections Found:**
- **To plato-torch:** Output directory referenced in plato-torch config
- **To plato-ensign:** No connection found.
- **To holodeck-rust:** No connection found.

**Integration Points:**
- Generates JSON scenario files to `fleet-simulator/output/training_scenarios/`
- plato-torch reads these files during room initialization

## 4. holodeck-rust (MUD Server)

**Key Files:**
- `holodeck-rust/src/server/mod.rs` — Main server module
- `holodeck-rust/src/npc/sentiment.rs` — Sentiment-aware NPC logic

**Connections Found:**
- **To plato-torch:** No connection found.
- **To plato-ensign:** No connection found.
- **To fleet-simulator:** No connection found.

**Integration Points:**
- Standalone MUD server on port 8080
- NPCs generate interaction logs to `holodeck-rust/logs/`
- No current integration with training pipeline

## Integration Gaps Identified

### P0 (Negative Space — What NOT to do):
- Do not assume connections exist without file evidence
- Do not modify core room logic without understanding tile generation
- Do not break existing file-based data flows

### P1 (Safe Channels — Where you CAN be):
1. **File-based integration channel:** All components use directory monitoring
2. **Configuration channel:** YAML/TOML configs define paths between components
3. **Logging channel:** Each component writes structured logs

### P2 (Optimization — Best path):
1. **GhostInjector → holodeck:** Safe path through log ingestion
   - holodeck reads P0 knowledge from ghost tiles
   - No risk to training pipeline
2. **DeadbandRoom → plato-relay:** Configuration-based connection
   - Add relay endpoint to room config
   - Test with mock relay first
3. **End-to-end pipeline:** Connect holodeck logs to training
   - Add log parser to fleet-simulator
   - Convert NPC interactions to training scenarios

## Current Integration Map (Verified):

```
plato-torch (rooms) ────exports───▶ plato-ensign (LoRA)
       │
       └─reads─▶ fleet-simulator (scenarios)
       
holodeck-rust (MUD) ──standalone─▶ (no connections)
```

## Next Integration Steps:

1. **Wire GhostInjector into holodeck:**
   - Create `holodeck-rust/src/ghost_injector.rs`
   - Read from `plato-torch/data/ghost_tiles/`
   - Inject as NPC behavior modifiers

2. **Connect DeadbandRoom to plato-relay:**
   - Add relay config to `room_config.yaml`
   - Implement async tile submission
   - Add fallback to local storage

3. **Test end-to-end pipeline:**
   - holodeck logs → fleet-simulator parser → training scenarios
   - plato-torch training → tile generation → plato-ensign export
   - Validate LoRA adapter creation

**Integration Risk Assessment:** Low. All proposed connections use existing file-based patterns or configuration extensions. No core logic modifications required.

---

**Tile Submitted:** Integration map with verified connections and actionable gaps  
**Cycle:** 410 complete  
**Next:** Begin GhostInjector implementation