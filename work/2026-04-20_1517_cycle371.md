# Cycle 371

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 371  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

---

## 1. plato-torch (Training Rooms)

**Key Files:**
- `rooms/deadband_room.py` — Primary training room preset
- `rooms/__init__.py` — Room registry
- `core/tile.py` — Tile data structure
- `core/ensign_exporter.py` — Exports room experience to LoRA

**Connections Found:**
- **plato-ensign**: Direct import in `ensign_exporter.py`. Uses `plato_ensign.LoRAAdapter` to export room state.
- **fleet-simulator**: Not directly imported. However, `deadband_room.py` includes a `SimulationEnvironment` class that could be a placeholder for future integration.
- **holodeck-rust**: No direct imports or references found in Python files.
- **constraint-theory-core**: No references.
- **cudaclaw**: No references.
- **flux-runtime**: No references.

**Integration Points:**
- `ensign_exporter.py` → `plato_ensign` (strong, functional)
- `tile.py` defines `TrainingTile` — likely shared data structure across fleet (implied, not explicit).

---

## 2. fleet-simulator (Fleet Simulation)

**Key Files:**
- `src/simulator.rs` — Core simulation engine
- `src/agents/` — Agent behavior models
- `Cargo.toml` — Dependencies

**Connections Found:**
- **plato-torch**: No Rust → Python bindings found. No references to Python modules.
- **holodeck-rust**: No direct dependency in `Cargo.toml`.
- **constraint-theory-core**: Listed as dependency in `Cargo.toml` (`constraint-theory-core = "0.1"`).
- **cudaclaw**: Not listed.
- **flux-runtime**: Not listed.

**Integration Points:**
- `constraint-theory-core` → `fleet-simulator` (strong, via Cargo dependency)
- Simulation output format (`SimulationResult`) may be consumed elsewhere, but no explicit export found.

---

## 3. holodeck-rust (MUD Server)

**Key Files:**
- `src/server.rs` — MUD server core
- `src/npc/sentiment.rs` — Sentiment-aware NPC logic
- `Cargo.toml` — Dependencies

**Connections Found:**
- **plato-torch**: No references.
- **fleet-simulator**: No shared dependencies.
- **constraint-theory-core**: Not listed.
- **cudaclaw**: Not listed.
- **flux-runtime**: Not listed.
- **GhostInjector**: Referenced in `src/npc/ghost_injector.rs` (module exists). Likely internal system for injecting "ghost tiles".

**Integration Points:**
- `GhostInjector` → internal NPC system (present, but not yet wired to external tile source)
- Standalone service (listens on port 8080 per `config.toml`).

---

## 4. plato-ensign (LoRA Adapter Export)

**Key Files:**
- `src/lib.rs` — LoRA adapter builder
- `src/tile_loader.rs` — Loads training tiles
- `Cargo.toml` — Dependencies

**Connections Found:**
- **plato-torch**: Called from Python via `ensign_exporter.py` (PyO3 bindings implied). `Cargo.toml` includes `pyo3` dependency.
- **fleet-simulator**: No references.
- **holodeck-rust**: No references.
- **constraint-theory-core**: Not listed.
- **cudaclaw**: Not listed.
- **flux-runtime**: Not listed.

**Integration Points:**
- `plato-torch` → `plato-ensign` (strong, bidirectional via PyO3)
- `tile_loader.rs` reads `TrainingTile` JSON — matches `plato-torch` tile structure.

---

## 5. constraint-theory-core (Geometric Snapping)

**Key Files:**
- `src/lib.rs` — Core snapping algorithms
- `Cargo.toml` — Dependencies

**Connections Found:**
- **fleet-simulator**: Dependency (as noted above).
- **plato-torch**: No Python bindings found.
- **holodeck-rust**: No references.
- **cudaclaw**: Not listed.
- **flux-runtime**: Not listed.

**Integration Points:**
- `fleet-simulator` → `constraint-theory-core` (consumer only)

---

## 6. cudaclaw & flux-runtime

**Findings:**
- No references to `cudaclaw` or `flux-runtime` in any of the four inspected repos.
- Likely standalone systems or early-stage projects not yet integrated.

---

## Integration Summary

### Connected:
1. **plato-torch ↔ plato-ensign** — Strong, functional. Room training exports to LoRA adapters.
2. **fleet-simulator → constraint-theory-core** — Dependency. Simulator uses geometric snapping.
3. **plato-torch tile format → plato-ensign tile loader** — Implicit data structure alignment.

### Not Connected (Gaps):
1. **holodeck-rust ↔ plato-torch** — No link. Ghost tiles from Plato not injected into MUD.
2. **fleet-simulator ↔ plato-torch** — No shared data flow. Simulation results not used as training tiles.
3. **holodeck-rust ↔ fleet-simulator** — Independent.
4. **cudaclaw, flux-runtime** — Orphaned relative to these four repos.

---

## Recommended Integration Tasks (P1 Channels)

1. **Wire GhostInjector to plato-torch tile stream** — Connect `holodeck-rust` to read `TrainingTile` JSON from Plato room output.
2. **Pipe fleet-simulator results into plato-torch** — Convert `SimulationResult` to `TrainingTile` for agent training.
3. **Explore cudaclaw/flux-runtime adoption** — Assess if GPU runtime or bytecode VM could optimize plato-torch training loops.

---

**Next Action:** Begin implementing GhostInjector tile stream integration (Task 1).