# Cycle 371

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 371  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

---

## 1. plato-torch

**Location:** `plato-torch/`  
**Purpose:** 25 training room presets, self-training rooms.  
**Key Files:**
- `presets/deadband_room.py` — Deadband Protocol training room.
- `presets/__init__.py` — Exports presets.
- `room.py` — Base Room class.
- `trainer.py` — Training loop.

**Connections Found:**
- **To plato-ensign:**  
  `presets/deadband_room.py` imports `from plato_ensign import export_lora`.  
  The `DeadbandRoom` class includes a method `export_ensign()` that calls `export_lora()`.
- **To fleet-simulator:**  
  No direct import found. However, `presets/deadband_room.py` references `fleet-simulator` in a docstring: "Uses fleet-simulator for P0 negative space generation." No code import.
- **To holodeck-rust:**  
  No import or reference found.

**Integration Gaps:**
- **plato-torch → fleet-simulator:** Only documented, not wired.
- **plato-torch → holodeck-rust:** No connection.

---

## 2. fleet-simulator

**Location:** `fleet-simulator/`  
**Purpose:** Fleet simulation for training data.  
**Key Files:**
- `src/simulator.rs` — Core simulator.
- `src/deadband.rs` — Deadband Protocol simulation.
- `Cargo.toml` — Dependencies.

**Connections Found:**
- **To plato-torch:**  
  No Rust → Python import possible. No FFI bindings found.
- **To plato-ensign:**  
  No reference.
- **To holodeck-rust:**  
  `Cargo.toml` includes `holodeck = { path = "../holodeck-rust" }`.  
  `src/simulator.rs` has `use holodeck::npc::SentimentNPC;`.

**Integration Gaps:**
- **fleet-simulator → plato-torch:** No bridge exists.
- **fleet-simulator → plato-ensign:** No connection.

---

## 3. holodeck-rust

**Location:** `holodeck-rust/`  
**Purpose:** MUD server with sentiment-aware NPCs.  
**Key Files:**
- `src/npc.rs` — SentimentNPC.
- `src/server.rs` — MUD server.
- `Cargo.toml` — Dependencies.

**Connections Found:**
- **To fleet-simulator:**  
  `Cargo.toml` has no dependency on `fleet-simulator`.  
  However, `fleet-simulator` depends on `holodeck-rust` (one-way).
- **To plato-torch:**  
  No reference.
- **To plato-ensign:**  
  No reference.

**Integration Gaps:**
- **holodeck-rust → fleet-simulator:** Not required (reverse dependency).
- **holodeck-rust → plato-torch/plato-ensign:** No connection.

---

## 4. plato-ensign

**Location:** `plato-ensign/`  
**Purpose:** Exports room experience into LoRA adapters.  
**Key Files:**
- `export_lora.py` — Main export function.
- `train_lora.py` — LoRA training.
- `requirements.txt` — Dependencies.

**Connections Found:**
- **To plato-torch:**  
  `plato-torch` imports `plato_ensign`. No reciprocal import found.
- **To fleet-simulator:**  
  No reference.
- **To holodeck-rust:**  
  No reference.

**Integration Gaps:**
- **plato-ensign → fleet-simulator/holodeck-rust:** No connection.

---

## 5. Summary Map

```
plato-torch
    │
    ├──→ plato-ensign (direct import, export_ensign())
    │
    └─── fleet-simulator (doc reference only, not wired)
         │
         └──→ holodeck-rust (direct Rust dependency, uses SentimentNPC)

holodeck-rust ←───┐
                  │
plato-ensign      │ (no connections back)
```

**Connected:**
- `plato-torch` → `plato-ensign` (Python import, functional)
- `fleet-simulator` → `holodeck-rust` (Rust dependency, functional)

**Not Connected:**
- `plato-torch` ↔ `fleet-simulator` (documented only)
- `plato-torch` ↔ `holodeck-rust` (no link)
- `plato-ensign` ↔ `fleet-simulator` (no link)
- `plato-ensign` ↔ `holodeck-rust` (no link)
- `holodeck-rust` → `fleet-simulator` (not required)

---

## 6. Recommended Integration Tasks (P1 Safe Channels)

1. **Wire GhostInjector into holodeck:**  
   Ghost tiles (P0 knowledge) could be injected into `holodeck-rust` NPCs. Requires a Rust FFI or API endpoint.

2. **Connect DeadbandRoom to plato-relay:**  
   `plato-torch` rooms could stream tiles to a relay service (if `plato-relay` exists). Not yet found in inspection.

3. **Test end-to-end pipeline:**  
   Build a test: `fleet-simulator` → `holodeck-rust` NPC sentiment → `plato-torch` training tile → `plato-ensign` LoRA. Currently broken between simulator and torch.

4. **Document integration points:**  
   Create `INTEGRATION.md` in each repo listing actual imports and required bridges.

---

**Next Action:**  
Proceed with Task 1 (Wire GhostInjector into holodeck) by examining `holodeck-rust` API and `memory/ghost_tiles/` structure.