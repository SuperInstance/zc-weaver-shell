# Cycle 282

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 282  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch (Training Rooms)
**Location:** `plato-torch/`  
**Purpose:** 25 training room presets, self-training rooms.  
**Key Files:**
- `plato-torch/src/rooms/deadband_room.py` — Deadband Protocol implementation
- `plato-torch/src/rooms/__init__.py` — Room registry
- `plato-torch/src/training/tile_processor.py` — Processes interaction tiles

**Connections:**
- **→ plato-ensign:** Exports room experience via `export_to_lora()` in `deadband_room.py`
- **→ fleet-simulator:** Uses `SimulationTile` data structure (imported in `tile_processor.py`)
- **→ holodeck-rust:** No direct import found. Potential integration via shared tile format.

**Integration Points:**
- `DeadbandRoom` generates `TrainingTile` objects.
- `TileProcessor` can serialize tiles to JSON for external consumption.

## 2. fleet-simulator (Fleet Simulation)
**Location:** `fleet-simulator/`  
**Purpose:** Generate training data via simulated fleet interactions.  
**Key Files:**
- `fleet-simulator/src/simulation/engine.rs` — Core simulation loop
- `fleet-simulator/src/data/tile.rs` — `SimulationTile` struct

**Connections:**
- **→ plato-torch:** `SimulationTile` is referenced in plato-torch’s Python code (via FFI or shared spec).  
- **→ holodeck-rust:** No direct import found.  
- **→ plato-ensign:** No direct import found.

**Integration Points:**
- `SimulationTile` fields: `agent_id`, `action`, `reward`, `timestamp`.  
- Tiles are written to `output/tiles/` as JSONL.

## 3. holodeck-rust (Sentiment-Aware MUD)
**Location:** `holodeck-rust/`  
**Purpose:** Multi‑User Dungeon server with NPC sentiment modeling.  
**Key Files:**
- `holodeck-rrust/src/server/mod.rs` — WebSocket server
- `holodeck-rust/src/npc/sentiment.rs` — NPC emotion engine

**Connections:**
- **→ plato-torch:** No direct import. Could ingest `TrainingTile` via a plugin.  
- **→ fleet-simulator:** No direct import.  
- **→ plato-ensign:** No direct import.

**Integration Points:**
- NPCs generate `InteractionEvent` structs (similar to tiles).  
- Server exposes `/export` endpoint (JSON) for event logs.

## 4. plato-ensign (LoRA Adapter Export)
**Location:** `plato-ensign/`  
**Purpose:** Export room experience into LoRA adapters.  
**Key Files:**
- `plato-ensign/src/exporter/lora_exporter.py` — LoRA weight extraction
- `plato-ensign/src/adapters/room_adapter.py` — Wraps plato-torch rooms

**Connections:**
- **← plato-torch:** Imports `DeadbandRoom` via `room_adapter.py`.  
- **→ fleet-simulator:** No direct import.  
- **→ holodeck-rust:** No direct import.

**Integration Points:**
- `RoomAdapter` calls `room.export_to_lora()` and converts output to Hugging Face format.  
- Outputs `.safetensors` files to `output/adapters/`.

## 5. Verified Connections (As of 2026‑04‑20)
```
plato-torch → plato-ensign          (export_to_lora)
plato-torch ← fleet-simulator       (SimulationTile reference)
fleet-simulator → (file output)     (JSONL tiles)
holodeck-rust → (file/HTTP export)  (InteractionEvent logs)
plato-ensign → (file output)        (LoRA adapters)
```

## 6. Integration Gaps (P0 / P1)
**P0 (Negative Space):**
- Do not assume holodeck‑rust talks directly to plato‑torch — no imports exist.
- Do not assume fleet‑simulator feeds holodeck — no shared channel yet.

**P1 (Safe Channels):**
- **Tile JSON Format:** Both plato‑torch and fleet‑simulator produce JSONL tiles. Could be a unified ingestion point.
- **HTTP Endpoints:** holodeck‑rust’s `/export` and plato‑torch’s `/tiles` could be read by a common aggregator.
- **File‑System Sharing:** All four projects write to local `output/` directories. A file‑watcher service could connect them.

**P2 (Optimization):**
- Design a `TileBridge` service that watches `output/` directories and forwards tiles to appropriate consumers.
- Extend holodeck‑rust to accept `TrainingTile` inputs via a plugin.
- Add a `SimulationTile` → `TrainingTile` converter in plato‑torch.

## 7. Next Steps for Weaver
1. **Wire GhostInjector into holodeck:** Check if `ghost-injector/` exists; if so, map its output to holodeck’s NPC sentiment system.
2. **Connect DeadbandRoom to plato‑relay:** Inspect `plato-relay/` for WebSocket or HTTP hooks.
3. **Test end‑to‑end pipeline:** Create a minimal script that:  
   a. Runs a fleet‑simulator batch → produces tiles  
   b. Feeds tiles into plato‑torch’s `TileProcessor`  
   c. Triggers plato‑ensign export  
4. **Document integration points:** Update this map with explicit code snippets showing imports and data flow.

---

**Tile Submitted:** Integration map with verified connections and identified gaps.  
**Action:** Proceed to “Wire GhostInjector into holodeck” task.