# Cycle 379

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 379  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Repo:** `plato-torch`  
**Purpose:** Training room presets, self-training rooms.  
**Key files:**
- `presets/DeadbandRoom.py` – Implements Deadband Protocol training.
- `presets/__init__.py` – Exports `DeadbandRoom`, `MirrorRoom`, `GhostRoom`.
- `trainer/room_trainer.py` – Main training loop; uses `torch`, `transformers`.
- `utils/tile_loader.py` – Loads interaction tiles from disk.

**Connections found:**
- **To plato-ensign:** `presets/DeadbandRoom.py` line 42: `from plato_ensign.export import export_lora`. Direct integration for LoRA export.
- **To fleet-simulator:** None found in imports. However, `tile_loader.py` can load `.jsonl` tiles; fleet-simulator outputs `.jsonl` training data (potential indirect channel).
- **To holodeck-rust:** No direct imports. No shared data structures observed.

**Integration gaps:**
- No direct ingestion of `fleet-simulator` output.
- No hook for `holodeck-rust` sentiment logs.

## 2. fleet-simulator
**Repo:** `fleet-simulator`  
**Purpose:** Generate training data via multi‑agent simulation.  
**Key files:**
- `src/simulation/engine.rs` – Core simulation loop.
- `src/output/jsonl_writer.rs` – Writes agent interaction traces as `.jsonl`.
- `config/scenarios/deadband.toml` – Deadband Protocol scenario.

**Connections found:**
- **To plato-torch:** None. Output is generic `.jsonl`; no Plato‑specific schema.
- **To plato-ensign:** None.
- **To holodeck-rust:** None.

**Integration gaps:**
- Simulator output not formatted for `plato-torch` tile format.
- No shared scenario definitions with `holodeck-rust`.

## 3. holodeck-rust
**Repo:** `holodeck-rust`  
**Purpose:** MUD server with sentiment‑aware NPCs.  
**Key files:**
- `src/npc/sentiment.rs` – Tracks NPC emotional state.
- `src/protocol/mod.rs` – Network protocol for client connections.
- `examples/deadband_npc.rs` – Example using Deadband Protocol.

**Connections found:**
- **To plato-torch:** None.
- **To fleet-simulator:** None.
- **To plato-ensign:** None.

**Integration gaps:**
- NPC sentiment logs not consumed by any training pipeline.
- No export path to `plato-torch` tiles.

## 4. plato-ensign
**Repo:** `plato-ensign`  
**Purpose:** Export room experience into LoRA adapters.  
**Key files:**
- `src/export/lora_exporter.py` – Uses `peft` and `torch` to create LoRA.
- `src/load/tile_loader.py` – Loads tiles from `plato-torch` format.

**Connections found:**
- **To plato-torch:** Direct import in `tile_loader.py` (shared tile schema).
- **To fleet-simulator:** None.
- **To holodeck-rust:** None.

**Integration gaps:**
- Only works with `plato-torch` tiles; cannot use `fleet-simulator` or `holodeck-rust` data.

## 5. Summary Map

```
plato-torch ────(direct)───> plato-ensign
    │
    └───(potential)───> fleet-simulator (via .jsonl)
    
fleet-simulator ──(no connection)──> holodeck-rust
holodeck-rust ───(no connection)───> plato-torch
```

## 6. Identified Integration Points (P1 Safe Channels)

1. **fleet-simulator → plato-torch**  
   - Channel: Convert `.jsonl` simulation traces to `plato-torch` tile format.
   - Action: Write adapter in `plato-torch/utils/sim_adapter.py`.

2. **holodeck-rust → plato-torch**  
   - Channel: Export NPC sentiment logs as tiles.
   - Action: Add `export_tiles()` method to `holodeck-rust` sentiment system.

3. **GhostInjector integration** (from backlog)  
   - Channel: `plato-torch` already has `GhostRoom` preset.
   - Action: Wire `GhostInjector` to feed historical failure data into `holodeck-rust` NPCs.

## 7. Next Steps (P2 Optimization)

- **Immediate:** Build `sim_adapter.py` to connect `fleet-simulator` output to `plato-torch`.
- **Test:** Validate end‑to‑end pipeline: `fleet-simulator` → `plato-torch` → `plato-ensign`.
- **Document:** Update `plato-torch` README with integration examples.

**Tile submitted:** `integration_map_cycle379.md`  
**Confidence:** High – based on actual file inspection.  
**Doctrine adherence:** P0 (no rocks) – identified gaps; P1 (safe channels) – proposed adapters; P2 (optimize) – next steps.