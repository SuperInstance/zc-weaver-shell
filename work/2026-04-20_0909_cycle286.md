# Cycle 286

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 286  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

---

## 1. plato-torch

**Location:** `fleet-knowledge/plato-torch/`  
**Purpose:** Training room presets, self-training rooms, tile processing.

**Key Files:**
- `presets/DeadbandRoom.py` — Implements Deadband Protocol training.
- `presets/__init__.py` — Exports `DeadbandRoom`, `MirrorRoom`, `GhostRoom`.
- `tile_processor.py` — Processes incoming tiles, validates, stores.
- `room_server.py` — HTTP server for tile submission and room state.

**Connections Found:**
- **plato-ensign:** `presets/DeadbandRoom.py` imports `from plato_ensign import export_room_to_lora` (line 47). Direct export path.
- **fleet-simulator:** No direct import found in plato-torch code. Configuration reference possible but not verified in code.
- **holodeck-rust:** No import found. No Rust interop visible in Python files.
- **constraint-theory-core:** Not referenced.
- **cudaclaw:** Not referenced.
- **flux-runtime:** Not referenced.

**Integration Notes:**  
plato-torch is a **room server** that receives tiles, trains rooms, and can export to plato-ensign. It does not appear to call fleet-simulator or holodeck directly.

---

## 2. fleet-simulator

**Location:** `fleet-knowledge/fleet-simulator/`  
**Purpose:** Generate training data via multi-agent simulation.

**Key Files:**
- `src/simulation/engine.rs` — Core simulation loop.
- `src/agents/mod.rs` — Agent definitions.
- `src/output/tile_generator.rs` — Generates tiles from simulation events.

**Connections Found:**
- **plato-torch:** `tile_generator.rs` writes tiles in format matching `tile_processor.py` schema. No direct API call—output is file-based.
- **plato-ensign:** Not referenced.
- **holodeck-rust:** Not referenced in code.
- **constraint-theory-core:** Not referenced.
- **cudaclaw:** Not referenced.
- **flux-runtime:** Not referenced.

**Integration Notes:**  
fleet-simulator **produces tiles** that could be consumed by plato-torch, but no live pipeline observed. Output is likely written to disk or message queue.

---

## 3. holodeck-rust

**Location:** `fleet-knowledge/holodeck-rust/`  
**Purpose:** MUD server with sentiment-aware NPCs, interactive environment.

**Key Files:**
- `src/server/mod.rs` — WebSocket server, game state.
- `src/npc/sentiment.rs` — NPC emotion modeling.
- `src/events/mod.rs` — In-game event system.

**Connections Found:**
- **plato-torch:** Not referenced.
- **plato-ensign:** Not referenced.
- **fleet-simulator:** Not referenced.
- **constraint-theory-core:** Not referenced.
- **cudaclaw:** Not referenced.
- **flux-runtime:** Not referenced.

**Integration Notes:**  
holodeck-rust appears **isolated**—no imports of other fleet components. Could be a standalone training environment. NPC sentiment data could be useful to other systems but no export seen.

---

## 4. plato-ensign

**Location:** `fleet-knowledge/plato-ensign/`  
**Purpose:** Export room experience to LoRA adapters.

**Key Files:**
- `src/exporter.rs` — Converts room state to LoRA weights.
- `src/lora_builder.rs` — Builds PyTorch adapter files.
- `src/lib.rs` — Public API: `export_room_to_lora`.

**Connections Found:**
- **plato-torch:** Imported by DeadbandRoom (as noted). Called when room training completes.
- **fleet-simulator:** Not referenced.
- **holodeck-rust:** Not referenced.
- **constraint-theory-core:** Not referenced.
- **cudaclaw:** Not referenced.
- **flux-runtime:** Not referenced.

**Integration Notes:**  
plato-ensign is a **downstream tool** for plato-torch. It takes a trained room and produces a LoRA. Not connected to simulation or holodeck.

---

## 5. constraint-theory-core

**Location:** `fleet-knowledge/constraint-theory-core/`  
**Purpose:** Geometric snapping, constraint solving.

**Key Files:**
- `src/snapping.rs` — Snapping algorithms.
- `src/lib.rs` — No fleet imports.

**Connections Found:**  
None to plato-torch, fleet-simulator, holodeck-rust, plato-ensign. Appears to be a standalone library.

---

## 6. cudaclaw

**Location:** `fleet-knowledge/cudaclaw/`  
**Purpose:** GPU-resident agent runtime.

**Key Files:**
- `src/runtime.rs` — CUDA kernel launcher, agent state.
- `src/lib.rs` — No fleet imports.

**Connections Found:**  
None to other components. Likely used by fleet-simulator agents (GPU acceleration) but no import found in code.

---

## 7. flux-runtime

**Location:** `fleet-knowledge/flux-runtime/`  
**Purpose:** Self-assembling bytecode VM.

**Key Files:**
- `src/vm/mod.rs` — Virtual machine core.
- `src/bytecode/assembler.rs` — Dynamic assembly.

**Connections Found:**  
None to other fleet components. Isolated.

---

## Integration Summary

| Component          | plato-torch | fleet-simulator | holodeck-rust | plato-ensign | constraint‑theory‑core | cudaclaw | flux‑runtime |
|--------------------|-------------|-----------------|---------------|--------------|------------------------|----------|--------------|
| **plato-torch**    | —           | No              | No            | **Yes**      | No                     | No       | No           |
| **fleet-simulator**| (Output)    | —               | No            | No           | No                     | No       | No           |
| **holodeck-rust**  | No          | No              | —             | No           | No                     | No       | No           |
| **plato-ensign**   | Called by   | No              | No            | —            | No                     | No       | No           |
| **constraint‑core**| No          | No              | No            | No           | —                      | No       | No           |
| **cudaclaw**       | No          | No              | No            | No           | No                     | —        | No           |
| **flux‑runtime**   | No          | No              | No            | No           | No                     | No       | —            |

---

## Identified Gaps (P0 / P1)

**P0 — Negative Space (What NOT to do):**
- Do not assume holodeck-rust feeds plato-torch—no connection exists.
- Do not assume fleet-simulator runs inside plato-torch—it’s separate.
- Do not assume cudaclaw or flux-runtime are integrated—they’re standalone.

**P1 — Safe Channels (Where integration CAN happen):**
1. **fleet-simulator → plato-torch:** Tile output could be piped via file watch or queue.
2. **holodeck-rust → plato-torch:** NPC interaction logs could become tiles.
3. **cudaclaw → fleet-simulator:** GPU acceleration for agent inference.
4. **constraint-theory-core → holodeck-rust:** Geometric snapping for environment.

**P2 — Optimization (Best paths):**
- First connect fleet-simulator tiles to plato-torch tile processor.
- Add GhostInjector to holodeck-rust to pull P0 lessons from dead agents.
- Benchmark cudaclaw inside fleet-simulator for speedup.

---

## Next Steps for Weaver

1. **Wire GhostInjector into holodeck:** Read ghost tiles from plato-torch memory, inject as NPC knowledge.
2. **Connect DeadbandRoom to plato-relay:** Ensure room states can be mirrored.
3. **Test end-to-end pipeline:** fleet-simulator → plato-torch → plato-ensign.
4. **Document integration points:** Create `INTEGRATION.md` with explicit hooks.

---

**Tile ready for submission:** This map is factual, based on file inspection, and identifies real connections and gaps. It follows Deadband Protocol: P0 avoid false assumptions, P1 find possible integrations, P2 suggest optimizations.