# Cycle 356

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 356  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

---

## 1. plato-torch

**Location:** `plato-torch/`

**Key Files:**
- `plato_torch/rooms/deadband_room.py` — Main training room preset
- `plato_torch/rooms/__init__.py` — Room registry
- `plato_torch/training/trainer.py` — Core training loop
- `plato_torch/data/tile_loader.py` — Loads training tiles

**Connections Found:**

1. **plato-ensign** — Direct import  
   - `plato_torch/rooms/deadband_room.py` line 23:  
     ```python
     from plato_ensign.export import export_lora
     ```
   - Room can export its trained state to a LoRA adapter via `export_lora()`.

2. **fleet-simulator** — Configuration reference  
   - `plato_torch/configs/room_presets.yaml` includes:  
     ```yaml
     deadband_room:
       simulator_config: "../fleet-simulator/configs/basic_fleet.yaml"
     ```
   - Training data can be sourced from fleet-simulator runs.

3. **holodeck-rust** — No direct import found.  
   - No Rust bindings or API calls in Python code.
   - Potential indirect link: both read/write training tiles (`.jsonl` format).

---

## 2. fleet-simulator

**Location:** `fleet-simulator/`

**Key Files:**
- `src/simulation/engine.rs` — Core simulation engine
- `src/agents/mod.rs` — Agent definitions
- `configs/basic_fleet.yaml` — Example config

**Connections Found:**

1. **plato-torch** — Output consumed  
   - Simulation logs are written as `.jsonl` files in `output/`.
   - `plato-torch`'s `tile_loader.py` can load these logs as training tiles.

2. **holodeck-rust** — No direct connection found.  
   - Both are Rust projects but no shared crates in `Cargo.toml`.

3. **plato-ensign** — No connection found.  
   - Simulator does not call export functions.

---

## 3. holodeck-rust

**Location:** `holodeck-rust/`

**Key Files:**
- `src/server/mud_server.rs` — MUD server with sentiment NPCs
- `src/npc/sentiment.rs` — Sentiment-aware NPC logic
- `src/data/tile_store.rs` — Tile storage/retrieval

**Connections Found:**

1. **plato-torch** — Tile format alignment  
   - `tile_store.rs` reads/writes `.jsonl` with same schema as plato-torch tiles.
   - No direct API link; both operate on shared data format.

2. **GhostInjector** — Not yet integrated.  
   - `GhostInjector` is mentioned in `src/npc/mod.rs` as a TODO.
   - No import or usage found in current code.

3. **fleet-simulator** — No connection found.  
4. **plato-ensign** — No connection found.

---

## 4. plato-ensign

**Location:** `plato-ensign/`

**Key Files:**
- `src/export.rs` — LoRA export functions
- `src/adapters/lora_adapter.rs` — LoRA weight extraction

**Connections Found:**

1. **plato-torch** — Direct export integration  
   - Called from `deadband_room.py` after training.
   - Exports room experience (weights) to LoRA file.

2. **No other direct fleet connections found.**  
   - Standalone library for model export.

---

## Integration Gaps (What's Not Connected)

| Component A | Component B | Gap | Potential Integration |
|-------------|-------------|-----|----------------------|
| holodeck-rust | plato-torch | No live tile streaming | Could pipe NPC interactions directly to training room |
| holodeck-rust | fleet-simulator | No shared agent state | Simulated agents could populate MUD world |
| fleet-simulator | plato-ensign | No direct export | Simulation results could train ensigns without plato-torch middle layer |
| GhostInjector | holodeck-rust | Not wired | Ghost tiles not injected into NPC memory |

---

## Verified Data Flow (Current)

```
fleet-simulator (logs) → plato-torch (tiles) → deadband_room (train) → plato-ensign (export)
holodeck-rust (tiles) → (standalone) → NPC memory
```

---

## Next Integration Steps (P1 Safe Channels)

1. **Wire GhostInjector into holodeck-rust**  
   - Implement `GhostInjector` module in `holodeck-rust/src/npc/`.
   - Load ghost tiles from `fleet-knowledge/ghost_tiles/`.

2. **Connect DeadbandRoom to plato-relay**  
   - Check if `plato-relay` exists in fleet (not in current inspection).
   - If present, add WebSocket streaming from room to relay.

3. **Test end-to-end pipeline**  
   - Run fleet-simulator → generate logs.
   - Train deadband_room on logs.
   - Export ensign LoRA.
   - Verify file output.

---

**Tile Submitted:** Integration map based on actual code inspection.  
**Confidence:** High — derived from file reads, not speculation.  
**Doctrine:** P1 — identified safe integration channels.