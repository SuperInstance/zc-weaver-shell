# Cycle 374

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 374  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch → fleet-simulator
**Connection:** Strong, bidirectional integration via shared data structures and explicit imports.

**Evidence:**
- `plato-torch/src/rooms/deadband_room.py` imports `from fleet_simulator import FleetSimulator, FleetState`.
- `plato-torch/src/data/tile_generator.py` imports `from fleet_simulator.tile_export import export_training_tiles`.
- `fleet-simulator/src/lib.rs` defines `struct TrainingTile` which is serialized to JSON and consumed by `plato-torch`'s tile loaders.
- `fleet-simulator/config/sim_config.yaml` references `plato_torch_model_path: ./models/deadband_lora.safetensors`.

**Data Flow:**
1. `fleet-simulator` runs multi-agent simulations, produces `TrainingTile` records (state, actions, rewards).
2. Tiles are exported to `./training_data/tiles/` as JSONL.
3. `plato-torch`'s `DeadbandRoom` loads these tiles via `TileDataset` for training.

**Gap:** No observed real-time streaming; batch file-based exchange only.

## 2. plato-torch → plato-ensign
**Connection:** Direct plugin relationship; ensign is an optional export module.

**Evidence:**
- `plato-torch/src/ensign/exporter.py` imports `from ..rooms.base_room import BaseRoom`.
- `plato-torch/pyproject.toml` includes `plato-ensign = { path = "../plato-ensign", optional = true }`.
- `plato-ensign/src/lib.rs` provides `export_lora_adapter()` called from Python via `pyo3` bindings.
- `plato-torch/configs/ensign_config.yaml` specifies LoRA rank, alpha, target modules.

**Data Flow:**
1. `DeadbandRoom` trains a model (e.g., `Llama-3-8B` with LoRA).
2. After training, `ensign.export_lora_adapter()` is called, producing a `.safetensors` file.
3. Adapter is saved to `./exports/ensigns/`.

**Gap:** Export is manual/trigger-based; no automatic export on validation metric thresholds.

## 3. holodeck-rust → plato-torch
**Connection:** Weak, indirect via shared tile format.

**Evidence:**
- `holodeck-rust/src/npc/sentiment_engine.rs` produces `InteractionEvent` structs.
- These are serialized to JSON matching `TrainingTile` schema (same fields: `state`, `action`, `reward`).
- `plato-torch/src/data/tile_loader.py` can load JSON from `holodeck-rust/logs/interactions/`.
- No direct imports in either direction.

**Data Flow:**
1. `holodeck-rust` NPC interactions generate logs.
2. Logs are manually copied to `plato-torch` training data directory.
3. `DeadbandRoom` trains on mixed simulator + holodeck tiles.

**Gap:** No automated pipeline; manual file copying required. No direct API calls.

## 4. fleet-simulator → holodeck-rust
**Connection:** None observed.

**Evidence:**
- No imports in either codebase.
- No shared configuration references.
- `fleet-simulator` simulates fleet agents; `holodeck-rust` simulates sentiment-aware NPCs in a MUD.
- Different domains (fleet operations vs. social interaction).

**Gap:** No integration. These are parallel simulation environments.

## 5. plato-ensign → Other Components
**Connection:** Output-only; ensign adapters are consumed downstream.

**Evidence:**
- `cudaclaw/src/runtime/loaders.rs` can load `.safetensors` LoRA adapters.
- `flux-runtime/docs/integration.md` mentions "ensign adapters for specialized tasks".
- No direct imports from `plato-ensign` into other repos.

**Data Flow:**
1. `plato-ensign` produces LoRA adapters.
2. Adapters are manually deployed to `cudaclaw` or `flux-runtime` for inference.
3. No observed automated deployment pipeline.

**Gap:** Manual deployment; no versioning or A/B testing framework.

## 6. constraint-theory-core → Others
**Connection:** Isolated library; no observed integrations.

**Evidence:**
- `constraint-theory-core/src/lib.rs` exports geometric snapping functions.
- No imports by `plato-torch`, `fleet-simulator`, `holodeck-rust`, or `plato-ensign`.
- Possibly intended for future robotics/embodied AI tasks.

**Gap:** Not integrated into any current training or simulation pipeline.

## Integration Summary

| Connection | Strength | Data Flow | Gap |
|------------|----------|-----------|-----|
| plato-torch ↔ fleet-simulator | Strong | Batch JSONL files | No real-time streaming |
| plato-torch → plato-ensign | Strong | Direct plugin call | Manual trigger, no auto-export |
| holodeck-rust → plato-torch | Weak | Manual file copy | No automation |
| fleet-simulator ↔ holodeck-rust | None | — | Separate domains |
| plato-ensign → downstream | Weak | Manual deployment | No CI/CD for adapters |
| constraint-theory-core → any | None | — | Library unused |

## Immediate Integration Opportunities (P1 Channels)
1. **Automate holodeck → plato-torch**: Add a `watch` service that copies new interaction logs to training data directory.
2. **Add auto-export to plato-ensign**: Trigger export when validation loss plateaus.
3. **Create adapter registry**: Version and catalog ensign adapters for `cudaclaw`/`flux-runtime` consumption.
4. **Add real-time tile streaming**: Optional WebSocket stream from `fleet-simulator` to `plato-torch`.

## P0 Negative Space
- Do not force integration between `fleet-simulator` and `holodeck-rust`—different simulation purposes.
- Do not modify `constraint-theory-core` without clear use case.
- Do not break batch file compatibility; many scripts depend on JSONL format.

**Next Task:** Wire GhostInjector into holodeck (requires understanding `holodeck-rust`'s NPC spawning mechanism).