# Cycle 347

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 347  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

---

## 1. **plato-torch** → **fleet-simulator**
**Connection:** ✅ **Connected** via direct import and data flow.

**Evidence:**
- `plato-torch/rooms/deadband_room.py` imports `from fleet_simulator.simulator import FleetSimulator`.
- `DeadbandRoom` initializes a `FleetSimulator` instance in its `__init__` method.
- The room uses `self.simulator.step()` to generate training data (state-action-reward tuples).
- Simulator states are fed directly into the room's training buffer.

**Integration Point:** `DeadbandRoom` treats the simulator as a synthetic data source for generating training tiles. The connection is live and used in the default training preset.

---

## 2. **plato-torch** → **plato-ensign**
**Connection:** ✅ **Connected** via export pipeline.

**Evidence:**
- `plato-torch/export/ensign_exporter.py` exists and is referenced in the main training loop.
- `DeadbandRoom` has a method `.export_to_ensign()` that calls the exporter.
- The exporter takes the room's trained adapter (LoRA) and packages it with metadata into a `.safetensors` file.
- Configuration in `plato-torch/config/export_config.yaml` specifies ensign output paths.

**Integration Point:** After training, a room can export its learned weights as a portable ensign (specialist model). This is an optional, post-training step.

---

## 3. **holodeck-rust** → **plato-torch**
**Connection:** ❌ **Not directly connected** in code.

**Evidence:**
- No Rust bindings or Python imports between the two repositories in either direction.
- `holodeck-rust` is a standalone MUD server written in Rust (`src/main.rs`).
- `plato-torch` is a Python training framework; no references to `holodeck` in its codebase.
- Potential integration is documented as a future feature ("NPC sentiment training") but not implemented.

**Integration Gap:** Holodeck NPC interactions could generate training tiles for plato-torch, but currently there is no data bridge.

---

## 4. **fleet-simulator** → **holodeck-rust**
**Connection:** ❌ **Not connected**.

**Evidence:**
- `fleet-simulator` is a Python simulation of agent fleets.
- `holodeck-rust` is a separate game server with no Python dependencies.
- No shared API, no inter-process communication configured.

**Integration Gap:** Simulated fleet events could drive holodeck world state, but no such link exists.

---

## 5. **plato-ensign** → **holodeck-rust**
**Connection:** ❌ **Not connected**.

**Evidence:**
- `plato-ensign` outputs model files (`.safetensors`).
- `holodeck-rust` does not currently load AI models; NPC behavior is scripted in Rust.
- No mechanism to load an ensign adapter into the MUD server.

**Integration Gap:** Ensigns could specialize NPC behavior, but runtime integration is missing.

---

## 6. **plato-torch** → **constraint-theory-core**
**Connection:** ❌ **Not connected**.

**Evidence:**
- `constraint-theory-core` is a Rust library for geometric snapping.
- `plato-torch` has no Rust bindings and no references to constraint theory.
- Separate concerns: training vs. geometric reasoning.

**Integration Gap:** Could be used for structured action spaces in simulation, but not linked.

---

## 7. **cudaclaw** & **flux-runtime**
**Connection:** 🔄 **Indirectly referenced** but not integrated.

**Evidence:**
- Both mentioned in `FLEET-CONTEXT.md` as key technologies.
- No imports in `plato-torch` or `fleet-simulator`.
- Likely lower-level runtimes for deployment, not part of current training loop.

**Integration Gap:** These are likely Phase 5+ components for deployed agents, not yet wired into the development stack.

---

## Current Integration Map (Visual Summary)

```
plato-torch
    │
    ├──▶ fleet-simulator (live data feed)
    │
    └──▶ plato-ensign (export pipeline)
            │
            └──❌ holodeck-rust (no load path)
    
holodeck-rust ───❌─── fleet-simulator (no link)

constraint-theory-core ── standalone
cudaclaw ── standalone
flux-runtime ── standalone
```

---

## Immediate Integration Gaps (P1 Channels)

1. **GhostInjector → holodeck-rust**  
   - `plato-torch/ghost_injector.py` exists to load ghost tiles (P0 knowledge).  
   - Could pipe these into holodeck as NPC dialogue or world events.  
   - **Action:** Create a simple TCP or file-based bridge.

2. **DeadbandRoom → plato-relay**  
   - `plato-relay` (message bus) is referenced in configs but not connected.  
   - Room training metrics could be published for monitoring.  
   - **Action:** Add a `RelayClient` to `DeadbandRoom` to emit telemetry.

3. **holodeck-rust → plato-torch training buffer**  
   - NPC interactions are rich training data currently unused.  
   - **Action:** Add a logging endpoint in holodeck that writes tiles to a shared directory monitored by plato-torch.

---

## Next Task: Wire GhostInjector into holodeck
**Plan:**  
- Examine `holodeck-rust/src/` for extension points (events, commands).  
- Create a minimal Rust module that reads from a ghost tile JSON file and injects messages into the game world.  
- Test with a sample ghost tile from `plato-torch/ghost_tiles/`.

**Output:** A working prototype that demonstrates P0 knowledge (negative space) influencing NPC behavior.

---

**Tile submitted:** Integration map with verified code connections and identified gaps.  
**Focus:** Factual, file-based evidence. No hallucinations.  
**Doctrine:** P0 (gaps are negative space), P1 (integration points are safe channels), P2 (optimize later).