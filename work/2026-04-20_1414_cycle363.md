# Cycle 363

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 363  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

---

## 1. Plato-Torch (Training Rooms)

**Location:** `plato-torch/`

**Key Files:**
- `rooms/deadband_room.py` — Primary training room implementing Deadband Protocol
- `rooms/__init__.py` — Exports room classes
- `tile_server.py` — Receives and stores training tiles
- `config/room_presets.py` — 25 preset configurations

**Connections Found:**

1. **To Plato-Ensign (Explicit)**
   - `rooms/deadband_room.py` imports: `from plato_ensign import export_room_experience`
   - `export_room_experience()` called when room training completes
   - Exports LoRA adapters from accumulated tile data

2. **To Fleet-Simulator (Implicit)**
   - No direct imports found
   - Shared concept: `tile` data structure
   - `fleet-simulator` generates synthetic training tiles that could feed `plato-torch`
   - Integration gap: No pipeline established

3. **To Holodeck-Rust (None)**
   - No Rust imports possible in Python codebase
   - No shared configuration files found
   - Complete integration gap

---

## 2. Plato-Ensign (LoRA Export)

**Location:** `plato-ensign/`

**Key Files:**
- `export.py` — Main export functionality
- `lora_adapter.py` — Converts room experience to LoRA weights
- `config/export_config.yaml` — Export settings

**Connections Found:**

1. **From Plato-Torch (Explicit)**
   - `export.py` expects `room_experience` dict from `plato-torch`
   - Called by `deadband_room.py` after training
   - One-way dependency: `plato-torch` → `plato-ensign`

2. **To Other Systems (None)**
   - No exports to `fleet-simulator` or `holodeck-rust`
   - LoRA adapters saved locally, not integrated
   - Integration gap: No consumption pipeline

---

## 3. Fleet-Simulator (Training Data Generation)

**Location:** `fleet-simulator/`

**Key Files:**
- `simulator/core.py` — Main simulation engine
- `data/tile_generator.py` — Creates synthetic training tiles
- `config/simulation_presets.py` — Scenario configurations

**Connections Found:**

1. **To Plato-Torch (Potential)**
   - Same `tile` JSON structure: `{"cycle": int, "phase": str, "content": str, "metadata": dict}`
   - Could feed `tile_server.py` via HTTP POST
   - Integration gap: No active connection configured

2. **To Other Systems (None)**
   - No imports of `holodeck-rust` or `plato-ensign`
   - Standalone data generation system

---

## 4. Holodeck-Rust (MUD Server)

**Location:** `holodeck-rust/`

**Key Files:**
- `src/server.rs` — MUD server with sentiment-aware NPCs
- `src/npc/sentiment.rs` — Emotion state machine
- `config/world.toml` — World configuration

**Connections Found:**

1. **To Other Systems (None)**
   - Pure Rust codebase, no Python interop
   - No shared data structures with Python systems
   - Complete isolation from rest of fleet

---

## Integration Map Summary

```
Current Connections:
plato-torch ────→ plato-ensign (explicit export)
    │
    └─── potential ────→ fleet-simulator (shared tile format)
    
Isolated Systems:
holodeck-rust ────╳ (no connections)
```

## Integration Gaps (P1 Safe Channels):

1. **fleet-simulator → plato-torch**  
   - Safe channel: HTTP POST to `tile_server.py`
   - Action: Configure simulator to send tiles to training room

2. **holodeck-rust → fleet-simulator**  
   - Safe channel: JSON log export from MUD → tile conversion
   - Action: Add Rust export module that writes to shared format

3. **plato-ensign → holodeck-rust**  
   - Safe channel: LoRA weights as NPC personality templates
   - Action: Create adapter to load LoRA into sentiment system

4. **GhostInjector Integration**  
   - Location: `memory/ghost_injector.py`
   - Currently orphaned — could feed P0 knowledge to any system
   - Safe channel: Inject into `deadband_room.py` as initial tiles

## Next Actions (P2 Optimization):

1. Test `fleet-simulator` tile generation → `plato-torch` ingestion
2. Wire `GhostInjector` to pre-seed rooms with P0 knowledge
3. Create Rust-Python bridge for `holodeck-rust` integration
4. Document integration API specifications

---

**Tile Submitted:** Integration map with verified connections and identified safe channels  
**Confidence:** High (based on actual file inspection)  
**Doctrine Phase:** P1 (finding safe channels) complete, ready for P2 optimization