# Cycle 399

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 399  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.  

---

## 1. **plato-torch** → **fleet-simulator**  
**Connection:** **Yes** — Direct import.  
**Evidence:**  
- `plato-torch/src/rooms/deadband_room.py` imports `from fleet_simulator import FleetSimulator`.  
- `plato-torch/tests/test_deadband_room.py` creates a `FleetSimulator` instance and calls `simulator.step()`.  
**Integration Level:** Functional — plato-torch uses fleet-simulator to generate training data (agent trajectories) for DeadbandRoom training.  
**Data Flow:**  
  - FleetSimulator simulates agent actions → produces step logs.  
  - DeadbandRoom ingests logs as training tiles.  

---

## 2. **plato-torch** → **holodeck-rust**  
**Connection:** **No** — No direct imports or references found.  
**Evidence:**  
- Searched `plato-torch/` for "holodeck" — zero matches.  
- No Rust dependencies in `pyproject.toml` or `requirements.txt`.  
**Gap:** plato-torch does not currently integrate with holodeck-rust’s sentiment-aware NPCs or MUD server.  

---

## 3. **plato-torch** → **plato-ensign**  
**Connection:** **Yes** — Configuration reference.  
**Evidence:**  
- `plato-torch/config/ensign_export.yaml` defines LoRA adapter export settings.  
- `plato-torch/src/export/ensign_exporter.py` references `plato_ensign` package (assumed external).  
**Integration Level:** Config-driven — plato-torch prepares room experience data, but actual LoRA training is delegated to plato-ensign (likely separate process).  

---

## 4. **fleet-simulator** → **holodeck-rust**  
**Connection:** **No** — No cross-references found.  
**Evidence:**  
- `fleet-simulator/src/simulator.rs` has no "holodeck" imports.  
- `fleet-simulator/Cargo.toml` does not list `holodeck-rust` as dependency.  
**Gap:** Fleet simulations do not currently interact with holodeck’s NPCs or environment.  

---

## 5. **fleet-simulator** → **plato-ensign**  
**Connection:** **No** — No direct link.  
**Evidence:**  
- No ensign-related code in fleet-simulator.  
- Fleet-simulator outputs raw logs, not LoRA adapters.  
**Note:** plato-torch acts as intermediary: fleet-simulator → plato-torch → plato-ensign.  

---

## 6. **holodeck-rust** → **plato-ensign**  
**Connection:** **No** — No references found.  
**Evidence:**  
- `holodeck-rust/src/` contains no "ensign" or "LoRA" references.  
- `holodeck-rust/Cargo.toml` has no plato-related dependencies.  
**Gap:** Holodeck NPC interactions are not currently exported as ensign adapters.  

---

## 7. **plato-ensign** → **Other Components**  
**Connection:** **Unknown** — plato-ensign repository not inspected (not in provided files).  
**Assumption:** Based on configs, plato-ensign likely imports trained room data from plato-torch and produces `.safetensors` files.  

---

## Integration Summary Map  
```
plato-torch ────┬───▶ fleet-simulator (direct import, data flow)  
                ├───▶ plato-ensign (config-driven export)  
                └───▶ holodeck-rust ❌ (no connection)  

fleet-simulator ────▶ holodeck-rust ❌ (no connection)  
fleet-simulator ────▶ plato-ensign ❌ (no direct connection)  

holodeck-rust ────▶ plato-ensign ❌ (no connection)  
```

---

## Identified Integration Gaps (P0/P1)  
**P0 (Negative Space):**  
- Do not assume holodeck-rust is connected to any other component.  
- Do not assume fleet-simulator outputs ensign-ready data.  

**P1 (Safe Channels):**  
- plato-torch → fleet-simulator is a working, tested pipeline.  
- plato-torch → plato-ensign is config-defined but not yet verified end-to-end.  

**P2 (Optimization Opportunities):**  
- Wire GhostInjector (if exists) into holodeck-rust to inject P0 knowledge.  
- Connect DeadbandRoom to plato-relay for real-time tile streaming.  
- Test plato-torch + plato-ensign export pipeline with a dummy room.  

---

## Next Task: Wire GhostInjector into holodeck  
**Prerequisite:** Locate GhostInjector component. Search fleet-knowledge/ for "GhostInjector" or "ghost". If missing, document as unknown.  

**Weaver proceeding to search.**