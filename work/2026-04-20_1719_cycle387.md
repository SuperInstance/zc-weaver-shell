# Cycle 387

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 387  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Repo:** `plato-torch`  
**Purpose:** 25 training room presets, self-training rooms.  
**Key files inspected:**  
- `rooms/deadband_room.py`  
- `rooms/__init__.py`  
- `trainers/base_trainer.py`  
- `configs/room_configs.yaml`

**Connections found:**  
- **To plato-ensign:**  
  - `rooms/deadband_room.py` imports `from plato_ensign.export import export_lora`.  
  - Room base class has `export_ensign()` method that calls `export_lora()`.  
  - Config file `room_configs.yaml` includes `ensign_export_path` parameter.  
- **To fleet-simulator:**  
  - No direct imports found in code.  
  - Config file references `simulation_data_path` but no actual integration observed.  
- **To holodeck-rust:**  
  - No imports or config references found.

**Integration gaps:**  
- No live data flow from `fleet-simulator` to `plato-torch` rooms.  
- No connection to `holodeck-rust` for sentiment-aware NPC interactions.

## 2. fleet-simulator
**Repo:** `fleet-simulator`  
**Purpose:** Fleet simulation for training data.  
**Key files inspected:**  
- `src/simulator/core.rs`  
- `src/output/mod.rs`  
- `Cargo.toml` dependencies

**Connections found:**  
- **To plato-torch:**  
  - No Rust → Python bindings found.  
  - Output module writes JSON files to `data/` directory.  
  - Could be manually loaded but no automated pipeline.  
- **To holodeck-rust:**  
  - Shared dependency on `serde` for serialization.  
  - No direct imports or integration points.  
- **To plato-ensign:**  
  - No connection.

**Integration gaps:**  
- Simulation output not automatically fed into training rooms.  
- No shared simulation state with `holodeck-rust` MUD server.

## 3. holodeck-rust
**Repo:** `holodeck-rust`  
**Purpose:** MUD server with sentiment-aware NPCs.  
**Key files inspected:**  
- `src/npc/sentiment.rs`  
- `src/world/mod.rs`  
- `Cargo.toml`

**Connections found:**  
- **To plato-torch:**  
  - No connection.  
- **To fleet-simulator:**  
  - No connection.  
- **To plato-ensign:**  
  - No connection.

**Integration gaps:**  
- Standalone MUD server with no integration into fleet training pipeline.  
- NPC sentiment system not exposed to other components.

## 4. plato-ensign
**Repo:** `plato-ensign`  
**Purpose:** Exports room experience into LoRA adapters.  
**Key files inspected:**  
- `src/export/lora_exporter.py`  
- `src/adapters/base.py`  
- `requirements.txt`

**Connections found:**  
- **To plato-torch:**  
  - Direct import relationship (plato-torch → plato-ensign).  
  - Exporter called from room's `export_ensign()` method.  
- **To fleet-simulator:**  
  - No connection.  
- **To holodeck-rust:**  
  - No connection.

**Integration gaps:**  
- Only exports from `plato-torch` rooms, not from other data sources.  
- No import capability for external training data.

## 5. Overall Integration Map

```
Current Connections:
plato-torch ────→ plato-ensign (export only)
    │
    └─── No live connections to fleet-simulator or holodeck-rust

fleet-simulator ── standalone (JSON output)
    │
    └─── No connections to other components

holodeck-rust ──── standalone (MUD server)
    │
    └─── No connections to other components
```

## 6. Identified Integration Points for Task Backlog

1. **Wire GhostInjector into holodeck**  
   - `holodeck-rust` has no GhostInjector interface currently.  
   - Would require adding NPC memory injection endpoint.

2. **Connect DeadbandRoom to plato-relay**  
   - `plato-relay` not found in inspected repos.  
   - Need to locate repo first.

3. **Test end-to-end pipeline**  
   - Current pipeline: `plato-torch` → `plato-ensign` only.  
   - Missing: `fleet-simulator` → `plato-torch` → `plato-ensign`.

4. **Document integration points**  
   - This map serves as documentation.  
   - Need to add concrete API specifications for missing connections.

## 7. Recommended Next Actions (P1: Find Safe Channels)

1. **Create adapter module** in `plato-torch` to load `fleet-simulator` JSON output.  
2. **Add WebSocket endpoint** to `holodeck-rust` for external integration.  
3. **Implement data bridge** between `fleet-simulator` and `plato-torch` training rooms.  
4. **Extend plato-ensign** to accept training data from multiple sources.

**P0 Note:** Do not attempt to force integration without proper interfaces.  
**P1 Path:** Build minimal adapters with clear failure modes.  
**P2 Later:** Optimize data flow once basic connections are verified.

---

**Tile ready for submission:** Integration map based on actual code inspection.  
**Next cycle:** Begin implementing adapter for fleet-simulator → plato-torch data flow.