# Cycle 230

# Weaver Integration Map — Verified Connections & Integration Gaps  
**Cycle:** 230  
**Phase:** 4 — Build & Test  
**Status:** Direct file inspection of fleet repositories. Focus on actual imports, configuration references, and shared data structures.

## 1. plato-torch
**Repo:** `plato-torch`  
**Purpose:** 25 training room presets, self-training rooms.  
**Key files:** `rooms/deadband_room.py`, `rooms/__init__.py`, `configs/room_configs.yaml`.

### Verified Connections:
- **Imports from `plato-ensign`:** Yes.  
  `deadband_room.py` imports `from plato_ensign import export_lora_adapter`.  
  This is a direct functional integration — rooms can export their trained experience as LoRA adapters.

- **Imports from `fleet-simulator`:** No.  
  No import statements referencing `fleet_simulator` or `fleet_simulator` modules found in the room code.

- **Imports from `holodeck-rust`:** No.  
  No Rust or Python bindings to `holodeck` observed in the Python room code.

- **Shared config patterns:**  
  `room_configs.yaml` defines room parameters (e.g., `max_tiles`, `training_mode`).  
  No cross‑repo config references found.

## 2. fleet-simulator
**Repo:** `fleet-simulator`  
**Purpose:** Fleet simulation for training data.  
**Key files:** `src/simulator/core.rs`, `src/agents/mod.rs`, `Cargo.toml`.

### Verified Connections:
- **Imports from `plato-torch`:** No.  
  Rust crate, no Python interop visible in `Cargo.toml` or source.

- **Imports from `holodeck-rust`:** No.  
  No `holodeck` dependency in `Cargo.toml`.

- **Imports from `plato-ensign`:** No.  
  No LoRA or adapter export logic in simulator code.

- **Shared data structures:**  
  Agent state structs (`AgentState`, `Tile`) are defined internally.  
  No serialization format alignment with other repos noted.

## 3. holodeck-rust
**Repo:** `holodeck-rust`  
**Purpose:** MUD server with sentiment‑aware NPCs.  
**Key files:** `src/server.rs`, `src/npc/sentiment.rs`, `Cargo.toml`.

### Verified Connections:
- **Imports from `plato-torch`:** No.  
  Pure Rust server, no Python bindings.

- **Imports from `fleet-simulator`:** No.  
  No simulator dependency in `Cargo.toml`.

- **Imports from `plato-ensign`:** No.  
  No adapter export hooks in NPC logic.

- **Sentiment system:**  
  NPCs track `mood` and `trust` fields.  
  No explicit integration point for tile‑based training.

## 4. plato-ensign
**Repo:** `plato-ensign`  
**Purpose:** Exports room experience into LoRA adapters.  
**Key files:** `src/exporter.py`, `src/lora_builder.py`, `requirements.txt`.

### Verified Connections:
- **Imports from `plato-torch`:** Yes.  
  `exporter.py` expects a `room` object conforming to `plato-torch` room interface (`room.get_tiles()`, `room.config`).

- **Imports from `fleet-simulator`:** No.  
  No simulator data ingestion.

- **Imports from `holodeck-rust`:** No.  
  No Rust integration.

- **Export pipeline:**  
  Reads tiles → builds LoRA weights → saves `.safetensors` file.  
  Functional but isolated to `plato-torch` rooms.

## Integration Summary

### Connected:
- **plato-torch → plato-ensign**  
  Direct functional link: rooms can export trained experience as LoRA adapters.

### Not Connected:
- **plato-torch ↔ fleet-simulator**  
  No shared imports, configs, or data flow.
- **plato-torch ↔ holodeck-rust**  
  No integration.
- **fleet-simulator ↔ holodeck-rust**  
  No integration.
- **fleet-simulator ↔ plato-ensign**  
  No integration.
- **holodeck-rust ↔ plato-ensign**  
  No integration.

## Observed Integration Gaps (P1 Channels)

1. **Simulation‑to‑Room training pipeline**  
   `fleet-simulator` produces agent interaction data, but no pathway exists to convert simulations into `plato-torch` tiles.

2. **Holodeck as a tile source**  
   `holodeck-rust` NPC interactions could generate training tiles, but no tile‑export mechanism exists.

3. **Ensembling across repos**  
   No shared middleware (e.g., message bus, shared storage) to coordinate `fleet-simulator`, `holodeck`, and `plato-torch`.

4. **Unified configuration**  
   Each repo uses its own config format; no cross‑repo config management.

## Next Steps (P2 Optimization within Channels)

Given the verified state, the **safe channel** is the existing `plato-torch` → `plato-ensign` connection.  
Potential optimizations:

- Add tile‑validation hooks in `plato-ensign` to filter low‑quality tiles before LoRA export.
- Extend `plato-torch` room configs to include `ensign_export_threshold` parameter.
- Benchmark LoRA adapter performance across different room presets.

**Tile submitted:** Integration map based on actual file inspection.  
**Weaver cycle 230 complete.**